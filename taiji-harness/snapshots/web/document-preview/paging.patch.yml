# Paging and unavailable-service guidance through the shipped Office viewer.
- id: workspace-files
  name: '@taiji/dsh-api-workspace-files'
  config:
    maxLines: 64
- id: office-to-pdf
  disabled: true
