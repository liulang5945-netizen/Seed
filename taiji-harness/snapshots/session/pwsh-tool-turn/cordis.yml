# Minimal live counterpart for the pwsh-tool-turn snapshot composition.
- id: llm-deepseek
  name: '@taiji/dsh-llm-deepseek'
  config:
    models:
      - id: deepseek-v4-pro

- id: subprocess
  name: '@taiji/dsh-subprocess-local'

- id: bash-sandbox
  name: '@taiji/dsh-bash-sandbox'
  disabled: true

- id: pwsh-sandbox
  name: '@taiji/dsh-pwsh-sandbox'
  disabled: false

- id: shell-env
  name: '@taiji/dsh-shell-env'

- id: agent-default-model
  name: '@taiji/dsh-agent-default-model'
  config:
    provider: deepseek-official
    model: deepseek-v4-pro

- id: session-persistence-jsonl
  name: '@taiji/dsh-session-persistence-jsonl'
  config:
    root: !!js dshHomePath('sessions')
    compression: !!js 'process.env.DSH_SNAPSHOT === undefined ? ''zstd'' : ''none'''

- id: agent-instructions
  name: '@taiji/dsh-agent-instructions'
  disabled: true

- id: system-prompt
  name: '@taiji/dsh-system-prompt'
  config:
    personaPrefix: You are a concise snapshot agent working in {{cwd}}.

- id: goal
  name: '@taiji/dsh-goal'
  disabled: true

- id: goal-round-driver
  name: '@taiji/dsh-goal-round-driver'
  disabled: true

- id: command-goal
  name: '@taiji/dsh-command-goal'
  disabled: true

- id: tool-goal
  name: '@taiji/dsh-tool-goal'
  disabled: true

- id: skill
  name: '@taiji/dsh-skill'
  disabled: true

- id: skill-filesystem
  name: '@taiji/dsh-skill-filesystem'
  disabled: true

- id: tool-skill
  name: '@taiji/dsh-tool-skill'
  disabled: true

- id: tool-bash
  name: '@taiji/dsh-tool-bash'
  disabled: true

- id: tool-pwsh
  name: '@taiji/dsh-tool-pwsh'
  disabled: false
