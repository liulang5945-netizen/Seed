# With 1,800 tokens of headroom, a 1,000-token output reservation lowers the
# trigger from 8,240 to 7,500, between the history and its condensed replacement.

- id: llm-deepseek
  name: '@taiji/dsh-llm-deepseek'
  disabled: true

- id: session-persistence-jsonl
  name: '@taiji/dsh-session-persistence-jsonl'
  config:
    root: !!js dshHomePath('sessions')
    compression: none

- id: compaction-basic
  name: '@taiji/dsh-compaction-basic'
  config:
    thresholdRatio: 0.8
    headroomTokens: 1800
    retainTokens: 20
    maxTokens: 32

- insert:
    - id: llm-replay
      name: '@taiji/dsh-llm-replay'
      config:
        providers:
          - id: deepseek-official
            name: DeepSeek
            models:
              - id: deepseek-v4-flash
                contextWindow: 10300
                defaultMaxTokens: 1000
