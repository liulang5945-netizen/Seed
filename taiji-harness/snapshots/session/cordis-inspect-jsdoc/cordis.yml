# Add PTC mode and Cordis tools to the base spawn/workflow stack, exercising
# all four boundaries in one ACP snapshot.
- id: agent-default-model
  name: '@taiji/dsh-agent-default-model'
  config:
    provider: deepseek-official
    model: deepseek-v4-pro

- id: session-persistence-jsonl
  name: '@taiji/dsh-session-persistence-jsonl'
  config:
    root: !!js dshHomePath('sessions')
    compression: !!js 'process.env.DSH_SNAPSHOT === undefined ? ''zstd'' : ''none'''

- id: agent-instructions
  name: '@taiji/dsh-agent-instructions'
  config:
    maxBytes: 65536

- id: tools
  name: '@taiji/dsh-tools'
  config:
    mode: both

- id: system-prompt
  name: '@taiji/dsh-system-prompt'
  config:
    personaPrefix: |
      You are a coding assistant powered by the {{model}} model. Your working directory is {{cwd}}.

      Verify your work by running the code or tests. Keep answers brief and factual.

- insert:
    - id: cordis-host-runner
      name: '@taiji/dsh-cordis-host-runner'
    - id: cordis-inspect-providers
      name: '@taiji/dsh-tool-cordis/host'
    - id: tool-cordis
      name: '@taiji/dsh-tool-cordis'
