- name: '@taiji/dsh-session'
- name: '@taiji/dsh-system-prompt'
- name: '@taiji/dsh-tools'
- name: '@taiji/dsh-fs-local'
- name: '@taiji/dsh-tool-fs'
- name: '@taiji/dsh-session-query-sqlite'
  config:
    path: ':memory:'
    openAt: never
- name: '@taiji/dsh-session-reference'
  config:
    maxReferenceBytes: 360
- name: './source-session.ts'
- name: '@taiji/dsh-spill-local'
  config:
    root: '{{spillRoot}}'
    cleanupPeriodDays: 0
