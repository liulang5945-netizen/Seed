# 2026-09-22 工作日志

## R2 重训语言读出：B 臂完成、C 臂续跑；修两处我自己的缺陷

**取证时刻 10:00（GMT+8）**：

| 臂 | 状态 | 关键读数 |
|---|---|---|
| **A** | **completed** | 16,000,000 跑满、写入面 **`['predictive_readout']`**、11.72 h、sha `732e59cf…` |
| **B** | **completed** | 16,000,000 跑满、写入面 **`['motor']`**、18.64 h（中位窗口 0.0038199）、sha `766dfa04…` |
| C | 在 12,250,000 处**进程死亡**，已续跑 | 中位窗口 0.0024165（首/末 0.0024264 / 0.0023641，平坦）⇒ 剩 4M 约 **2.7 h** |

⇒ **写入面隔离在 A、B 两臂都拿到实测**：合同要隔离的那个自变量（A 只写读出头 / B 只写运动面）
不是声明而是实测。C 跑完三臂才齐。

### 🔴 我上一轮对所有者说错的话（必须记在明处）

我说"重跑同一条命令即可续跑，幂等"。**那句话当时不成立。**
原实现对"已跑满的臂"`raise SystemExit` ⇒ 多臂 campaign 会**在第一个跑完的臂上中止**。
于是 A/B 早已完成，C 在进程死掉后**没有任何 driver 能接上它**，`campaign_report.json` 也从未生成。

- **修**：引入 `ArmAlreadyComplete`；campaign 层**跳过并记录** `arms_status`；`--fresh` 是唯一重做通路。
- **回归** `test_a_finished_arm_is_skipped_so_a_campaign_can_be_re_run`：同一条命令复跑 ⇒
  `arms_status == {B: already_complete, C: already_complete}`、`failed_arms == []`，
  且 C 的 `session_id` **逐字不变**（证明没被偷偷重跑）。
- **实测续跑生效**：重启后输出
  `[A] already_complete … skipping` / `[B] already_complete … skipping` / `[C] start`，C 随即继续。

### 🔴 第二个缺陷：`--fresh` 在 Windows 上必崩

归档旧报告的文件名用了 `_utc_now()`，含 `:` ⇒ `OSError [WinError 123] 文件名、目录名或卷标语法不正确`。
**是上面那条新写的测试当场抓到的**——此前 `--fresh` 路径**从未被执行过**，所以这个崩溃一直躺着。
修：新增 `_stamp_for_filename()`（`:` → `-`），JSON 字段仍用 `_utc_now()`。

### 可复用教训（已入 MEMORY §4）

1. 「幂等重跑」这类承诺**必须用测试证明，而且要证多臂情形**——单臂测试全绿挡不住 campaign 层中止。
2. **时刻进文件名要换安全字符集**：`:` 在 Windows 非法。
3. 长跑的 driver 要**分臂记状态**（`arms_status`），否则"哪一臂停了"只能靠翻进度流猜。
4. **报速率报中位窗口，不报累计**（累计把一次性扰动永久计入，看起来像模型性质）。
5. 空窗口的指标写 `null`，**不写 `0.0`**（0.0 会被读成塌陷）。

回归：runner 合同测试 10 → **11 passed**；ruff 零告警。提交 `1be6371f`；
合同 §4.5 与 03 队首第十六批已记录全部。

## 优雅停机新增（应所有者"想重启电脑、能否存最新进度"）

**取证 11:06**：C 臂**已保存点 = 13,500,000 / 16,000,000**（`saved_at_utc=2026-09-22T03:01:31Z`，
5.1 分钟前），本次会话速率 0.0023877 s/符号 ⇒ 剩 2.5M 约 **1.66 h**；
**边界之后约 127,654 符号只在内存里**（约 5 分钟计算），下一落盘边界 14,000,000 还要 14.8 分钟。

**诚实的回答是"当时不能"**：落盘只发生在 `consumed % checkpoint_every` 边界上（C 用 500,000 ≈ 20 分钟），
所以"最新进度"只能等于"最后一次边界"。

**新增能力**：`--stop-file`（缺省 `<out-dir>/STOP_RUN`）+ `--stop-check-every`（缺省 10,000）。
哨兵一出现 ⇒ 落盘并退出；报告标 `stopped_by_request`（**不是失败**），并给出 `arms_pending` 与
`resume_command`；哨兵用一次即失效。回归 `test_a_stop_file_saves_and_exits_cleanly`
（含"停机后接着跑，`session_symbols` 只算新增"）。**停机损失：一个 checkpoint 间隔 → ≤10,000 符号（≈24 s）**。

**同批修掉**：语料比预算短时甩**裸 `StopIteration` 栈** ⇒ 改为带"第几臂/跑到第几个/跳过量"的
`SystemExit`。**是 stop-file 的测试撞出来的**。

**新增能力救不了当前这个已跑的进程**（它加载的是旧代码，不会看哨兵）⇒ 这次重启的损失是固定的
（约 5.1 分钟）。与"再等 15 分钟到 14M 边界"相比：省 5 分钟 CPU，花 15 分钟人的时间。
⇒ 建议**现在就重启**，续训把 `--checkpoint-every` 降到 **100,000**（此后任何中断最多丢 4 分钟）。

runner 合同测试 11 → **12 passed**；ruff 零告警。

### MEMORY.md 单方合并瘦身（应所有者要求，同日完成）

**5,543 → 2,996 字符**（注入上限 3,000）。做法不是删，是**移出**：

- 新建 `docs/DESKTOP_AUTOMATION_PITFALLS.md`，把前端工具链 / Electron 壳 / 进程守卫 / electron-builder /
  NSIS 的**逐条实测**原文搬过去（含 `extraFiles` 必须排除 `Seed.exe`、`clean-release.mjs` 改为只重命名
  等细节）；MEMORY §4 只留一行指针 + 三条最容易烧到人的（Vite 不重写 `.js`→`.ts`、
  `ELECTRON_RUN_AS_NODE=1` 的假"二进制没装"、NSIS `/D=` 必须加引号）。
- 逐节压缩措辞，**规则一条没删**：§1 架构契约 3 条、§2 环境硬约束 5 条、§3 文档四类与 5 条纪律、
  §4 判据 8 条、§5 状态与回退点，全部在。
- 唯一**移出 MEMORY.md 且不再逐行保留**的是「序列级 ≠ 位置级」那条判据——它仍在已归档的
  `plans/reference/M5_R2_READOUT_DIAGNOSIS_20260917.md`，且 §5 的 `plans/reference/` 指针指向它。
- 复跑守卫：目录结构守卫 + 泄露守卫 **11 passed**（新 doc 不触发任何规则）。

**给后来者的提醒**：这个文件是**两条并行线共写**的，各自追加很快会再次越过 3,000。
瘦身必须**单方主导一次做完**（两人同时压会互相覆盖）；移出的内容要落到 `docs/` 下的专文并在
MEMORY 留指针，**不要直接删**。
