# 2026-09-22 工作日志

## R2 重训语言读出：B 臂完成、C 臂续跑；修两处我自己的缺陷

**取证时刻 10:00（GMT+8）**：

| 臂 | 状态 | 关键读数 |
|---|---|---|
| **A** | **completed** | 16,000,000 跑满、写入面 **`['predictive_readout']`**、11.72 h、sha `732e59cf…` |
| **B** | **completed** | 16,000,000 跑满、写入面 **`['motor']`**、18.64 h（中位窗口 0.0038199）、sha `766dfa04…` |
| C | 在 12,250,000 处**进程死亡**，已续跑 | 中位窗口 0.0024165（首/末 0.0024264 / 0.0023641，平坦）⇒ 剩 4M 约 **2.7 h** |

⇒ **写入面隔离在 A、B 两臂都拿到实测**：合同要隔离的那个自变量（A 只写读出头 / B 只写运动面）
不是声明而是实测。C 跑完三臂才齐。

### 🔴 我上一轮对所有者说错的话（必须记在明处）

我说"重跑同一条命令即可续跑，幂等"。**那句话当时不成立。**
原实现对"已跑满的臂"`raise SystemExit` ⇒ 多臂 campaign 会**在第一个跑完的臂上中止**。
于是 A/B 早已完成，C 在进程死掉后**没有任何 driver 能接上它**，`campaign_report.json` 也从未生成。

- **修**：引入 `ArmAlreadyComplete`；campaign 层**跳过并记录** `arms_status`；`--fresh` 是唯一重做通路。
- **回归** `test_a_finished_arm_is_skipped_so_a_campaign_can_be_re_run`：同一条命令复跑 ⇒
  `arms_status == {B: already_complete, C: already_complete}`、`failed_arms == []`，
  且 C 的 `session_id` **逐字不变**（证明没被偷偷重跑）。
- **实测续跑生效**：重启后输出
  `[A] already_complete … skipping` / `[B] already_complete … skipping` / `[C] start`，C 随即继续。

### 🔴 第二个缺陷：`--fresh` 在 Windows 上必崩

归档旧报告的文件名用了 `_utc_now()`，含 `:` ⇒ `OSError [WinError 123] 文件名、目录名或卷标语法不正确`。
**是上面那条新写的测试当场抓到的**——此前 `--fresh` 路径**从未被执行过**，所以这个崩溃一直躺着。
修：新增 `_stamp_for_filename()`（`:` → `-`），JSON 字段仍用 `_utc_now()`。

### 可复用教训（已入 MEMORY §4）

1. 「幂等重跑」这类承诺**必须用测试证明，而且要证多臂情形**——单臂测试全绿挡不住 campaign 层中止。
2. **时刻进文件名要换安全字符集**：`:` 在 Windows 非法。
3. 长跑的 driver 要**分臂记状态**（`arms_status`），否则"哪一臂停了"只能靠翻进度流猜。
4. **报速率报中位窗口，不报累计**（累计把一次性扰动永久计入，看起来像模型性质）。
5. 空窗口的指标写 `null`，**不写 `0.0`**（0.0 会被读成塌陷）。

回归：runner 合同测试 10 → **11 passed**；ruff 零告警。提交 `1be6371f`；
合同 §4.5 与 03 队首第十六批已记录全部。

## 🔍 判决已出（13:01）：三条门都没过，但 M1 只差一个不一致对

判决件 `reports/taiji_r2_readout_retrain_verdict_20260920.json`；后端 5m57s。
**反向对照门先过**（真中文 1.0 / 打乱 0.9567 / 门 −7.8 / n=300）⇒ 判据有判别力。

| 门 | 判定 | 读数 |
|---|---|---|
| **M1** | **不成立** | 成句率 A **0.6047**、B **0.3953**、C **0.2326**；A−最好者(B)=**+20.94 pp**（裕度门 **过**）；配对符号检验 A vs B `b=14 c=5 n=19` **p=0.0636**（显著性门 **不过**）|
| **M2** | **不成立** | 三臂 CAP D+E 机器计分 **全 0 / 36** |
| **K2** | **规则自身有缺陷** | 字面 ⇒ "无差别"；本意 ⇒ "可分辨"。**两者相反** |

**M1 只差一个不一致对**：n=43 只有 19 个不一致对，精确双侧检验要 **≥15/4** 才 p<0.05，实测 **14/5**
（p=0.0636）。⇒ 这是"只差一点点的未过"，不是"没效果"；但**合取判据就是不过**，不许改门。

**交叉校验（值得记）**：C 臂 0.2326 与 FROZEN 的 P1/P2 在同一基座上的读数**逐位一致**
⇒ 冻结臂确实没动任何东西，基线没漂。

### 🔴 又一处我自己的错，而且是同一个类：K2 预注册写成有符号比较

§5.1 冻结的是 `（B − A）的成句率差 < +15 pp`。实测 B−A = **−20.94 pp** ⇒ 字面成立 ⇒ 判"无差别"。
**但同一段自己写明的本意是「B 落后 A 不到 15 pp」**，即 **|B−A| < 15 pp** ⇒ 判"可分辨"。**两者结论相反。**

- **根因**：把"无差别"写成**有符号**比较。与 `M5_R2_SURFACE_DECODING` §6.1 第 2 条
  （D2 写成 `delta < 5`，把 −23 pp 误判成满足）**是同一个错误**——那处已订正为 `abs(delta) < 5`，
  本件却在预注册里重犯。
- **处置**：**本轮按字面报（读出侧不记功），§5.1 一字不改**；修正版只能作为下一轮新预注册。
  理由：两种读法下 M1 都不成立、头条不变，不值得冒"看到结果才挑读法"的风险；
  "看到结果再修判据"比"损失一次归因"贵得多。
- **新纪律入 MEMORY**：写"无差别/接近"类判据时，**先问这是双侧还是单侧**。

### 📌 数字说明什么（连同禁止主张一起引用）

方向与量级明确：只训读出头把 n 元成句率从基座 0.2326 拉到 **0.6047（+37.2 pp）**，运动面到 0.3953（+16.3 pp）。
M2 三臂齐平为 0 ⇒ 这条门是**地板效应、无判别力**，不是"A 不如 B"；也实证了 §3 那条禁令——
**成句率改善不等于会回答**。不得据此解除 R2 的 M5 排除，不得改 CAP 冻结线。

## ✅ 三臂全部跑满（12:55）—— 训练侧结束

C 在 12:55 报 `completed`（本会话 `wall=5603.8 s`）。**三臂各 16,000,000 符号全部跑满**：

| 臂 | 写入面（实测） | wall | s/符号 | checkpoint sha256 |
|---|---|---|---|---|
| A | `['predictive_readout']` | 11.72 h | 0.0026378 | `732e59cf…` |
| B | `['motor']` | 18.64 h | 0.0041938 | `766dfa04…` |
| C | **`[]`** | 10.87 h（跨 3 会话：8.143+1.165+1.557）| 0.0022415（末会话）| `97cb2c08…` |

**写入面隔离三向拿齐** —— §3 设计要隔离的那个自变量（A 只写读出头 / B 只写运动面 / C 两处都不写）
从"声明"变成"实测"。基底 sha256 跑前跑后未变。**三臂合计 41.23 h**（估 37.3→42 h）。
`campaign_report.json`：`{A: already_complete, B: already_complete, C: completed}` / `failed_arms: []` / `completed`。

**又一处我自己的缺陷（同批修）**：`arms_pending` 把 `already_complete` 当 pending ⇒ C 跑完时
`resume_command` 吐出 `--arms A,B …`，一条**什么都不做的空转命令**。
改为"没跑满预算的才算 pending"，并把"二次复跑后 `arms_pending == []`、`resume_command is None`"
写进回归（runner 合同测试 **12 passed**）。

**判决器已启动**（后台，M1 成句率 + M2 CAP D+E + K2 反事实）；落地后另起一节并回填 03。

## 重启后已续跑（11:22）

机器 11:17 左右重启（11:20 测得开机 3.6 分钟）。重启**确实丢掉了边界之后那段**：
C 的 checkpoint 仍是重启前那份 **13,500,000**（`saved_at_utc=03:01:31Z`），
`progress.jsonl` 最后写入也在重启前 ⇒ 丢约 127,654 符号（≈5.1 min），与预判一致。

**一条未定性的观察（不许当结论用）**：11:20 看到过一个 **291,360 K 的 `python.exe`**，11:25 已消失。
**读不到它的命令行**（`wmic` 已不在系统里；PowerShell 工具查询返回空，`Out-File` 也是空文件）。
⇒ 不把它当"有人在续跑"，也不据此推断任何事；当时 C 产物零写入，故按"没有 driver"处理。

**11:22 续跑**（`--arms A,B,C --symbols 16000000 --checkpoint-every 100000 --progress-every 100000`）：
输出 `[A]/[B] already_complete … skipping` + `[C] start` ⇒ **多臂幂等续跑在真机验证通过**。
新会话 `C-2026-09-22T03:22:16Z` 已写出第一条进度（13,600,000），实测 **0.00223 s/符号**
（比重启前的 0.0023877 还略快）⇒ 剩 2.4M ≈ **1.5 h**，预计 **13:10 前后**跑完。
顺带删掉探针留下的 `output/_proc_probe.txt`。

### 更正我自己的一个数字：丢的不是 5 分钟，是约 14 分钟

11:06 我算"边界后未落盘 127,654 符号 ≈ 5.1 min"——那是**那一刻的快照**，而重启发生在 **11:16**，
中间又跑了 10 分钟。按旧会话最后一条进度（13,750,000 @ 11:11:47）外推，进程实际停在 ≈13,850,000
⇒ **真实损失 ≈ 350,000 符号 ≈ 14 min**。
⇒ 教训：**"已经丢了多少"必须在停机那一刻量，不能拿更早的快照当结论**（与"报速率报中位窗口"同族）。

### 一条未定性的新观察：重启后启动开销约 7 分钟

新进程 03:22:16 启动，但 `session_started`（进度里的 `elapsed_seconds` 起点）之前**花了约 7 分钟**。
该段含 torch/seed/taiji 导入、`torch.load`（基座 4 MB + 臂 88 MB）、`Seed.from_checkpoint`。
**同时排除了语料侧**：热缓存下跳 24.7M 符号只要 **1.083 s**，再取 100k 符号 0.005 s。
**候选解释**：本机已知的**间歇性 I/O 卡顿**（另一线独立记录过同机文件操作停顿 0.2 s～22 min）。
**证据不足，只登记现象与两个被排除项，不下结论。**

## 优雅停机新增（应所有者"想重启电脑、能否存最新进度"）

**取证 11:06**：C 臂**已保存点 = 13,500,000 / 16,000,000**（`saved_at_utc=2026-09-22T03:01:31Z`，
5.1 分钟前），本次会话速率 0.0023877 s/符号 ⇒ 剩 2.5M 约 **1.66 h**；
**边界之后约 127,654 符号只在内存里**（约 5 分钟计算），下一落盘边界 14,000,000 还要 14.8 分钟。

**诚实的回答是"当时不能"**：落盘只发生在 `consumed % checkpoint_every` 边界上（C 用 500,000 ≈ 20 分钟），
所以"最新进度"只能等于"最后一次边界"。

**新增能力**：`--stop-file`（缺省 `<out-dir>/STOP_RUN`）+ `--stop-check-every`（缺省 10,000）。
哨兵一出现 ⇒ 落盘并退出；报告标 `stopped_by_request`（**不是失败**），并给出 `arms_pending` 与
`resume_command`；哨兵用一次即失效。回归 `test_a_stop_file_saves_and_exits_cleanly`
（含"停机后接着跑，`session_symbols` 只算新增"）。**停机损失：一个 checkpoint 间隔 → ≤10,000 符号（≈24 s）**。

**同批修掉**：语料比预算短时甩**裸 `StopIteration` 栈** ⇒ 改为带"第几臂/跑到第几个/跳过量"的
`SystemExit`。**是 stop-file 的测试撞出来的**。

**新增能力救不了当前这个已跑的进程**（它加载的是旧代码，不会看哨兵）⇒ 这次重启的损失是固定的
（约 5.1 分钟）。与"再等 15 分钟到 14M 边界"相比：省 5 分钟 CPU，花 15 分钟人的时间。
⇒ 建议**现在就重启**，续训把 `--checkpoint-every` 降到 **100,000**（此后任何中断最多丢 4 分钟）。

runner 合同测试 11 → **12 passed**；ruff 零告警。

### MEMORY.md 单方合并瘦身（应所有者要求，同日完成）

**5,543 → 2,996 字符**（注入上限 3,000）。做法不是删，是**移出**：

- 新建 `docs/DESKTOP_AUTOMATION_PITFALLS.md`，把前端工具链 / Electron 壳 / 进程守卫 / electron-builder /
  NSIS 的**逐条实测**原文搬过去（含 `extraFiles` 必须排除 `Seed.exe`、`clean-release.mjs` 改为只重命名
  等细节）；MEMORY §4 只留一行指针 + 三条最容易烧到人的（Vite 不重写 `.js`→`.ts`、
  `ELECTRON_RUN_AS_NODE=1` 的假"二进制没装"、NSIS `/D=` 必须加引号）。
- 逐节压缩措辞，**规则一条没删**：§1 架构契约 3 条、§2 环境硬约束 5 条、§3 文档四类与 5 条纪律、
  §4 判据 8 条、§5 状态与回退点，全部在。
- 唯一**移出 MEMORY.md 且不再逐行保留**的是「序列级 ≠ 位置级」那条判据——它仍在已归档的
  `plans/reference/M5_R2_READOUT_DIAGNOSIS_20260917.md`，且 §5 的 `plans/reference/` 指针指向它。
- 复跑守卫：目录结构守卫 + 泄露守卫 **11 passed**（新 doc 不触发任何规则）。

**给后来者的提醒**：这个文件是**两条并行线共写**的，各自追加很快会再次越过 3,000。
瘦身必须**单方主导一次做完**（两人同时压会互相覆盖）；移出的内容要落到 `docs/` 下的专文并在
MEMORY 留指针，**不要直接删**。
