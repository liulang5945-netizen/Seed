# 2026-09-23 工作日志

## 所有者裁决「甲选1 + 乙选1」，两件都落地

**甲（K2 预注册缺陷）选1**：上一轮**按字面报**（读出侧不记功）、`§5.1` **一字不改**，
修正版只作新一轮预注册、**不回溯**。理由：两种读法下 M1 都不成立、头条不变，
不值得冒"看到结果才挑读法"的风险。

**乙（M1 补功效）选1**：批 **v3 未见题面集**，**不涉训练预算**（三臂 checkpoint 已在盘，纯评价）。

## v3 未见题面集：先建先封（打分前落 digest）

- 件 `plans/manifests/r2_readout_m1_v3_prompts.json`，**sha256 `f1ac06a1b8652c4dd8fd7a659117ae960d5604c9f65779b1955ff32d93305489`**，160 条。
- **机械抽取，没有人工挑选余地**（关键设计）：源 = `data/p3b_all_fresh.jsonl`；可用区间 = 行内累计偏移
  `Σ(len(text.encode())+1) ≥ 16,000,000`（**三臂训练窗口 `[11,199,800, 27,199,800)` 之外**）⇒ 第 4026 行起共
  25,851 行；`stride = 25851 // 160 = 161` 等距取 160 条；题面 = 该行的**前 32 个字符**。
  构建器 `scripts/training/build_r2_readout_m1_v3_prompts.py`（已存在即拒跑）。
- **为什么不让"我来写新题"**：本轮已经看过 CAP-43 的结果，人工撰题就有无意中挑到对 A 有利的题的空间
  —— 正是"看到结果再挑样本"。机械抽取把挑选权直接交出去。
- **如实登记的代价**：题面总体从"CAP B/G 的 43 个问句"变成"未见中文散文的前 32 字"，**不只样本量变大**。
  因此新增**回声控制** `prompt_echo_8gram_rate`（输出中任一 8 字子串出现在题面里的比例），
  不参与判定，只让"照抄上下文"这条解释可见。

## 预注册（`plans/reference/M5_R2_READOUT_RETRAIN_V3_POWER_PREREG_20260923.md`）

门与裕度**一字不改**（成句判据 import 自 FROZEN 的 P1/P2；M1 = A ≥ max(B,C) + 15 pp 且配对精确双侧
符号检验 p<0.05）；**K2 本轮用双侧口径**（`|B−A| < 15 pp 且 p ≥ 0.05`），两种口径在报告里并列；
**只看一次**；两轮数字必须一并引用；M2 不重跑（上一轮三臂齐平 0/36 的地板效应结论保持）。

## 判决器扩展（加性，不破坏上一轮可复现）

`--prompts-file` 改用封存题面集；新增回声控制；`judge_k2` 同时给出**字面**与**双侧**两种读法
（`literal_reading` / `bilateral_reading`），两者相反时 `consequence` 明说要分别引用。
回归：判决器合同测试 10 → **13 passed**（新增"封存题面集替换 CAP 题面"、"回声控制能识别照抄"、
"K2 两种口径可以相反"三项）。

提交 `53fc3a7f`。v3 轮已启动，产物 `reports/taiji_r2_readout_retrain_verdict_v3_20260923.json`。
