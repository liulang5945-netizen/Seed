# 2026-09-21

## 前端语言 vs 开源 Harness 采用 —— 决策简报落档

- 触发：所有者提问「是否把前端改用 TypeScript，或改用 DeepSeek Harness / ZCode 做 taiji 化改造」。
- 取证（10:30 GMT+8）：
  - ZCode（智谱 ADE）：`zai-org/ZCode` 于 2026-09-20 20:01 开源，Apache-2.0，TS 97.6%，**仅 2 条提交、无 release**，正处工作区快照静默上传 OSS 的安全事故整改期 ⇒ 不可作为 fork 基座。
  - dsh（DeepSeek Harness）：MIT，TS + Cordis 插件内核，v0.1 预览并明言破坏性变更；四档 preset（Standard / PTC Code Mode / **Minimal 裸 harness** / Create）。
  - 反例：Aider / OpenHands / AutoGen 为 Python ⇒ **实现语言与 agent 效果无因果关系**。
- 前端实测：`frontend/src` 124 文件 / 21,117 行；`.vue` 45 且**全部 `<script>` 为 js**；`.js` 69；**无 tsconfig**，仅有 `jsconfig.json`（仅 `@/*` 别名）。
- **关键发现**：`src/composables/nativeApi.js` 已是类型化边界门面的雏形（冻结 `nativeApiPaths` 路径表，注释明言 payload 保持 plain object 且仅在此边界序列化）⇒ 契约单点化已做完，**前端是「TS 形状的 JS」，缺的只是类型层**，迁移是补类型层而非重写。
- 结论：**采纳**「前端增量 TS 化」；**否决**「采用 dsh/ZCode 做内核替换」（层次错位 + 架构倒置 + v0.1 破坏性变更 + ZCode 不可 fork + 与 R2/M5 主线冲突）。
- 落档：`plans/reference/FRONTEND_TS_VS_HARNESS_ADOPTION_DECISION_BRIEF_20260921.md`（状态：待所有者裁定，未裁定前不产生代码改动）。
- 待批准后唯一下一步：前端 TS 地基（`typescript` + `vue-tsc` + `openapi-typescript`；`tsconfig.json` 开 `allowJs`、`checkJs:false`；由 api OpenAPI 快照生成 `src/api/schema.d.ts`；`nativeApi.js` → `nativeApi.ts`；CI 加 `vue-tsc --noEmit` 仅限新增/改动文件）。零 `.vue` 改动、零回归、可逆。
- 可借用思想（借架构不借代码）：append-only session log + trajectory；Code Mode / PTC 收敛 workbench 端点；**dsh 的 Minimal 裸 harness 形态 = R2 所需裸环境对照臂**。

## 前端 TS 地基 —— 落地执行（同日）

- 所有者批准选项甲，并同时提出 **Electron 迁移**意向。
- 落盘：`frontend/tsconfig.json`、`src/api/schema.d.ts`（生成物 7,144 行 / 202,933 字节，源 `tests/snapshots/openapi_baseline.json` = 116 paths / 123 ops / 50 schemas）、`src/api/paths.ts`、`src/api/contract.ts`、`scripts/check-api-types.mjs`、`.prettierignore`；改 `nativeApi.js`（表上移）、两个守卫脚本、`.eslintrc.cjs`、`package.json`、`ci.yml`。**未改任何 `.vue`**。
- **实测否决的原设计**：`nativeApi.js` → `nativeApi.ts` 的 rename 不成立——**Vite / Vitest 不会把 `./x.js` 说明符重写到 `x.ts`**，24 个测试文件 / 29 个用例转红；要 rename 就得改 20 处 import（13 处在 `.vue`）。改走「路径表上移到 `src/api/paths.ts`」，文件面零变化。
- 工具版本精确钉：`typescript@5.9.3` · `vue-tsc@3.3.11` · `openapi-typescript@7.13.0` · `@typescript-eslint/parser@8.70.0`。
- 门禁读数：typecheck rc=0；check:api-types 逐字节一致；check:api-contract PASS（55 literals）；check:native-boundary PASS；vitest 47 files / 267 tests；eslint 0 errors / 13 warnings（全为存量）；vite build 成功（4830 modules）。
- 红/绿取证：三个门各用一次探针值证明会响（`vue-tsc` 报 `TS2344` 并点名越界字面量）。
- 待裁定：**Electron 迁移**（评估见简报 §8；建议采纳，且顺序必须是 TS 地基 → Electron 壳，否则壳要先写一遍 JS 再转 TS）。

## Electron 壳迁移 —— 执行（同日，所有者批准后）

- 新增 `desktop-electron/`（8 个文件：config/logger/settings/backend/websocket/preload/main + package.json/tsconfig/electron-builder/.gitignore）。**未改动 `desktop/` 任何文件**，PyQt6 仍是出货路径。
- **冒烟测试端到端通过**（本机实测）：Backend 8000 ready → WS 8765 ready → frontend loaded → **`Window bridge self-check passed`**。最后一项是唯一标注「离线无法证明」的接口（contextBridge 承载可 new 的函数 + 回调内传函数），实测成立 ⇒ `AppTitlebar.vue` 零改动。
- 与 PyQt6 对照：frozen Qt DLL 疤痕、`_EdgeResizeFilter`(~50行)、`_apply_window_shape`、`_RestartWorker(QThread)` 四处**消失**；新增拖拽（注入 `-webkit-app-region: drag`）、toggle 300ms 去抖、桥接自检。
- **踩坑（判据级）**：`ELECTRON_RUN_AS_NODE=1` 在本机环境里被设置 ⇒ `electron.exe --version` 打印 `v24.21.0`（Node 版本）而非 `v44.4.3`，`require('electron')` 退化为返回路径字符串，报 `Cannot read properties of undefined (reading 'isPackaged')`。**症状与二进制缺失极易混淆**。
- electron 二进制不随 `npm install` 下载（287 包 29 秒装完但无 `dist/`）；补下：`ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/ node node_modules/electron/install.js`。
- 受限会话下 Electron 同样需要 `--no-sandbox --disable-gpu --disable-gpu-compositing --in-process-gpu`，否则 `FATAL: GPU process isn't usable` 直接 abort ⇒ **已订正简报 §8.2 里「Qt 那 70 行整段消失」讲过头的话**。
- `SEED_PYTHON` 开关必需：本机 PATH 上 `python` 是 managed 3.13（无 uvicorn），Python312 才有。
- 未闭合：`SeedWs.exe` 第三入口缺失（frozen WS 分支明确报错不静默降级）、frozen 路径未验证、UI 交互未验证。
- `logs/desktop_main.log` 两个壳共用 —— 双轨可直接并排对照。

## SeedWs 第三入口 + frozen 路径验证（同日续）

- **闭合 §9.5.1**：新增 `desktop/seed_ws.py`（8765 独立进程入口）；`desktop/seed.spec` 改**三入口**（a_ws / MERGE 三项 / pyz_ws / exe_ws / COLLECT 补齐）；`scripts/release.py` 的 `_verify_artifacts` 补 `SeedWs`。PyQt6 出货路径行为零变化。
- **PyInstaller 实测通过**：三 PYZ / 三 PKG / 三 EXE 全成功，`dist/Seed/` = 三 exe + `_internal`（9,293 文件 / 1.45 GB）。
- **frozen 分支端到端通过**：`Backend worker started`（= frozen 措辞，确认分支正确）→ ready → `SeedWs.exe` 起 8765 → frontend loaded → 桥接自检通过，**全链路零告警**。
- 新增验证接缝 `SEED_FORCE_FROZEN` / `SEED_ROOT_DIR`（默认关闭），让 frozen 分支能在源码树上对真实产物验证，不必每次 electron-builder 打包。
- **抓到两个真 bug**（都是 frozen/repeat 运行才暴露）：
  1. `reapOrphanedBackend` 漏掉 `main.py` 原有的 `owner == os.getpid()` 自我排除 ⇒ 看门狗把自己上一轮的 child 当"别的实例"，跳过回收后同端口再起一个，必然互撞。
  2. `findBrandIcon()` 的 frozen 候选用了 Electron 的 `process.resourcesPath`，而本项目是 PyInstaller onedir（datas 在 `_internal/`）⇒ **托盘被直接禁用**。
- **我自己的流程错误**：首次 frozen 探针绕过了 `npm run build`，`dist/` 是旧的，探针实际跑 dev 分支。`npm start` 的 `build && electron .` 顺序不能绕过。
- 仍未验证：UI 实际交互（按钮/拖拽/托盘菜单/关闭到托盘）。仍未闭合：孤儿回收的跨壳缺口。
- **补做的回归检查**：改的是出货路径的 `seed.spec`，故补跑 `dist/Seed/Seed.exe` —— PyQt6 frozen 客户端正常：
  `Child job object armed` → 前端 loaded (ok=True) → **前端 useWebSocket 已连上 8765**（`新客户端连接: ('127.0.0.1', 49542)`）。
  结论：三入口改造未破坏出货路径。硬杀后端口全释放、无残留进程（kill-on-close 成立）。
  QtWebEngine 的 GLES3→GLES2 回退与 `V8 Proxy resolver in single process mode` 是受限桌面的既有噪声，不影响启动。

## Electron 打包（electron-builder）—— 首次验证（同日续）

- **安装包产出**：`cd desktop-electron && CSC_IDENTITY_AUTO_DISCOVERY=false npm run dist` → 34 秒 rc=0；
  `release/SeedSetup-1.6.0-electron.exe` 111.76 MB（sha256 `c52c054d…bccc`）+ `win-unpacked/` 76 文件 / 385.7 MB。签名跳过（无证书）。**迁移最后一道未验证门槛闭合。**
- **关键缺口**：包内**只有壳**（Electron + Seed.exe + app.asar），`SeedBackend.exe` / `SeedWs.exe` / `_internal` / `frontend` 全都不在 ⇒ 安装包目前不可用。需把 PyInstaller 产物作 `extraResources` 打进包并接进 `release.py`。
- **三个卡点（都已绕过）**：①工具链下载挂死 → 从 `app-builder-lib/out/toolsets/windows.js` 读确切版本+官方 sha256，手工抓取校验后放入 `%LOCALAPPDATA%/electron-builder/Cache/`；②`unpacking default Electron distribution` 死住 → 配 `electronDist: node_modules/electron/dist`；③`release/` 已存在时 `copying unpacked Electron` 死住（32/75 文件后零增长）→ 每次先清空 `release/`（固化为 `scripts/clean-release.mjs`）。
- **未定性（重要）**：这些卡点是 electron-builder 缺陷还是本机 VM 大文件 I/O 不稳，**判据不足，不下结论**。成功都发生在 `release/` 不存在时；但 `fs.rmSync` 删已存在的大目录也挂过一次，而 python `shutil.rmtree` 删同一目录仅 0.7 秒。只记录可复现的最短操作序列。
- 修掉 4 处降级路径缺陷并在真实产物上复验：过期的 `SeedWs.exe not found` 措辞；看门狗每 10 秒无限重试（改为永久缺失即停）；Brand icon 警告重复 3 条（加缓存）；后端缺件报错信息补足语义。
- 仍未闭合：Python 载荷未打进包、`release.py` 未接 Electron 分支、安装器未做安装测试（会改用户机器，未擅自执行）、UI 交互仍未验证。

## Python 载荷装入 Electron 包（同日续）

- `electron-builder.yml` 加 `extraFiles`（从 `../dist/Seed/` 取到应用根），filter 用黑名单 `**/*` + **`!Seed.exe`** —— 后者必须排除，否则 PyInstaller 里的 PyQt6 GUI 会**覆盖掉 Electron 自己的 Seed.exe**。
- `scripts/release.py` 加 `--electron` 分支（`build_electron()` / `_verify_electron_artifacts()` / `_run(env=)`），默认 PyQt6 路径零改动。
- **门先红后绿**：装载前 3 errors（精确点名 SeedBackend.exe / SeedWs.exe / _internal），装载后 0 errors。
- 产物：安装包 111.76 MB → **540.52 MB**（sha256 `b2c116e6489e6b0f716871ff5a1968a4eb1680ee6d0ee54572190868be89e791`）；win-unpacked 76 → **9375 文件 / 1764.7 MB**。
- **打包版 Electron 首次端到端跑通**：包内 `SeedBackend.exe` → 8000 ready → 包内 `SeedWs.exe` → 8765 ready → 前端来自 `_internal/frontend/dist` → 桥接自检通过，**零告警**（`Brand icon not found` 也消失了）。
- **诊断订正（重要）**：我之前把「删除 `release/` 挂住 4 分钟」归因为 VM 大文件 I/O 不稳。实际跑 `release.py` 时抓到 `[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":9333,"threshold":50,...}` ⇒ **批删守卫在拦截**（你们记忆里早记过这条：跑批量删除必须 `CODEBUDDY_SAFE_DELETE_ENABLED=0`）。所以至少一部分"死住"是守卫等确认，不是 I/O。已把两个候选原因都写进简报，不单选。
- `clean-release.mjs` 改为「重命名」而非递归删除（rename 是元数据操作，与体积无关），`.gitignore` 由 `release` 放宽为 `release*`。
- 顺带发现冗余：载荷里带着整个 `_internal/PyQt6/Qt6/`（含 QtWebEngineProcess.exe）——因为 `seed.spec` 的 `a_main` 需要 PyQt6，而 Electron 轨道根本不需要 Seed.exe。**约 200 MB 纯冗余**，修法是为 Electron 轨道出一个不含 `a_main` 的 spec 变体。
- **完整链路跑通**：`CODEBUDDY_SAFE_DELETE_ENABLED=0 python scripts/release.py --electron --skip-frontend` → **29m33s，rc=0**，[1/5]~[5/5] 全过、产物验证无错误。安装包 540.52 MB · win-unpacked 9370 文件。
- **诊断订正**：`release.py` 首次不带环境变量跑，当场抓到 `[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":9333,...}` ⇒ 之前的"删除死住"至少有一部分是**批删守卫在拦截/等待**，不是磁盘 I/O。但 `copying unpacked Electron` 那次是拷贝、与守卫无关，故两因并存，不单选。已同步订正简报 §11.4 与 MEMORY.md §2。
- 仍未闭合：安装器安装流程未验证；UI 实际交互未验证；Qt6 冗余 ~200 MB 未处理；带前端构建的全链未跑。

## 安装测试（同日续）—— 首次执行

- **安装通过**：`SeedSetup-1.6.0-electron.exe /S /D=E:\_seed_install_probe` → 59 秒 rc=0，**未要求提权**（按用户安装）；9371 文件 / 1764.9 MB 与 win-unpacked 一致；桌面+开始菜单快捷方式已建。
- **运行通过（零告警）**：包内 SeedBackend.exe → 8000 ready → 包内 SeedWs.exe → 8765 ready → 前端 loaded → 桥接自检通过。
- **卸载未通过（产品缺陷）**：`Uninstall Seed.exe /S` → **rc=0 但什么都没删**（9375 文件与两个快捷方式原样保留）；加 `_?=` → **rc=2** 仍无删除；注册表 `HKCU/HKLM\...\Uninstall` 下**找不到**登记项 ⇒ 控制面板里也不会出现。⚠ 本次只测了静默卸载，交互式卸载未测、不能断言同样失败。
- **本机批量 I/O 第三次复现**：删安装树 9375→4271 后再次 FROZEN 数分钟，另一次同目录删除仅 2.3 秒。已知一类成因是批删守卫，其余未定性。⇒ 凡涉及本机大目录批量删除/拷贝的步骤都可能需人工介入。
- **探针残留（如实）**：`E:\_seed_install_probe\` 残留 4271 文件 + 桌面/开始菜单 `Seed.lnk`，需手工删除（简报不再尝试）。
- **技能检索结论（§14）**：不必安装——SkillHub 的 frontend-design 系 SKILL.md 与本会话已可用的 `impeccable` 同源（Anthropic frontend-design），装了是重复；SkillHub 上名为 `impeccable` 的是简诗 AI 海报系统，**同名不同物勿混淆**。唯一不重叠的增量是 `web-design`（0.110，CSS 实现模式）。三个候选包下载解包审计：危险信号零命中。

## 卸载"缺陷"反转 + 圆角图标（同日续）

- **卸载缺陷结论反转：没有产品缺陷，是我第一次测错了。** 根因：安装命令把 `/D=E:\_seed_install_probe` **没加引号**，bash 把 `\_` 当转义吃掉 `\` ⇒ 安装器把**畸形路径** `E:_seed_install_probe` 写进 `InstallLocation` 与 `UninstallString` ⇒ 卸载器照错误路径 `RMDir /r` ⇒ rc=0、什么都不删。"注册表里没有登记项"也是探针 bug——正确探针在 `HKCU\...\Uninstall\571056b5-…` 找到了 `Seed 1.6.0`（当时 InstallLocation 恰是那个畸形值）。
- **复测证明卸载链路完整**：正确引号安装（UninstallString 反斜杠在）→ `Uninstall Seed.exe /S` rc=0 → **目录/桌面快捷方式/开始菜单快捷方式/注册表键全部移除、零残留进程**。上一版简报 §13.3 的"产品缺陷"结论已作废并订正。
- **教训（已进 MEMORY）**：NSIS `/D=` 自定义路径在 bash 里**必须加引号**。这类错误最阴险：安装与运行都正常（文件落在别处），只有卸载才暴露。
- **桌面快捷方式图标已改四角圆润**（应所有者要求）：`icon.ico` 256 帧做 4x 超采样圆角 alpha 遮罩（半径 48px ≈ 19%，近 Win11 比例），重建 256/128/64/48/32/16 六帧，原件备份 `icon.ico.bak`；`npm run dist` 重建后用 ctypes `PrivateExtractIconsW` 从**安装后的** `Seed.exe` 抽 256px 图标，**与圆角版逐像素零差异（0/65536）**、四角 alpha 全 0。装→运行→卸全链复验通过。
- 注意：托盘/窗口图标走的是另一份资产 `frontend/public/seed-taiji-network.png`，**本次未动**；要圆角需单独处理。

## 所有者实测反馈的四项（同日续）

- **占位屏 bug（已修）**：所有者双击快捷方式启动打包版，停在「Seed / 界面加载中」占位屏 + 右侧竖向长条。①占位屏 = 注入的 3 秒兜底（#app 为空）⇒ **Vue 未挂载**，根因是 GPU 默认开启 ⇒ **§8.2「Electron 默认保留 GPU」判断反转**，`DISABLE_GPU` 现默认为真（`SEED_DISABLE_GPU=0` 可开）；②长条 = body 默认 margin + 内容超高在透明窗口上撑出滚动条 ⇒ 壳侧注入 `NO_SCROLLBAR_CSS` 压掉，零 .vue 改动。
- **托盘退不出：未复现**，需要所有者补操作序列（点「退出」没反应 = quit() 路径；左键没菜单/没恢复窗口 = tray.on('click') 路径），定位前先分辨。
- **Logo 候选已出**（§16）：北斗七星=神经元节点、连线=突触；斗四星在黑鱼、柄三星扫向白鱼、红点落天枢。PIL 矢量绘制（非 AI 生图，星形必须精确）。产物 `design/logo-candidates/{v1-logo-transparent,v2-icon-squircle,all-candidates}.png`。**待所有者选定后再生成 icon.ico 与托盘/窗口图标**（当前圆角 icon.ico 仍是旧水墨方案）。
- **`web-design` 技能已装**（应所有者要求）：审计零命中后落到 `~/.workbuddy/skills/web-design/`（SKILL.md 269 行，CSS 实现模式）。
- **Electron 专用打包规格**：`desktop/seed-electron.spec`（两入口，无 Seed.exe/PyQt6，省约 200 MB Qt6）；`release.py --electron` 改用它；`_verify_artifacts` 在 electron 模式不再要求 `dist/Seed/Seed.exe`。**实测落地**：dist 1385.6→826.4 MB、安装包 540.52→**403.39 MB**、`_internal/PyQt6` 零残留。
- **GPU 降级第二层修正（关键）**：只给 `--disable-gpu` 不够——打包版默认启动直接 `FATAL: GPU process isn't usable. Goodbye.`（Chromium 仍会起软件合成 GPU 进程，受限 VM 上被自身沙箱挡住）。四件套 `no-sandbox + disable-gpu + disable-gpu-compositing + in-process-gpu` 固化为默认。**CDP 终验**：无开关启动安装版，`#app` 有子节点、body 即真实界面 ⇒ **占位屏问题修复**。
- **验证方法论**：哈希不能证明"包含某改动"（本轮两次不同构建 sha256 前缀曾相同）；应 grep 产物里的特征字符串（如 `app.asar` 里的 `in-process-gpu` 计数）。
- **clean-release.mjs 再修正**：关键路径上 `fs.rmSync` 陈旧 stale 目录会随机挂住（0.2s～22min），改为**只重命名当前 release/、绝不删除**，陈旧目录只列出。
- 托盘退不出：仍未复现，待所有者补操作序列。**（更正：并行会话已复现并修复——main.ts 加 `quitting` 真退出标志（close→preventDefault→hide 曾拦死 app.quit()），SEED_TRAY_SMOKE=1 红绿验证，已提交 `cce2b0f3`。所有者当时的「托盘退不出」由此闭合。）**
- **项目清理（应所有者要求）**：删 15,754 文件 + 135 空目录，释放约 3.5 GB——`build/`(472MB)、`dist/`(866MB)、`win-unpacked/`(1.65GB)、`release.stale.*`、`.seed_test_tmp/`、`_pytest_m2aa_full/`。保留：`frontend/dist`（在服务）、`desktop-electron/dist`、`release/SeedSetup-*.exe`（最终安装包）、`.m0-checkpoint-a4x_ye4_`（未 ignore 的空目录，名字像实验标记，保守保留）。**跳过**：`direct-w3-cross-file-r5b-20260830`（含 `w3-loop.pt` 检查点）与 `direct-workbench-r5b-20260830{,-b}`（非 ignored 实验工作区，共 136 文件）——需所有者决定。教训：**批删守卫必须关**；本机删 1.6 GB 树耗时 15 分钟（build/dist 却很快），I/O 间歇性仍未定性。
- 发现并行会话在跑：其未提交工作（main.ts 托盘修复 + 03 账本纳账）出现在工作区，**未触碰**；随后其自行提交为 `cce2b0f3`。`desktop/settings.json` 持续显示 M 属运行时窗口几何写入，正常。
- **目录结构规范已立（应所有者要求）**：`docs/FOLDER_STRUCTURE_RULES.md`（S1-S8，与 R 系互补）+ `scripts/clean_worktree.py`（默认 dry-run）+ `.gitignore` 补 4 条缺口（`/.tmp-*/`、`/.m0-checkpoint-*/`、`.mypy_cache/`、`.ruff_cache/`，R4 双向校验通过）。提交 `a7b9d260`。**dry-run 拦下一次误删**：`data/` 是 dev 运行数据根（63.5 GB，含训练数据），初版清理清单错标为可删——已归入 OWNER_DECISION（永不自动删除）并订正规范 S2。规范要点：临时目录一律 `.tmp-<主题>/`；空目录直接删；运行时可写数据走 `get_external_path()`；打包产物唯一入口 release.py；`desktop/settings.json` 会因窗口拖动持续 M（正常，运行时几何）。

- **Logo 第二轮（§17）**：所有者否掉北斗单星座方案，要求**先忠实复现 social preview 细节**再用星象。逐像素解剖发现：原图是**左右双栏**（左文字排版/右标志），此前把右半当全部是走偏根源；底色 #FAFBF6、盘 R 只占画布高 0.249、**双眼 ≈0.144R 在经典位**、右上灰偏移环、mesh 是不规则网状（非星座）。节点用**连通域检测**自动提取（白 13/黑 10/红 1）。产物：`repro-faithful.png`（复现）、`star-imagery.png`（星象版：星等差+细连线+碎星）、`compare-original-repro-stars.png`（三联）。待所有者判定。
- **教训：目测坐标不可靠，逐像素测量/连通域检测才是复现的正路**；另外 RGBA 转 RGB 会把透明角衬成黑，对比图必须带 mask 粘贴。


- 打包版运行时 Chromium 打了几条 `Unable to move the cache: 拒绝访问 (0x5)` / `Gpu Cache Creation failed: -2`，为磁盘缓存争用的内部告警，应用照常完成加载，非阻断。
- **目录结构守卫已落地**：`tests/test_folder_structure_guard.py`（4 条，红绿各跑一次验证会响）。台账**单一事实源 = 规范文档 S2 节**（守卫解析文档，不另抄清单）：① 根级台账外目录即红（红消息给出两条出路：进台账 / 按 S3 改名或删除）；② 反向钉住核心模块必须始终在台账（防条目被静默删除后 ① 因目录不在盘上而管不到）；③ 命名族 ignore 规则须命中假想新目录（R1）；④ 文档本体存在。提交前清单已加第 5/6 条（clean_worktree dry-run + 结构守卫）。
