# 2026-09-17 工作日志（接续会话）

## 入场核实（距上次 2 天）

上次会话结束于 9/15 22:37（`c0dd1fb0`）。9/17 一天内另有大量推进（**~40 个提交**）：

- **主线 = R2 整模型语言能力**（9/16 用户决策"先推主线"，Mini 验收后置）。
- **R2-H3.8 全流程闭环**：
  - `12381e1c` 冻结 matched-dev 预注册 + 数值预算；
  - `55f7bf62` H3.8 首个 matched dev ⇒ **`workspace_unused`**（workspace 无因果负载）；
  - `95547d0a` → `881819c0` → `2edbcb67` → `12748e69` **H3.8-v2 修订**（单一前缀通道）⇒ **仍 `workspace_unused`**；
  - `2a922986` **按预注册读数结项**：lesion 非全正 ⇒ 不能用 bypass 解释 ⇒ **归因升级到
    W-generation 路径本身** ⇒ **该架构族停止投入预算**；R2 主线回到
    **user-gated candidate re-deliberation point**（两 seed lesion 翻转作为方向性证据保留）。
- **CAP-0 身份重锚**（`2dec6145`）：9/15 的基线**不含执行身份**（git head / checkpoint sha256 /
  eval-set sha256）；runner 现在把三者绑进 identity block；在当前 checkpoint 上重跑复现 9/15 画面
  （C 0/14、D 0/16、E 0/20；B/G 待人工复核；A/F/H not_executed）
  ⇒ **"无法复用旧身份"的缺口已闭合，但语言缺陷在当前身份上依旧完全存在**。
- `91ff600a` roadmap 证据表收敛（H3.7B 三阶段闭合 / H3.9 六维总账 / H3.8 采纳路径）。

## 本轮做的事：收尾 9/17 的格式与 lint 债（`3b0f530d`）

入场时发现：**15 个文件不符合 black + ruff 4 项**（9/17 那批 ~40 提交留下的），
CI 的 black / ruff 步骤会红。

按 9/15 的教训，**格式化前先做锚点安全检查**：

- 收集全仓"多行字符串常量"（686 个，潜在逐字节锚点）⇒ **5 个**待格式化文件含锚点；
- 逐个比对 **black 的删除行 vs 锚点文本行** ⇒ **overlap 全部为 0**（改动不落在锚点文本内）；
- 单独确认 `taiji/model.py` 里的 **P3a 包装锚点** `next_symbol = step.predicted_symbol`
  **未被 black 触及**。

处置：
- black 15 文件；`ruff --fix` 1 处（import 排序）；
- 手工把 `checkpoint_roundtrip_preflight` / `paired_checkpoint_diagnostic` 加入 `taiji/__all__`
  —— ruff 报的 F401 "unused import" 实为**对外 re-export**
  （同组 `factorized_response_plan_preflight` 本就在 `__all__` 里）。

验证：`import taiji` 正常、`__all__` 723 项；`ruff check .` **All checks passed**；
`black --check .` **1210 files unchanged（0 待格式化）**。

**入场时的两个假信号（如实记录）**：
1. `black --check .` 结果在 **0 / 15 之间交替**，一度怀疑有并发会话在改仓库；
   三次连续采样 + `tasklist` 确认**无并发**（唯一 python.exe 是我自己），
   且 `.git/hooks/pre-commit` **不存在**（只有 `.pre-commit-config.yaml`，hook 未安装）
   ⇒ 交替来自**读取输出不完整**，不是并发。
2. `git status` 干净但 `git log` 无新提交 ⇒ 是"13:24 的改动被我 black 回 HEAD 形态"所致。

## 全量回归结果与失败分类（**12 failed / 1612 passed / 6 skipped**，21.5 min）

逐项定位归属：

| # | 测试 | 根因 | 归属 | 状态 |
|---|---|---|---|---|
| 1 | `test_project_identity::…one_execution_owner…` | `expected exactly one execution owner, got []` | 9/17 改 03 文档引入 | **待修** |
| 2–9 | `test_b0_b2_selection_contract`（**8 项**） | `KeyError: 'G1_intervention_reality' / 'context_conditioned' / 'pooled' / 'controls' / 'intervention_reality' / 'boundary'` + 1 个 `TypeError` | 待查（B2 报告键缺失） | **待查** |
| 10 | `test_b0_n2_stop_reason_disposition_contract` | 新增 5 个消费者：`eval_taiji_r2_h3_7_dev_aggregate.py`、`eval_taiji_r2_h3_6b_dev_aggregate.py`、`audit_taiji_r2_h3_7_attribution.py`、`eval_taiji_p5_2d_online_writeback_gate.py`、`taiji/language_alignment.py` | 9/17 新增文件 ⇒ 扫描面变化 | **待处置** |
| 11 | `test_cap0_baseline_contract::test_utf8_dfa_…` | `assert 128 == 160` —— `_utf8_allowed` 的三字节分支被 `2667b93a` 从 `remaining==3` **修正为** `remaining==2`，9/15 的契约没跟上 | 代码已修正、契约未同步 | ✅ **已修** |
| 12 | `test_naming_boundary_contract::…never_imports_legacy_or_transformers` | `taiji/language_alignment.py` 与 `taiji/sequence_workspace.py` 内部 `from seed.persistence import atomic_save` | **9/17 引入的架构铁律违约** | ✅ **已修** |

### 已修两项（提交 `df95c4d3`）

- **#12（架构违约，最严重）**：新增自足的 `taiji/persistence.py`（`atomic_save`，
  仅依赖 stdlib + torch），两处改为 `from .persistence import atomic_save`；
  **未改任何测试断言**（铁律要求修代码，不放宽测试）。
  语义与 `seed.persistence.atomic_save` 一致（同目录 `mkstemp` + `os.replace` +
  陈旧 tmp 清理 + 异常删 tmp 再 raise），与 `structural_artifact_store.py` 既有模式同源。
- **#11（契约未同步）**：DFA 契约的 3 字节断言由 `remaining==3` 改为 `remaining==2`，
  并补一条正向断言；**代码不动**。
- 验证：`naming_boundary` + `cap0_baseline` + `sequence_workspace` + `language_alignment`
  四项契约 **69 passed**；`ruff check .` All checks passed；`black --check .` 1211 unchanged。

### 入场时的两个假信号（如实记录）

1. `black --check .` 结果在 **0 / 15 之间交替**，一度怀疑有并发会话在改仓库；
   三次连续采样 + `tasklist` 确认**无并发**（唯一 python.exe 是我自己），
   且 `.git/hooks/pre-commit` **不存在**（只有 `.pre-commit-config.yaml`，hook 未安装）
   ⇒ 交替来自**读取输出不完整**，不是并发。
2. `git status` 干净但 `git log` 无新提交 ⇒ 是"13:24 的改动被我 black 回 HEAD 形态"所致。

### 再修一项：#2–9（B2 选择门 8 项）—— **归档报告被覆盖 + schema 重构**

根因（对比 git 两个版本后确认）：

- `fde682ef`（9/15 归档版）：顶层键含 `pooled` / `controls` / `estimation_targets` /
  `intervention_reality` / `corpus` / `boundary` / `outcome_detail` / `preregistration_gap` /
  `stability`；`gates` 键为 **`G1_intervention_reality` … `G6_budget_gate`** —— **正是契约所断言的**。
- `5abb123a`（9/16 重跑版）：**schema 重构**（29 → 20 个顶层键），`gates` 键改为小写
  `g1_intervention_reality` / `g4_routing_beats_deployable` / `g6_budget` …，
  上述 9 个顶层键**全部消失** ⇒ 契约 `KeyError` × 8。

**关键：两次执行结论一致**（都 `outcome=failed`、`experiment_passed=False`）
⇒ 这是「**归档被覆盖 + schema 升级**」，**不是把负结果改成正结果**。

**按铁律「归档报告只追加、不改写」处置**（未改契约任何断言）：
- 把 `fde682ef` 版**恢复**回 `reports/taiji_b0_b2_selection_20260915.json`（归档位，12571 B）；
- 把 `5abb123a` 版**另存**为 `reports/taiji_b0_b2_selection_rerun_20260916.json`（保留重跑证据，12388 B）。

验证：`test_b0_b2_selection_contract` **10 passed**。

### 失败清单收敛：12 → **0**（再修 #10 与 #1，提交 `716bb393`）

**#10 N2 消费面扫描器**（7 个新增项**逐个判定**，不是一刀切）：
- **真消费者 2 个** ⇒ 登记进 `EXPECTED_CONSUMERS`：
  `eval_taiji_b0_b2v4_collaboration_gate.py`（`record_only`，`_stop_reason_counts` 统计频次）、
  `audit_taiji_r2_h3_7_attribution.py`（`judgement`，断言冻结的 aggregate reason 出现在 R2 决策）；
- **误报 5 个** ⇒ 记入 `SCAN_EXCLUSIONS`（每条写明理由）：
  `eval_taiji_p5_2d_online_writeback_gate.py`（只有一个计数键，不读 reason）、
  `r2_h3_6b_dev_aggregate.py` / `r2_h3_7_dev_aggregate.py`（**产生**自己的 R2 stop 决策，非消费）、
  `taiji/language_alignment.py` 与 `test_language_alignment.py`（**同名不同义**：那里的
  `stop_reason` 是生成终止原因 `end_marker`）。

重新生成 N2 报告（它记录"当前扫描面"，清单变了必须同步；历史 hardening 的 `file_count=11` 不动）：
`consumer_count` / `consumer_count_now` **17→19**、`added_since_hardening_report` **6→8**、
`classification_counts` **{record_only 8, judgement 9} → {9, 10}**；
契约里这 4 处数字**显式同步**（是清单真变大，不是放宽断言）。

**#1 唯一执行 owner**：`test_project_identity` 要求活跃计划树里**恰有一条**
`## 当前唯一下一步：` 且必须在 `03_CURRENT_EXECUTION.md`；9/17 把 03 头部改成引用块后变成
**0 条** ⇒ `got []`。已在 03 补回该标题，内容反映：H3.8 结项（架构族停止投入）/
**R2 停在 user-gated 候选重审议点** / CAP-0 身份重锚已闭合但语言缺陷犹在。

**踩坑（新的）**：用 **bash 内联 python 写文件时，反引号被 shell 当命令替换**
（bash 报 `workspace_unused: command not found`）⇒ 两处内容被吃掉 ⇒ **改用 Edit 工具补回**。
**教训**：bash 内联 Python 时**避免反引号**（与既有"避免 heredoc / `-c` 内长中文"同源）。

验证：受影响 5 个契约文件 **64 passed**（project_identity、b0_b2_selection、
b0_n2_stop_reason_disposition、cap0_baseline、naming_boundary）；
`ruff check .` All checks passed；`black --check .` **1211 unchanged**；
03 的 **44 条链接 0 broken**。

### 全量复跑：6 failed = **1 我引入 + 5 环境污染**

`pytest tests/`（30 分钟）：**6 failed / 1618 passed / 6 skipped**（上一轮 12 failed）。

**A. 我引入（1 项，已修）**：
`test_b0_n2_stop_reason_semantics_contract::test_the_frozen_preregistration_states_the_measured_counts`
要求**冻结预注册文档里出现 live 计数（19）**，同时保留 `"17 个消费文件"` 与 `"10 处判断点"`。
⇒ 按「**冻结文档只追加、不改上文结论**」在 §7 之后追加 **§8**，写明 2026-09-17 起 live 面为
**19 个消费文件**（2 个真消费者 + 5 个误报入 `SCAN_EXCLUSIONS`），并声明冻结时点的
"17 个消费文件"表述**保持原样**（L22/L52 未动）。**27 passed** ✓

**B. 环境污染（5 项）**：`runtime_artifact_store_audit_projection`、
`runtime_artifact_store_runtime_reconciliation`、`runtime_retention_store_audit`、
`structural_artifact_measurement_bundle_recovery`、`structural_artifact_measurement_sidecar`。
症状：store 目录出现杂质（`unexpected file: invalid-name.json`）、
orphan 集合多出 `external_orphan`、`assert 2 == 1`、`DID NOT RAISE ValueError`。
⇒ **单独复跑全部通过（5 passed）** ⇒ 确认是**全量跑的产物污染**，非代码问题。
⇒ 这是**测试隔离缺陷（DEBT-I7）的实例**：某个测试把产物写进共享目录且未清理，
污染后续测试。**它让"全量测试"这一最基本的验收手段不可靠** —— 30 分钟里混进 5 个假失败，
有掩盖真失败的风险。（注意 `outputs/r2_h3_7b_repair_20260917/` 至今未跟踪、未被清理。）

### 用户授权 P3b-v2：冻结「目标对齐」预注册（2026-09-17）

**用户答复「是」** ⇒ 授权按"目标对齐"方向冻结预注册并执行训练。

**我给出的决策依据（用户认可）**：H3.3→H3.8 机制族全落空 + P1 证明训练目标是
**纯字节自监督** + P3a 证明**编码层不是瓶颈** ⇒ **缺的是目标，不是机制**。

**预注册要点**（`M5_P3B_V2_GOAL_ALIGNED_PREREGISTRATION_FROZEN_20260917.md`，已冻结）：

- **入口**：`scripts/training/train_taiji_r2_aligned.py`（自带 `checkpoint_roundtrip_preflight` 零步前置）；
- **起点**：`reports/r2_h3_7b_validation/byte_aligned/checkpoint.pt` —— **16,502,913 B**，
  sha256 `285d4b23a88bc458e486201a382a9e66b3551a1ff29f077f6c1f6368defa696f`，
  报告 `status=passed_engineering_only`（含 prior/credit 修复 + byte-aligned target）；
- **数据**：结构化 `LanguageEpisode` jsonl（8 键：`episode_id`/`family_id`/`task_family`/
  `split`/`context`/`user_input`/`response`/`required_terms`）；基准是 H3.5a fixture（24 行）；
  **P3b-v2 的差异化 = 放大规模**（模板 × 实体池合成 train/dev/final 三分离集）；
- **目标**：response-only 条件化信用（沿用 H3.7B 的 0.75/0.25 混合 + byte-aligned 16-byte 窗口），
  **不改架构**；
- **推理链路**：必须与 P3a 一致（放宽 legacy 守卫 + UTF-8 约束解码），否则与基线不可比；
- **判据**：J1–J5 沿用 + **J6（新）：dev `exact_response` > 0** —— H3.x 全系列 exact **恒为 0**
  ⇒ 这是"目标对齐是否真的发生"的**直接证据**（比 surprise 更能证伪）；
- **停止线**：非有限值 / **J6 未达成** / 连续 3 检查点无改善 / 退化则回滚 / wall cap；
- **反假设**：若仍无改善 ⇒ **缺口在架构层**，本包结项、不再加机制（H3.8 族已按预承诺停止）。

**关键认知（已写进预注册 §1）**：H3.5a–H3.8 **用的就是同种结构化 episode** 却全 negative
⇒ P3b-v2 的差异化**不能只是"换个数据"**，必须是三者叠加：
① **H3.7B 的一致性修复**（prior + 0.75/0.25 信用）+ ② **放大规模** + ③ **byte-aligned target**。

**待办（执行前必补，否则不启动）**：数据合成脚本与 sha256；放大档预算（pilot 实测吞吐外推）。

### P3b-v2 数据侧完成（`12602a6f`）

生成器 `scripts/training/build_taiji_r2_p3b_v2_corpus.py`（沿用 H3.8 数据合同的分离规则）；
产物 `tests/fixtures/r2_p3b_v2_goal_aligned_episodes_v1.jsonl`
（**80 行** = train 40 / dev 20 / final 20，**3.3 ×** H3.5a 的 24 行，满足"放大规模"）；
`corpus_digest = 4d19974783cbd093a968a8e9fb1a4e945e060843a8c34f3bc1c571fce0311f2b`；
**7 项分离自检全过**（五形状齐备 / 实体池互斥 / 提问 split 内唯一 / 提问跨 split 互斥 /
组合跨 split 互斥 / required 均在 response 内 / train 无 holdout 泄漏）；
`LanguageEpisodeCorpus.from_jsonl` **实测加载成功**（manifest digest `5fcebf34…`）。

**生成期自检抓到并修掉三处真问题**（自检的价值，属推进而非反复）：
1. `unknown` 形状原带 `required_terms=[value]`，但它的 response 是"没有信息"
   ⇒ **改空**（**诚实弃答不应被要求命中实体**）；
2. 实体池原用**笛卡尔积** ⇒ 同一实体多值 ⇒ 同 split 内**提问重复** ⇒ **改一一配对**；
3. dev/final 的 `combination` 模板 `user_input` **漏了 `{item}`** ⇒ 4 个实体产生相同提问 ⇒ 补上。

**另一条硬约束（LanguageEpisode 强制）**：`episode_id` / `family_id` 必须是
**ASCII 稳定标识符** ⇒ 用 `<split>-<shape>-<index>`，**中文实体只出现在文本槽**。

**数据格式差异（重要）**：H3.8 的语料是 6 键（`prefix`/`response`/`shape`/`entities`/
`split`/`template_id`），而 `train_taiji_r2_aligned.py --dataset` 要 **8 键
`LanguageEpisode`**（`context`/`user_input`/`response`/`required_terms`/`split`/
`task_family`/`family_id`/`episode_id`）⇒ **两者不通用**，必须各建各的语料。

⇒ **预注册 §6 只剩"放大档预算"**（需 pilot 实测吞吐外推）⇒ **pilot 可立即启动**。

### P3b-v2 pilot 启动（途中修掉三个配置约束）

**先摸清入口语义**（避免误用）：
- `--checkpoint` **不是起点**，而是**输出路径 + 受保护检查**（`_refuse_protected` 只拒 `checkpoints/<PROTECTED_NAMES>`）；
- **真正的起点是 `--resume`**；且 resume 时 `--preregistration` / `--response-plan-target-geometry` /
  `--response-plan-variant` **必须留默认**（由 checkpoint 自带各自契约）。

**修掉的三个约束**（逐个由报错精确指出，不是盲试）：
1. **`ValueError: language alignment checkpoint corpus digest mismatch`** ——
   H3.7B 的 child 绑定 `corpus_digest = 0bc5b5540d4b622f907942f42d7b809e825932e50c6e6c505a3cd091edabd691`，
   **正是 24 行的 `r2_h3_5a_response_plan_v3.jsonl`** ⇒ **「继承其权重」与「放大语料规模」不可兼得**
   （digest 绑定是**设计**，防跨语料复用）⇒ **改为从零构建 + P3b-v2 语料**（预注册追加 **§7.3** 记录，
   并写明报告必须披露"起点不是 H3.7B child"）；
2. **`the H3.7 contract fixes plan width 48 and four slots`** ⇒ 补 `--response-plan-width 48`
   （默认 32 不合 H3.7 合同）+ `--response-plan-slots 4`。

**最终 pilot 配置**（从零构建，factorized plan）：
`--epochs 2 --max-episodes 12 --seed 20260917 --parameter-budget 300000
 --response-plan-readout --response-plan-variant factorized_v1 --response-plan-width 48
 --response-plan-slots 4 --response-plan-target-geometry h3_7_factorized_response_chunks
 --defer-final --sequence-diagnostic`

⇒ **已成功进入训练**（>15 分钟无报错，说明配置校验与零步 preflight 均通过）。
**J6（dev `exact_response` > 0）是本次 pilot 的判定核心。**

## 主线状态与可继续的方向

**R2 主线处于 user-gated candidate re-deliberation point**（`91ff600a` 明示）——
即**需要用户对候选方向重新审议**；H3.8 架构族已按预承诺停止投入。

**我能继续做的（不需额外授权）**：
- 分析全量测试失败清单（区分既有 / 新引入）—— 本轮已启动；
- 处置技术债 §4.2（`select` 边界**无容差**；9/15 已诊断 + 已加契约测试，**修法 A/B 待拍板**）。
