# 2026-09-17 工作日志（接续会话）

## 入场核实（距上次 2 天）

上次会话结束于 9/15 22:37（`c0dd1fb0`）。9/17 一天内另有大量推进（**~40 个提交**）：

- **主线 = R2 整模型语言能力**（9/16 用户决策"先推主线"，Mini 验收后置）。
- **R2-H3.8 全流程闭环**：
  - `12381e1c` 冻结 matched-dev 预注册 + 数值预算；
  - `55f7bf62` H3.8 首个 matched dev ⇒ **`workspace_unused`**（workspace 无因果负载）；
  - `95547d0a` → `881819c0` → `2edbcb67` → `12748e69` **H3.8-v2 修订**（单一前缀通道）⇒ **仍 `workspace_unused`**；
  - `2a922986` **按预注册读数结项**：lesion 非全正 ⇒ 不能用 bypass 解释 ⇒ **归因升级到
    W-generation 路径本身** ⇒ **该架构族停止投入预算**；R2 主线回到
    **user-gated candidate re-deliberation point**（两 seed lesion 翻转作为方向性证据保留）。
- **CAP-0 身份重锚**（`2dec6145`）：9/15 的基线**不含执行身份**（git head / checkpoint sha256 /
  eval-set sha256）；runner 现在把三者绑进 identity block；在当前 checkpoint 上重跑复现 9/15 画面
  （C 0/14、D 0/16、E 0/20；B/G 待人工复核；A/F/H not_executed）
  ⇒ **"无法复用旧身份"的缺口已闭合，但语言缺陷在当前身份上依旧完全存在**。
- `91ff600a` roadmap 证据表收敛（H3.7B 三阶段闭合 / H3.9 六维总账 / H3.8 采纳路径）。

## 本轮做的事：收尾 9/17 的格式与 lint 债（`3b0f530d`）

入场时发现：**15 个文件不符合 black + ruff 4 项**（9/17 那批 ~40 提交留下的），
CI 的 black / ruff 步骤会红。

按 9/15 的教训，**格式化前先做锚点安全检查**：

- 收集全仓"多行字符串常量"（686 个，潜在逐字节锚点）⇒ **5 个**待格式化文件含锚点；
- 逐个比对 **black 的删除行 vs 锚点文本行** ⇒ **overlap 全部为 0**（改动不落在锚点文本内）；
- 单独确认 `taiji/model.py` 里的 **P3a 包装锚点** `next_symbol = step.predicted_symbol`
  **未被 black 触及**。

处置：
- black 15 文件；`ruff --fix` 1 处（import 排序）；
- 手工把 `checkpoint_roundtrip_preflight` / `paired_checkpoint_diagnostic` 加入 `taiji/__all__`
  —— ruff 报的 F401 "unused import" 实为**对外 re-export**
  （同组 `factorized_response_plan_preflight` 本就在 `__all__` 里）。

验证：`import taiji` 正常、`__all__` 723 项；`ruff check .` **All checks passed**；
`black --check .` **1210 files unchanged（0 待格式化）**。

**入场时的两个假信号（如实记录）**：
1. `black --check .` 结果在 **0 / 15 之间交替**，一度怀疑有并发会话在改仓库；
   三次连续采样 + `tasklist` 确认**无并发**（唯一 python.exe 是我自己），
   且 `.git/hooks/pre-commit` **不存在**（只有 `.pre-commit-config.yaml`，hook 未安装）
   ⇒ 交替来自**读取输出不完整**，不是并发。
2. `git status` 干净但 `git log` 无新提交 ⇒ 是"13:24 的改动被我 black 回 HEAD 形态"所致。

## 全量回归结果与失败分类（**12 failed / 1612 passed / 6 skipped**，21.5 min）

逐项定位归属：

| # | 测试 | 根因 | 归属 | 状态 |
|---|---|---|---|---|
| 1 | `test_project_identity::…one_execution_owner…` | `expected exactly one execution owner, got []` | 9/17 改 03 文档引入 | **待修** |
| 2–9 | `test_b0_b2_selection_contract`（**8 项**） | `KeyError: 'G1_intervention_reality' / 'context_conditioned' / 'pooled' / 'controls' / 'intervention_reality' / 'boundary'` + 1 个 `TypeError` | 待查（B2 报告键缺失） | **待查** |
| 10 | `test_b0_n2_stop_reason_disposition_contract` | 新增 5 个消费者：`eval_taiji_r2_h3_7_dev_aggregate.py`、`eval_taiji_r2_h3_6b_dev_aggregate.py`、`audit_taiji_r2_h3_7_attribution.py`、`eval_taiji_p5_2d_online_writeback_gate.py`、`taiji/language_alignment.py` | 9/17 新增文件 ⇒ 扫描面变化 | **待处置** |
| 11 | `test_cap0_baseline_contract::test_utf8_dfa_…` | `assert 128 == 160` —— `_utf8_allowed` 的三字节分支被 `2667b93a` 从 `remaining==3` **修正为** `remaining==2`，9/15 的契约没跟上 | 代码已修正、契约未同步 | ✅ **已修** |
| 12 | `test_naming_boundary_contract::…never_imports_legacy_or_transformers` | `taiji/language_alignment.py` 与 `taiji/sequence_workspace.py` 内部 `from seed.persistence import atomic_save` | **9/17 引入的架构铁律违约** | ✅ **已修** |

### 已修两项（提交 `df95c4d3`）

- **#12（架构违约，最严重）**：新增自足的 `taiji/persistence.py`（`atomic_save`，
  仅依赖 stdlib + torch），两处改为 `from .persistence import atomic_save`；
  **未改任何测试断言**（铁律要求修代码，不放宽测试）。
  语义与 `seed.persistence.atomic_save` 一致（同目录 `mkstemp` + `os.replace` +
  陈旧 tmp 清理 + 异常删 tmp 再 raise），与 `structural_artifact_store.py` 既有模式同源。
- **#11（契约未同步）**：DFA 契约的 3 字节断言由 `remaining==3` 改为 `remaining==2`，
  并补一条正向断言；**代码不动**。
- 验证：`naming_boundary` + `cap0_baseline` + `sequence_workspace` + `language_alignment`
  四项契约 **69 passed**；`ruff check .` All checks passed；`black --check .` 1211 unchanged。

### 入场时的两个假信号（如实记录）

1. `black --check .` 结果在 **0 / 15 之间交替**，一度怀疑有并发会话在改仓库；
   三次连续采样 + `tasklist` 确认**无并发**（唯一 python.exe 是我自己），
   且 `.git/hooks/pre-commit` **不存在**（只有 `.pre-commit-config.yaml`，hook 未安装）
   ⇒ 交替来自**读取输出不完整**，不是并发。
2. `git status` 干净但 `git log` 无新提交 ⇒ 是"13:24 的改动被我 black 回 HEAD 形态"所致。

### 再修一项：#2–9（B2 选择门 8 项）—— **归档报告被覆盖 + schema 重构**

根因（对比 git 两个版本后确认）：

- `fde682ef`（9/15 归档版）：顶层键含 `pooled` / `controls` / `estimation_targets` /
  `intervention_reality` / `corpus` / `boundary` / `outcome_detail` / `preregistration_gap` /
  `stability`；`gates` 键为 **`G1_intervention_reality` … `G6_budget_gate`** —— **正是契约所断言的**。
- `5abb123a`（9/16 重跑版）：**schema 重构**（29 → 20 个顶层键），`gates` 键改为小写
  `g1_intervention_reality` / `g4_routing_beats_deployable` / `g6_budget` …，
  上述 9 个顶层键**全部消失** ⇒ 契约 `KeyError` × 8。

**关键：两次执行结论一致**（都 `outcome=failed`、`experiment_passed=False`）
⇒ 这是「**归档被覆盖 + schema 升级**」，**不是把负结果改成正结果**。

**按铁律「归档报告只追加、不改写」处置**（未改契约任何断言）：
- 把 `fde682ef` 版**恢复**回 `reports/taiji_b0_b2_selection_20260915.json`（归档位，12571 B）；
- 把 `5abb123a` 版**另存**为 `reports/taiji_b0_b2_selection_rerun_20260916.json`（保留重跑证据，12388 B）。

验证：`test_b0_b2_selection_contract` **10 passed**。

### 失败清单收敛：12 → 2

| 状态 | 项 |
|---|---|
| ✅ 已修 | #12 架构违约、#11 DFA 契约、#2–9 B2 归档（共 10 项） |
| ⬜ 待处置 | **#10** N2 扫描器新增 5 个消费者；**#1** project_identity 缺 execution owner |

## 主线状态与可继续的方向

**R2 主线处于 user-gated candidate re-deliberation point**（`91ff600a` 明示）——
即**需要用户对候选方向重新审议**；H3.8 架构族已按预承诺停止投入。

**我能继续做的（不需额外授权）**：
- 分析全量测试失败清单（区分既有 / 新引入）—— 本轮已启动；
- 处置技术债 §4.2（`select` 边界**无容差**；9/15 已诊断 + 已加契约测试，**修法 A/B 待拍板**）。
