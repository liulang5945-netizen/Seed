# 2026-09-19 工作日志

## 仓库修复：推送积压 + 泄漏凭据轮换

**背景**：用户报「我需要修仓库」。只读体检发现远端停在 9/18 的 29fb9a12，本地领先 27 个提交
（0/27 纯 ahead，无分叉）—— 9/18 晚之后另有会话在此本地提交了一批工作（含
727c53f7「停止跟踪打包运行生成的凭据」、938665e7「R2 content binding candidate contract」、
以及一批 DEBT-I*/隔离修复/普查收口）。

**执行的**：
1. **推送 27 个提交** -> origin/main 从 29fb9a12 推进到 727c53f7
   （push --dry-run 事先 rc=0，仓库仅 24.5 MiB，无冲突）。
2. **处置凭据泄漏**（用户选「推送 + 处置凭据泄漏」）：
   - 影响面实测：三套凭据里**只有** output/p6-1d-packaged-data/security/ 那套被跟踪过
     （引入于 8f7fc6fa，9/01，已在远端历史）；security/ 与 dist/Seed/security/ 一直未被跟踪。
   - .gitignore 的 **/security/{.jwt_secret,.storage_salt,audit_logs/} 修复**经 check-ignore 实测彻底。
   - **执行轮换**：备份到仓库外 E:/Seed-backup-secrets-20260919/ 后删除那两个文件。
     **交叉印证**：备份的 .jwt_secret sha256 前 16 位 = 提交信息里记的 pkg 值 e6401f86c1313d6d，逐位吻合。
   - **隔离实证「删除即轮换」**：空目录下 JWTManager() 自动生成 64 字符密钥、SecureStorage()
     自动生成 32 字节盐、二次实例化复用不重生成。
   - **未做并说明理由**：不动未泄漏的另两套（security/ 的 salt 可能派生着存量密文，删了会丢数据）；
     不重写历史（值已轮换后收益低，代价是全部哈希改写 + force push）。

**结论/状态**：本轮结束时 origin/main = d830ac94，本地无 ahead；工作区仅剩用户的
taiji/organs.py 未提交（全程未触碰）。方案与执行记录见
plans/reference/REPO_SECRET_REMEDIATION_20260919.md。

**顺带**：MEMORY.md 已于 9/18 整理（18669 -> 6575 字符，六章节结构）。

## 加固 C：`.gitignore` 按约定覆盖打包运行目录

**根因同类性**：`.gitignore` 里 `output/` 是**逐个实例名**列举（`output/taiji_m5_k_p3_0_*` 等一长串），
`p6-1d-packaged-data` 不在列表 ⇒ 被跟踪。**与"带斜杠锚定"是同一类疏忽：写死实例名而非命名约定。**

**改法**（`.gitignore` L215–220，两条互补）：
```
output/*-packaged-data/     # 按命名约定覆盖整族
output/p6-*/                # 兜住当前实例
```
**不能整体 ignore `output/`**：那里有 23 个**有意入库**的版本化验收证据
（`manual-r5-*/README.md`、`output/playwright/*.png`）。

**守卫 2 → 4 条**（`tests/test_repo_secret_guard.py`）：新增
① 按约定覆盖（同时探 `p6-1d-packaged-data` 与**假想的** `anything-packaged-data`）；
② **反向钉住**（通配不得吞掉有意入库的产物 —— 只查一侧会接受"为了不泄漏而把验收证据一起 ignore"的坏中间态）。

**实测**：`output/p6-1d-packaged-data/data/app_settings.json` 与
`output/v9-candidate-packaged-data/security/.storage_salt` 均被命中；
`manual-r5-s0/README.md`、`playwright/*.png` **仍被跟踪**；`output/` 下跟踪数 **23 不变（零误伤）**；
守卫 **4 passed**。

⇒ **两道独立防线**：凭据由 `**/security/*` 护（与目录名无关）；运行产物由 `<name>-packaged-data`
命名约定护。都不再依赖逐个实例名列举。**仓库修复工作到此结束**（origin/main = 73c944e5）。

## 更正与补强（用户核对后）

用户独立核对**推翻了我文档里的两条前提**，我复现后确认成立，已全部更正：

1. **`security/.storage_salt` 并非"未泄漏"** —— 在位值指纹 `f1d2ed7bdd5ea4f9` 等于**已推送历史里的
   blob `5bfda9cd`**（逐位相同）⇒ 原 §2B「未泄漏却主动轮换 = 白担风险」的理由**对该文件失效**，
   结论修正为**应当轮换**。（`.jwt_secret` 的「未泄漏」成立：该路径历史 0 个 blob。）
2. **三套凭据并非互不相同** —— `dist/Seed/security/` 与 `security/` 的两个文件指纹**都相同**
   ⇒ dist 构建把开发机这套凭据**原样打了进去**（若已分发，接收方拿到同一个盐；属**发布面**）。

**已执行**：

- **轮换根盐**：先全仓扫 Fernet token（`gAAAAA`）—— `dist/ frontend/ output/ outputs/ security/
  data/ seed_platform/ neuroplex/ api/ .workbuddy/ artifacts/` 共 **6739 文件 0 命中**
  ⇒ 无存量密文；备份泄漏的盐到仓库外后删除；root 重建为新盐 `f75efc8df6516828`（≠ 泄漏值），
  `dist/` 那份由其下次运行重建。
- **新增第 5 条守卫**：比对「在位凭据 vs 历史 blob 指纹」，**逐路径查询**（避免合并查询的归因错误）。
  **红/绿各跑一次证明它能响**：轮换前 `1 failed, 4 passed`（精准报出 storage_salt）；
  轮换后 `5 passed`。
- **处置 `report.xml`**（pytest junit 产物，内含 `hostname="DESKTOP-45AH838"` —— 正是机器指纹的
  hostname 来源）：`git rm --cached`（本地文件保留）+ ignore `report.xml` / `junit*.xml`。
- **查出并登记第三道同类漏洞**：`outputs/`（复数）目录被 ignore 但**子文件不被覆盖**
  （`outputs/x.txt` 实测 NOT ignored）；`git ls-files outputs report.xml` 共 6 个，
  其中 `outputs/` 下 5 个疑似**有意入库**的证据**未动**。

**方法教训**（已入 `MEMORY.md` §4e）：判泄漏要比「在位值 vs 历史 blob 指纹」，而非数提交个数
或看目录是否被 ignore；多路径合并查询不能逐路径归因；守卫只测「规则命中/清单为空」
拦不住「在位值 == 历史 blob」；「目录规则不覆盖子文件」要按命名约定通配且**反向钉住**。

**⚠️ 一个自己的反复失误**：本轮我**第四次**踩「反引号被 shell 当命令替换」的坑
（这次是在 `python -c "…'''…'''…"` 里含反引号 ⇒ shell 执行了 `security/.storage_salt` 等
不存在的命令，并试图把密钥文件当脚本跑；仅 `syntax error`，未产出文件）。
**该纪律早已写入记忆却仍复犯** ⇒ 说明「记在文档里」不足以保证执行；**凡含反引号/代码片段，
一律走 Write/Edit 工具，不要塞进 shell 引号**。

**结束时状态**：`origin/main = 7ca89637`；用户的 `taiji/organs.py` 全程未触碰。

## 🔴 事故与恢复：`.git` 被部分清空（15:5x）

**用户指令**：确认 dist 未分发 ⇒「旧值清掉」（即历史重写）+ 询问 `outputs/` 下 5 个文件是什么。

**先说 `outputs/` 那 5 个文件**（已查明）：
- `project-improvement-20260915/baseline-commands.json`（349 B）＝ ruff/typing 的 before 基线（命令+退出码+耗时）；
- `project-improvement-20260915/focused-before.xml`（9.1 KB）＝ **pytest junit 产物，含 `hostname`** ⚠️（与 report.xml 同类）；
- `r2_h3_7b_repair_20260917/*/report.json`（3 个）＝ R2-H3.7B 修复臂报告（byte_aligned / retry1 / legacy_target）。
- **依据 2026-09-17 的日志**：「`outputs/r2_h3_7b_repair_20260917/` 至今未跟踪、未被清理」
  ⇒ 说明它当年是**运行产物**、**后被误 `git add`**，**不是有意入库的证据**。

**事故经过**：
1. 为历史重写做准备：先 `shutil.copytree` 备份整个 `.git` ⇒ `E:/Seed-backup-git-20260919`（60.2 MiB / 109 项，
   已核对 HEAD 与提交数一致）**这一步救了命**；
2. 随后跑一个准备脚本（移临时安装 / 备份 organs.py / `git stash`）⇒ **该脚本超时被 SIGTERM**；
3. 之后 `git status` 报 `fatal: not a git repository`；
4. 检查发现：`.git` 只剩 `objects/`(70 子目录) + `logs/` + 我历次写的 `COMMIT_MSG_*`（共 34 项），
   **`HEAD` / `config` / `refs` / `index` 全部消失** ⇒ 仓库结构文件被清掉（对象库还在）。

**恢复**：把损坏的 `.git` 改名保留 → 从备份整目录复制回来 ⇒
`rev-parse --is-inside-work-tree = true`、`HEAD = 281efee8`（与事故前一致）、1695 提交、
`## main...origin/main`、`git stash list` 为空、**`taiji/organs.py` 与备份逐字节相同**。

**未查明**：具体是哪一步清掉了结构文件（候选：`git stash` 在 SIGTERM 中断 / 沙箱 safe-delete 守卫 / 其他）。
损坏现场留在 `E:/_git-broken-evidence-20260919/` 未删。

**教训**：
- **做危险操作前先备份，且备份要验证可读**（本次备份发生在事故前约 90 秒，是唯一的救命绳）；
- 超时/SIGTERM 前后的**中间态**可能不完整 ⇒ **长操作要拆成可验证的小步**；
- **历史重写不应在主仓库里做** —— 正确做法是 **`git clone --mirror` 出独立镜像、在镜像里 filter、
  验证后再推**，主仓库全程零风险。

**当前状态**：仓库已恢复，`origin/main = 281efee8`，未推送任何新提交；
**历史重写尚未执行**（旧值仍在历史里，但已失效）。`taiji/organs.py` 的未提交改动完好。

## ✅ 历史重写已完成（镜像隔离方式）

用户确认「dist 未分发」⇒「旧值清掉」，按镜像方式执行（主仓库全程只读）：

1. **扫全历史**：遍历历史中出现过的 **3644 个路径**，可疑的 7 个里 4 个是误报
   （本处置文档、守卫测试、两个 `tokenize_*` 脚本），**真正要删的正好 3 个**：
   `security/.storage_salt`、`output/p6-1d-packaged-data/security/.jwt_secret`、
   `output/p6-1d-packaged-data/security/.storage_salt`（`security/.jwt_secret` 从未提交）。
2. **镜像隔离**：`git clone --mirror` → 在镜像里 `git filter-repo --invert-paths` → 验证 → 强推。
   **主仓库不参与改写**（这是对上次 `.git` 事故的纠正：历史重写永远不该在主仓库里做）。
3. **推前再次验证**：3 个路径在历史里 0 提交、3 个原 blob 全部不可达 ⇒ 才推。
   推送结果：`+ 0774a387...96e1d6f5 main -> main (forced update)`。
4. **主仓库同步**：`fetch --force` + `reset --hard origin/main`，并从逐字节备份还原
   `taiji/organs.py`（sha16 `217d6ae4dd6e6858`，与备份一致）。
5. **收尾**：守卫 **5 passed**；`## main...origin/main` 同步；`M taiji/organs.py` 保持未提交。

### ⚠️ 两个必须记住的发现

- **`refs/codex/*` 不被 filter-repo 重写** —— 这是本次"删了却仍可达"的真正原因：
  镜像里有个 `refs/codex/turn-diffs/checkpoints/...`，它指向的旧树仍保留凭据 blob，而
  `filter-repo` 只重写 `refs/heads/*` 与 `refs/tags/*`。**处理**：删除该 ref + `repack -ad` 后
  才真正不可达。（该 ref 只存在于本地，从未推送到远端；主仓库里保留了它，未动，以免影响 codex 工具。）
- **远端 `refs/pull/1..19/head`** —— GitHub 的 PR 引用，**force-push 改不到**。
  若某个 PR 的提交里含那两个路径，**旧对象在 GitHub 侧仍可能短期可达**；彻底清除需删 PR 或
  联系 GitHub 支持。**此外**：force-push 后 GitHub 侧旧对象通常也不会立刻回收。
- **所有历史提交哈希已变**（1696 个里约 67% 的前缀都变了）⇒
  **本仓库文档/记忆里此前记录的旧 sha（如 `281efee8`、`7ca89637`）已不再是当前历史中的对象**。

### 回退点（保留，勿删）

| 路径 | 内容 |
|---|---|
| `E:/Seed-backup-git-20260919` | **重写前的完整 `.git`**（109 项，HEAD 为旧 `281efee8`）|
| `E:/Seed-backup-secrets-20260919` | 泄漏值原件 + `organs.py` 未提交版本 |
| `E:/_git-broken-evidence-20260919` | `.git` 事故现场（34 项）|

**最终状态**：`HEAD = origin/main = 96e1d6f5`（1696 提交），工作区仅 `M taiji/organs.py`。

## 收尾：`outputs/` 处置 + PR refs 的确切结论（一个关键洞察）

**① `outputs/` 处置**：`focused-before.xml`（pytest junit 产物，含 hostname）已 `git rm --cached`
并加入 `.gitignore`；`outputs/` 下跟踪数 5 → 4，其余 4 个（`baseline-commands.json` 与 3 个
`r2_h3_7b_repair_*/report.json`）作为研究过程产物保留。

**② PR refs 的确切结论**：
- 把远端 `refs/pull/*` 临时取到 `refs/prcheck/*` 逐个查证：**31 个 PR ref **全部**指向含旧盐
  `security/.storage_salt` 的提交**（每个都触及该路径 2 个提交，即那段历史在其祖先链里）。
  ⇒ 旧盐在 GitHub 侧**确实仍可通过 PR 引用取出**（不是可能性，是事实）。查完已删除临时 refs。
- `gh pr list --state all`：**31 个全部是 Dependabot 的依赖升级 PR，全部 CLOSED、全部未合并**
  ⇒ 它们含旧盐只是因为**基于当时的主线分支**。
- **GitHub 没有"删除 PR"的 API** ⇒ 关闭 ≠ 删除 ref ⇒ `refs/pull/*/head` 依然存在；
  彻底清除需**联系 GitHub 支持**。

**③ 一个关键洞察（改变了处置建议）**：
既然 `auth.py` 用**机器指纹**（`hostname|machine|USERNAME|processor`）当 PBKDF2 口令，
而其中 **hostname / username 本来就在仓库里**（`report.xml` 直接写着 hostname、`USERNAME`
出现在多个被跟踪文件）、machine / processor **取值空间极小可枚举** ——
**那么即便把历史（含 PR refs）清得干干净净，拿到仓库的人仍能推出「当前在用」的 Fernet 密钥。**
⇒ **清历史没有实质收益；真正该修的是设计**（用**高熵随机密钥**替代机器指纹作为密钥材料）。
⇒ 因此 **PR refs 的残留建议接受**，把力气转到 `auth.py` 的设计修正上（另议）。

**当前状态**：`HEAD = origin/main = a6b1174f`，工作区 `M .gitignore` / `D outputs/…/focused-before.xml`
（待提交）/ `M taiji/organs.py`（用户未提交改动，完好）；卫生守卫 5 passed。

## 第二轮全面体检（9 个维度，全部实测）

用户要求"再仔细查一次仓库"。方法：每个维度都用命令验证（不目测），结果如下。

| 维度 | 结果 |
|---|---|
| A 跟踪清单里的敏感/产物 | **2 处命中，但都是"有意白名单"**（见下）|
| B `.gitignore` 规则是否真的命中 | **8/8 全部 OK**（含 `**/security/.fernet_key`、`output/*-packaged-data/`、`report.xml`、`focused-before.xml`）|
| C 大文件（>200 KB）| 67 个、合计 49 MiB —— 抽样确认都是**有意资产**（`sp_general.vocab/.model`、`plans/manifests/*.json`、`reports/*.json` fixture）|
| D 未跟踪且未忽略 | **仅 1 个：本次体检脚本自身** |
| E 顶层目录近义/单复数（R9）| 47 个顶层目录，**唯一可疑对：`output` / `outputs`**（已知）|
| F 缓存/遥测是否被跟踪 | **全部 0**（`__pycache__`/`.pytest_cache`/`.mypy_cache`/`.ruff_cache`/`node_modules`/`.venv`）|
| G CI/配置里的秘密 | `ci.yml` **0 处**可疑赋值 |
| H 内容级扫描（3130 个文本文件）| hostname：**2 个文件**（都是本轮我写的记录文档）；username：**24 个文件**（memory/plans/reports JSON）|
| I ref 命名空间 | `codex`(1) / `heads`(1) / `remotes`(1)；**`refs/codex/*` 仍在本仓库** |

### ⚠️ 差点踩的坑：那两个 `.log` 不是问题

`.gitignore` 里有 **`*.log` + `!reports/_gate_*.log`** —— **显式白名单**，
说明 `reports/_gate_clip_repair.log`（1.5 KB）与 `reports/_gate_full_20260823.log`（40 KB，
JSON 化的完整门输出）是**有意入库的证据**。
若顺手 `git rm`，就正好犯了 R4 要防的错（为"干净"删掉有意保留的产物）。
**结论：不动。**

### 其余三项（登记，暂不动作）

1. **`output` / `outputs` 单复数并存** —— R9 的实例；合并会动到 23+4 个有意入库文件，需单独决策；
2. **`refs/codex/*` 仍含旧盐对象**（**仅本地，从未推送**）—— 它是 codex 工具的 checkpoint ref，
   删了会丢该工具的检查点；**不推即不泄漏**，故保留；
3. **用户名/hostname 出现在若干被跟踪文件** —— 因密钥已改为随机（不再依赖机器指纹），
   该暴露的影响已大幅下降；若要彻底处理需批量改写历史文档，不划算。

**方法教训（补充）**：`*.log` 这类"看起来一定该忽略"的扩展名，**也可能被显式白名单例外**——
排查前先读 `.gitignore` 全文与既有的 `!` 例外，再决定是否动文件。

