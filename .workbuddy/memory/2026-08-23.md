# 2026-08-23

## 全项目严格代码审计（四路并行深读 + 关键发现人工复核）
- 产出：`reports/project_audit_2026-08-23.md`，整体评分 4.6/10
- 最重要发现（均已复核属实）：
  - 评估泄漏：`eval_seed_corpus.py:154-163` holdout 取自训练语料同一文件前 32 行；verify 族脚本 train==test —— 94.12% 指标可信度受威胁
  - `neuroplex/brain/cortex.py:2601` 生成主循环未接入 KV cache，逐 token 全量重算
  - `api/app.py:124-129` JWT 中间件 fail-open；插件上传/代码执行端点未认证 RCE 面
  - 全仓 50+ 处 `torch.load(weights_only=False)`；neuroplex 自制 XOR 加密
- 模块评分：neuroplex 3.5 / api 4.5 / taiji+seed 6 / frontend+desktop 6.5
- P0 清单：修评估泄漏、认证 fail-closed、weights_only=True、废弃自制加密与 exec 沙箱、SPA 路径穿越

## 审计修复实施（P0 全部 + P1 架构项）— 完成
- 交付报告：`reports/project_audit_fixes_2026-08-23.md`；pytest 全量 115 passed / 0 failed
- 四路并行（api 安全 / neuroplex 安全 / 研究端指标 / desktop）+ 本人施工 layers.py/cortex/ensemble
- 关键决策：
  - cortex 生成主循环**不接 KV cache**——round 2+ field_state 加性注入 hidden 使缓存失效，架构不兼容；且 neuroplex 是冻结基线不能改数学。已记录为设计结论
  - KV cache 改 3 元组 `(k,v,abs_len)` 修 RoPE 驱逐后位置错乱；GQA 用 SDPA enable_gqa（两路径 diff=0.0）；RMSNorm 升 fp32
  - verify v7 heldout 探针 `b"abcabcabcabc"`，阈值 0.60（实测 0.692）；**94.12% 需按新口径重测后才能宣称**
  - `load_cross_spec_reference` 测试 flaky 根因是 WorkBuddy 沙箱 safe-delete 组件拦截 unlink（回收站不可用则抛 OSError），已改 tmp_path 修复
- 环境发现：E:\Seed 真实磁盘无 .git（此前 git 可用是沙箱虚拟化）；python 项目环境 = 系统 Python312（torch 2.13+cpu）
- verify 同族 N7/N8/N9/M5/M6/M7 未修，待单独评审

## P2 还债修复 — 完成
- 交付报告：`reports/project_audit_fixes_p2_2026-08-23.md`；pytest 全量 116 passed / 0 failed
- 安全债：168 处纯吞 except→logger.debug（62 文件）；前端 stream 禁重试+lang 转义+移除 style 白名单（node --check 通过）；tests/conftest.py+pyproject testpaths+docs/metrics_conventions.md+防腐测试 test_verify_metrics_contract.py；scripts/legacy 隔离区迁移 4 个硬编码诊断脚本
- 冻结基线（行为保持）：审计两条"大重构"经核查已在前序 P2-3 闭环——AppState 已细粒度锁、ResonanceEnsemble 已有 to/state_dict 委托
- Cortex 第一刀：抽离 _is_degenerate_text/_fuse_leader_quality 到 neuroplex/brain/_cortex_helpers.py，staticmethod 绑定委托，等价性断言通过
- 刻意未做：Cortex 3198 行完整拆分——需研究负责人签字+黄金向量等价测试，已建模式样板待分步推进
- 经验：本会话 E:\Seed 真实磁盘无 .git（沙箱虚拟化提供 git 可用幻象）；python 项目环境=系统 Python312(torch 2.13+cpu)；WorkBuddy 沙箱 safe-delete 组件会拦截 Path.unlink 致测试 teardown 偶发失败（用 tmp_path 规避）
