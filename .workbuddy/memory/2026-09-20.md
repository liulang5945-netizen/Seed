# 2026-09-20 工作日志

## R2 受控重训语言读出：步骤 0 成本标定已执行

**背景**：上一轮立下「重训语言读出」合同草案 v0（`c45509ae`），其 §4 指定**唯一可先行动作**——
一次几十 tick 的试标定，产出 wall/tick 与峰值内存，写进 §4.1 后才提 N 上限。本会话执行了它。

**先解决的问题：步骤 0 需要一条能跑起来的臂。** 读码确认当前 `Taiji.observe()` 已经暴露
`readout` / `learn_motor` / `learn_fabric` / `learn_predictive_context` / `learn_predictive_readout`
五个开关，两个写入站互斥：`:1986` 挂 `readout=="action"`（`motor.learn`），`:1939` 挂读出门控
（`predictive_readout.learn`）。⇒ 三条臂无需改产品代码即可表达。
⚠️ 一处坑：`readout` **不允许在同一个 dynamics episode 内切换**（`model.py:1752` 抛
"readout changed inside an active dynamics episode"）⇒ 每臂必须新建实例 + `reset_dynamics()`。
`Seed.observe()` 不转发这几个开关，故仪器走公开的 `Seed.architecture.observe(...)`。

**交付三件**：

| 件 | 内容 |
|---|---|
| `scripts/training/calibrate_taiji_r2_readout_retrain.py` | 只读标定仪器：硬上限 200 tick、报告已存在即拒跑、报告不得落 `checkpoints/`、跑完硬核基座 sha256 未变、**三臂写入面互斥不成立就 rc=1 且不落报告** |
| `tests/taiji_native/test_readout_retrain_step0_contract.py` | 14 项；含 A/B/C **双向**写入面钉住、"标定必须落在当前产品默认基座上"的时效守卫、四条拒跑守卫 |
| `reports/taiji_r2_readout_retrain_step0_calibration.json` | 实测报告 |

**实测**（CPU，30 计时 tick + 5 预热；基座 `seed_beta.pt` sha256 `ad2a0646…`，4,143,542 B，tick=16,000,000；
语料 `data/p3b_all_fresh.jsonl`，跳过量 **11,199,800**（血缘推导，取自其清单），窗口内**重放 0**）：

| 臂 | 写入面（实测） | s/tick | ticks/h |
|---|---|---|---|
| A 只训读出头 | 只动 `predictive_readout` | **0.0023630** | 1,523,513 |
| B 只训运动面 | 只动 `motor` | **0.0031833** | 1,130,889 |
| C 双冻结 | 两处都不动 | **0.0020120** | 1,789,273 |

进程峰值工作集 **276.152 MB**（`measured`）；一次启动开销 ≈0.49 s（跳语料 0.331 + 载入 0.005 + 建模型 0.151）。
**N 上限提案：16,000,000 符号/臂 ⇒ 合计 ≈33.6 h**（A 10.50 / B 14.15 / C 8.94，可断点续跑）。

**守卫红跑（证明它们能响，不是装饰）**：把 A 的预期写入面改错 ⇒ **rc=1 且报告未落盘**；
`--skip-symbols 0`（切片落进已见前缀）⇒ 拒跑（SystemExit 2）。绿跑 rc=0 且报告落盘。

**纪律执行**：`checkpoints/*.pt` 全程只读（报告里 `checkpoint_unchanged: true`）；报告落**新文件名**；
未读 sealed；未训练；未改任何冻结阈值与产品默认。回归：新测试 + 相邻三份合同测试 **36 passed**，ruff 零告警。

**新登记的一条前置**（写进合同 §4.1，不是步骤 0 的产物）：正式训练前必须先实测"能正确落盘 checkpoint
并在新进程恢复"（含 `--checkpoint-every` 节奏与恢复后的 `tick` 基线）；未过不开训。

**同步**：`plans/active/roadmap/03_CURRENT_EXECUTION.md` 队首加「第十二批」段（导航文档必须改）；
合同 §4.1 填实测、§7 第 2 件作废（步骤 0 已执行）并新增待批项；文档链接校验 110 条 0 缺失。
**MEMORY.md 已瘦身**：6575 → 2954 字符（六→五节，删掉已在归档案里的 D 系列过程细节，
只留仍有效的契约/纪律/判据与当前状态）。

**未做**：未申请两次批准（§5 数值线认可、§4 预算），未写训练 runner 的三臂正式实现，
未建未见题面集（§7 第 3 件待所有者裁决）。
