# 2026-09-20 工作日志

## R2 受控重训语言读出：步骤 0 成本标定已执行

**背景**：上一轮立下「重训语言读出」合同草案 v0（`c45509ae`），其 §4 指定**唯一可先行动作**——
一次几十 tick 的试标定，产出 wall/tick 与峰值内存，写进 §4.1 后才提 N 上限。本会话执行了它。

**先解决的问题：步骤 0 需要一条能跑起来的臂。** 读码确认当前 `Taiji.observe()` 已经暴露
`readout` / `learn_motor` / `learn_fabric` / `learn_predictive_context` / `learn_predictive_readout`
五个开关，两个写入站互斥：`:1986` 挂 `readout=="action"`（`motor.learn`），`:1939` 挂读出门控
（`predictive_readout.learn`）。⇒ 三条臂无需改产品代码即可表达。
⚠️ 一处坑：`readout` **不允许在同一个 dynamics episode 内切换**（`model.py:1752` 抛
"readout changed inside an active dynamics episode"）⇒ 每臂必须新建实例 + `reset_dynamics()`。
`Seed.observe()` 不转发这几个开关，故仪器走公开的 `Seed.architecture.observe(...)`。

**交付三件**：

| 件 | 内容 |
|---|---|
| `scripts/training/calibrate_taiji_r2_readout_retrain.py` | 只读标定仪器：硬上限 200 tick、报告已存在即拒跑、报告不得落 `checkpoints/`、跑完硬核基座 sha256 未变、**三臂写入面互斥不成立就 rc=1 且不落报告** |
| `tests/taiji_native/test_readout_retrain_step0_contract.py` | 14 项；含 A/B/C **双向**写入面钉住、"标定必须落在当前产品默认基座上"的时效守卫、四条拒跑守卫 |
| `reports/taiji_r2_readout_retrain_step0_calibration.json` | 实测报告 |

**实测**（CPU，30 计时 tick + 5 预热；基座 `seed_beta.pt` sha256 `ad2a0646…`，4,143,542 B，tick=16,000,000；
语料 `data/p3b_all_fresh.jsonl`，跳过量 **11,199,800**（血缘推导，取自其清单），窗口内**重放 0**）：

| 臂 | 写入面（实测） | s/tick | ticks/h |
|---|---|---|---|
| A 只训读出头 | 只动 `predictive_readout` | **0.0023630** | 1,523,513 |
| B 只训运动面 | 只动 `motor` | **0.0031833** | 1,130,889 |
| C 双冻结 | 两处都不动 | **0.0020120** | 1,789,273 |

进程峰值工作集 **276.152 MB**（`measured`）；一次启动开销 ≈0.49 s（跳语料 0.331 + 载入 0.005 + 建模型 0.151）。
**N 上限提案：16,000,000 符号/臂 ⇒ 合计 ≈33.6 h**（A 10.50 / B 14.15 / C 8.94，可断点续跑）。

**守卫红跑（证明它们能响，不是装饰）**：把 A 的预期写入面改错 ⇒ **rc=1 且报告未落盘**；
`--skip-symbols 0`（切片落进已见前缀）⇒ 拒跑（SystemExit 2）。绿跑 rc=0 且报告落盘。

**纪律执行**：`checkpoints/*.pt` 全程只读（报告里 `checkpoint_unchanged: true`）；报告落**新文件名**；
未读 sealed；未训练；未改任何冻结阈值与产品默认。回归：新测试 + 相邻三份合同测试 **36 passed**，ruff 零告警。

**新登记的一条前置**（写进合同 §4.1，不是步骤 0 的产物）：正式训练前必须先实测"能正确落盘 checkpoint
并在新进程恢复"（含 `--checkpoint-every` 节奏与恢复后的 `tick` 基线）；未过不开训。

**同步**：`plans/active/roadmap/03_CURRENT_EXECUTION.md` 队首加「第十二批」段（导航文档必须改）；
合同 §4.1 填实测、§7 第 2 件作废（步骤 0 已执行）并新增待批项；文档链接校验 110 条 0 缺失。
**MEMORY.md 已瘦身**：6575 → 2954 字符（六→五节，删掉已在归档案里的 D 系列过程细节，
只留仍有效的契约/纪律/判据与当前状态）。

**未做**：未申请两次批准（§5 数值线认可、§4 预算），未写训练 runner 的三臂正式实现，
未建未见题面集（§7 第 3 件待所有者裁决）。

## 所有者批准 → 正式 runner + 前置 + 三臂已启动

所有者对"批准 §5 数值线 + §4 预算（N ≤ 16M 符号/臂）"答**批准**，随即交付三件。

**① 正式 runner 与规格唯一来源**
- `scripts/training/readout_retrain_spec.py`（新）：`ARMS`（三条臂的 observe 开关 + 预期写入面）、
  `SURFACES`、`sha256_of`、`load_lineage_skip`、`iter_corpus_window`（**生成器**，16M 符号不能
  物化成 list）、`APPROVED_SYMBOL_CEILING = 16_000_000`。标定仪器与 runner 共用同一份，
  测试断言两者 `ARMS` 是**同一对象**（不是两份相等拷贝）。
- `scripts/training/train_taiji_r2_readout_retrain.py`（新）：拒绝路径全部落在代码里——
  超批准上限需显式 opt-in、`--out-dir` 不得落 `checkpoints/`、写靶不得是基底本身、
  切片落进已见前缀即拒跑；跑完硬核基底 sha256 未变；每臂核写入面（A=`predictive_readout`／
  B=`motor`／C=空），不符即 `failed` + 非零退出；断点续跑自动、跑满即拒重跑、`--fresh` 先归档旧报告。
- 用 `Seed.architecture.observe(...)`（公开访问器），**不改产品代码**。

**② 保存 / 新进程恢复前置：已过**（`reports/taiji_r2_readout_retrain_preflight_20260920.json`）
- 真子进程：600 → 新进程要 1000 时**只再跑 400**（不重放前缀），`absolute_tick=16,001,000`；
  第三次同预算**拒跑**（"already consumed 1000"）。基底 sha256 全程未变。
- 测试 `tests/taiji_native/test_readout_retrain_runner_contract.py`（9 项，含真 subprocess 的端到端）。
  连同步骤 0 的测试共 23 passed。

**③ 训练前封存（合同 §4.2 新增）**：本件**不新增题面集**。M1/M2 打在 `cap0_eval_set_v2.json`
（37,869 B，sha256 `310b6d64…`，自述 `frozen_before_any_candidate_score=true`、2026-09-15 冻结）上，
M1 复用**已 FROZEN** 的 P1/P2 成句判据（n 元对数似然门 −7.8 + 代码内拒跑门 + 打乱反向对照）。
⇒ §7 第 3 件（是否新增题面集）**不是本件前置**：那是评价集 v3，需另行批准，不批只是统计功效弱。

**④ 三臂正式训练已启动**：`--arms A,B,C --symbols 16000000 --checkpoint-every 500000
--progress-every 250000`，产物 `output/taiji_r2_readout_retrain/<arm>/`，断点续跑安全
（重跑同一命令即可：跑满的臂会拒重跑、未跑满的臂自动续）。

### 🔴 成本更正（本会话第二次因"没测就写成本"出错，如实记账）

步骤 0 的 30 tick 窗口落在 episode **最便宜**的一段，33.6 h **低估约 11%**。
正式 runner 稳态实测（24,000 符号窗口，速率在窗口间**平坦**、无随 episode 增长的漂移）：

| 臂 | 稳态 s/符号 | 16M 符号 |
|---|---|---|
| A | 0.0025904（步骤 0 报 0.0023630，+9.6%） | 11.5 h |
| B | 0.0035600 | 15.8 h |
| C | 0.0022450 | 10.0 h |
| | | **合计 ≈37.3 h** |

另有每臂收尾一次原子落盘（≈0.45 s）。**成本数字以合同 §4.2 ③ 为准，步骤 0 那张表降级为"先导测量"。**
教训：短窗口标定会给"最便宜的一段"，长跑的预算必须用**长窗口稳态速率**，并检查速率是否随
episode 增长漂移。

### 附带修与登记

- `progress.jsonl` 加 `session_id`：进度流是**跨进程追加**的，没有会话标识会把两条不同进程的
  记录混算成负速率（本会话实测踩到 `-35.951`）。
- `.gitignore` 按**命名约定**加 `output/taiji_r2_*/`，不再逐个实例名列举（上面那批
  `output/taiji_m5_k_*` 就是逐个列举的反面教材）。
- 提交：`7f545ccd`（runner + 前置 + 封存）。

## 判决器就位（训练跑完之前的空窗不空过）

训练要跑 ≈38.5 h，等它结束才动手做判决器就白等了。本轮把**判决侧**做掉。

**执行件** `scripts/training/eval_taiji_r2_readout_retrain.py`：M1（成句率 + 逐题配对符号检验）、
M2（`eval_taiji_cap0_baseline.run_baseline(checkpoint, dimensions=("D","E"))` 的机器计分之和）、
K2（反事实）。两条自我约束写进代码：

1. **判据一个字都不重写**——成句判据 / n 元模型 / 反向对照门 / 配对符号检验全部 `import` 自
   已 FROZEN 的 `diag_taiji_r2_surface_decode`，测试断言取到的是**同一函数对象**（不是相等的副本）。
   解码固定 `greedy`（P1 已实测四臂最好；本件问训练侧）。
2. **`--dry-run` 不产出判决**——`judgment` 整块被替换成 `suppressed`，且默认落盘路径与正式判决
   **分开**（否则一次自检就把"已存在即拒跑"的正式路径占掉）。

**管线自检（dry-run，不是判决）**：三臂共用同一基座 ⇒ 逐臂都是 0.2326（这是自检不是读数），
但顺带完成**交叉校验**：43 题、成句率 0.2326、反向对照（真中文 1.0 / 打乱 0.9567 / 中位
−6.8136 与 −8.7833 / 门 −7.8）与 FROZEN 的 P1/P2 读数**逐位一致** ⇒ 新判决器接的是同一把尺子。
每臂 43 题约 22 s。

**K2 的操作化预注册**（合同 §5.1，写于**任何一臂跑完之前**）：无差别 := (B−A) 成句率差 < +15 pp
**且** 配对符号检验 p ≥ 0.05。§5 只写 K2 的语义没写阈值；不补，执行后就会各取所需。
15 pp 复用 M1 同一个"可分辨最小步进"，不是新拍的数。

**守卫**：缺臂、未跑满预算、基底 sha 不一致、报告已存在 —— 四种输入一律拒跑（都会产出"看起来像结果"的
数字，所以必须在代码里挡）。`tests/taiji_native/test_readout_retrain_verdict_contract.py` 10 项把它们
逐个钉住，并对 M1 做红/绿双跑（1.0/0.0 的对局必须过；25 pp 但 p=0.0625 的对局必须不过）。
三份合同测试合计 **33 passed**。

**训练健康**：A 臂首个进度点 `consumed=250,000 / elapsed=669.45 s` ⇒ **0.002678 s/符号**
（稳态估计 0.002590，差 3.4%），`online_accuracy=0.3513`。三臂合计外推 **≈38.5 h**。

**下一步没有可执行动作**：等训练跑完，然后一条命令跑判决器。这不是"没事做"，
而是本件把两前置 + 判决侧都做完了、训练是唯一剩下的长动作。

（本日志追加于 2026-09-21 23:53 之后）
