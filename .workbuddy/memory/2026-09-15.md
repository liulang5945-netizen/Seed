# 2026-09-15 工作日志

## CI 修复收口 + 3.10 剩余项登记（承接 09-14 深夜）

**本轮动作**：把 `test (3.10)` 的 2 项**跨解释器数值敏感**失败登记进技术债登记册
**§4.2**（未修，含失败形态、已排除的两个假设、剩余怀疑与三种候选处置），
避免「回归主线后该 2 项悬空、下次 CI 红没人记得原因」。

**当时状态**（run `34860023522` @`292b66a5`）：

| job | 结论 |
|---|---|
| **test (3.12)** | **success** —— 28 步 + Post 全绿，含 `complete regression suite` **首次跑完** |
| **test-windows** | **success** —— 19 步全绿，**历史首次** |
| test (3.10) | failure —— 步骤 24 的 2 项（既有；跨解释器数值敏感；本机无 3.10 无法复现） |
| docker-build / Startup smoke ×2 / build-frontend | success |

**两条已排除的假设**（写进 §4.2 以免后人重复）：
① 顺序/哈希不确定（`train_only_candidates` 已 `sorted` + `combinations`，且脚本显式测两种顺序）；
② torch 版本差异（两腿均 `2.14.0+cpu`，CI 日志核对）。

**决策倾向**：质量线已达实质目标（3.12 + Windows 双绿 ⇒ 代码质量被完整覆盖），
3.10 的红属「跨解释器数值一致性」问题、与主线推进无关 ⇒ **推荐回归主线**，
把该 2 项留作独立议题（§4.2 已给出三种候选处置）。

**纪律**：登记册仍遵守「定性前不放宽断言」——§4.2 只登记与诊断，未改任何测试或阈值。

## 回归主线：上限优先修订 WP-1 建议 + WP-1.5 扩面起跑

**用户偏好（新，已存入用户级 auto-memory）**：「我都更倾向于上限更高的方案，而不是成本更低的」。
⇒ 推荐排序一律按**可声称结论的强度**，成本只影响执行顺序、不作否决理由；只读动作也取**大规模档位**。

**读到代码才发现的口径错误（重要）**：我上一轮答复里说「D1 选最强参照 ⇒ H2 永远不可达」是**错的**。
`audit_taiji_b0_m4_artifact.py:169` 的 `best_pair_gain` 用的是
`dictionary.policy_mean_gain_vs_all_singleton_oracle` ⇒ 实测 **`+2.000` 本来就是对最强参照**
（`reference_gain 1.5`、`required 1.65`）算的。F2 的「≤0 可证上界」只约束**冻结规则**（轨迹恒为单成员），
M4 因 `interleaved 4/4` 已逃出。三个 create 格在**三种参照下全部 `feasible: true`**、
`ceiling_gain 2.0`、`max_clearable_reference 1.85`。
**代价**：`+2.000 = ceiling_gain 2.0` ⇒ **零余量**，任一格退化就跌破 `required` ⇒ 扩面从「可选并行」升为「冻结前必过」。
教训：**推荐某个选项不可行之前，先确认那个数字是在哪个参照下算出来的**，别把「上界定理」跨规则外推。

**文档修订（5 个文件，未提交）**：
- `M5_B0_CLOSEOUT_AND_PLAN_REVISION_20260914.md`：D1/D2/D5/N2/N1a 建议改为上限档并附口径更正；
  WP-1.5 升到 6 context / 7 种子；**新增 WP-6 binder 议题**（N1a → N1a-2，挂 WP-3 之后、打 `binder_version` 标注、
  出口含「新格结果指纹必须与 create 行三格不同」）；依赖图与风险 3/5 同步；§5 改为「上限档 vs 低成本档」对照表。
- `03_CURRENT_EXECUTION.md`：D1 行口径更正、WP-1.5 上限档、**WP-6 行**、结尾加 2026-09-15 排序原则修订。
- `README.md`：执行顺序段 + 文档职责行 + **新增 N2 处置审查证据行**。
- `M5_B0_ROUTE_B_PREREGISTRATION_DRAFT_20260913.md`：**追加 §10.2**（口径更正 / 零余量 / 上限优先取值 / 仍未冻结），§10.1 原表不动。
- `06_P5_2C_PRIME_NEXT_STEP_DECISION.md`：追加 2026-09-15 补充段。

**顺手纠正的过时数字**：N2 处置审查（`M5_B0_N2_STOP_REASON_DISPOSITION_20260914.md`）实测**实时消费面 14 个文件**
（加固轮记录的 11 已过时，新增 N1 探针 + 两个契约测试；脚本排除自身避免自污染），其中**只有 5 个是真判断点**、
9 个仅记录/聚合；判断点语义只两类（副本一致性、`contract_intercepted:*` 前缀），新原因不带该前缀 ⇒ 不会被误计为安全拦截。
⇒ WP-2 的「消费表骨架」**其实已交付**，不再是待办；剩下的是 D5 之后的冻结版语义预注册 + 双向测试。

**WP-1.5 工具改造（不改默认行为）**：给 `probe_taiji_b0_structure_space.py` 加
`--contexts-per-cell` / `--seed-offsets` 两个旗标，并把 `contexts_per_cell` / `offsets` 以**关键字参数 + 常量默认值**
穿到 `probe()` / `validity()` / `measure_cell()` / `seed_sweep()`；payload 里的 `contexts_per_cell` 与
`seed_sweep.offsets` 改为**发布实参而非模块常量**。
理由：直接改常量会让归档的 20260913 报告**无法再从同一仪器逐字节复现**（F12 就被自己破坏了）。
新增 4 项合同测试钉住这点（默认值 == 归档常量、6 context 产生 6 个不同 id/文件名且结构不变、
有效性门在更宽规模下**不放松**、`--contexts-per-cell 0` 被 argparse 拒为 SystemExit(2)）⇒ **34 passed / 1.86 s**，ruff 全绿。
`INDEX_BASE + ordinal*10 + step` 的间距对 count ≤ 10 安全，6 context 不会撞 id。

**在跑**：`--contexts-per-cell 6 --seed-offsets 0 101 202 303 404 505 606`，
输出 `reports/taiji_b0_structure_space_probe_wide_20260915.json`，连跑两次（第二次写 TEMP）做字节相同校验。

## 取值定案（全上限档）+「决策 ≠ 执行许可」

**用户答复**：「全部按照上限档来，把计划修订好，**先不开始实际推进**」。
⇒ 两件事必须分开写进文档：**WP-1 决策窗口关闭**（D1–D5 / N2 / N1a / N1a-2 全部定案），
但**执行未获授权**（冻结版预注册、WP-2、WP-3 改 runner、WP-4 训练、WP-6 改 binder 一律待另行许可）。
教训：**"决策已定"很容易被下游文档读成"可以开工"**，所以每处提到定案的地方都要同时写授权边界，
包括 01 号的能力账本（"仍不能承诺几轮训练或某日期晋级"）和 03 号的训练门
（"D1–D5 已明确 **不等于** B1 可以启动训练"）。

**改了 8 个文档**（150 条相对链接、0 断链；archive 里 199 条历史断链与本轮无关，未动）：
- `M5_B0_CLOSEOUT_AND_PLAN_REVISION_20260914.md`：标题改 WP-1…WP-6；头部加 2026-09-15 状态；
  §2 新增**「WP-1 取值已确认」表**（8 行，每行含"冻结版必须据此写死的条款"）；WP-1.5 升为必过门并记录旗标改造；
  §3 依赖图改成带**许可门**的形状；§5 从"要不要决策"改为**执行队列表**（序 0–5 + 并行，逐行标"许可状态"）。
- `03_CURRENT_EXECUTION.md`：更新日期行、"仍未实施"行、当前唯一下一步标题（改为"写冻结版——待许可"）、
  D1 行口径更正、D 表后加**一行定案摘要**、WP-1 行、WP-1.5 行、**新增 WP-6 行**、训练门那句、结尾授权边界。
- `01_SCOPE_AND_PHASES.md`：§5 的"WP-1.5 为可选"改为"冻结前必过门"，**并按同节下一条的要求写明理由**
  （零余量 ⇒ 原协议规模无法区分"真稳健"与"样本恰好贴边"）；能力账本那句改为"已定案但未授权 ⇒ 仍不能承诺日期"。
- `README.md`：唯一推荐下一步段、执行顺序段（加授权边界）、**新增 N2 处置审查证据行**、文档职责行。
- `M5_B0_ROUTE_B_PREREGISTRATION_DRAFT_20260913.md`：§10.2 第 4 条改为"取值已确认、本文仍未冻结"。
- `M5_B0_N2_STOP_REASON_DISPOSITION_20260914.md`：§6 唯一下一步划掉旧句，记定案 + 未授权，并指明本文 §2/§5 即冻结版底稿。
- `M5_B0_STRUCTURE_SPACE_RESULT_20260913.md`：§9 后**追加前向指针**（原文 1/2/3 条不改）。
- `06_P5_2C_PRIME_NEXT_STEP_DECISION.md`：追加"2026-09-15 定案"段。

**两条纪律自查**：
① **新增必达门必须给理由**——01 号 §5 自己写着"冻结后新增门必须说明原协议无法识别的风险，不悄悄移动终点"，
所以把 WP-1.5 从可选升为必过门时，必须在同一处写出"原协议识别不了什么"，否则就是我自己违反自己登记的规则。
② **历史文档只追加前向指针**——结果文档 §9 的三条旧"下一步"不改字，只在后面加引用块说明已定案；
草案 §10.1 原表不动，新事实进 §10.2。

## WP-3 动手前：先落地 WP-1/WP-2，再查出两件要命的事

**提交**：`f23c9953`（探针旗标 + 4 测试 + 扩面报告）、`97aff6ed`（两份冻结版 + N2 守卫 + 计划同步）。均未 push。

### 一、消费面计数是错的，而且是被扫描器自己抓出来的

我之前往 4 个文档里写的「14 文件 / 5 个真判断点」**不对**。链条是：
新建的 N2 语义测试**本身**使用了 `all_members_blocked` ⇒ 重扫时它成为**第 15 个消费者** ⇒
扫描器把 `drift.added` 填上并**退出码 1**（"re-disposition before landing"）——**它拒绝让清单悄悄长大**。
按它自己的规则给新文件补处置（登记为 **J8**、同步 `EXPECTED_CONSUMERS`）后重扫：
**15 文件 / 8 处判断点（4 live gate + 4 测试断言，分布在 7 个文件）/ 8 文件仅记录**。
旧审查文标为「非权威计数」，**不回改正文**。
⇒ 教训：**新增任何读取 `stop_reason` 的文件都会改变被审查的面**；改完就重扫，别相信上一轮的清单。
顺带：`test_b0_n2_stop_reason_disposition_contract.py` 里硬编码的 14 / 7 / `draft_for_review` 三处断言
**必须显式改**（这次是真变化，不是把测试改绿）——旧守卫就是为这一刻存在的。

### 二、我把不变式 I5 写强了，被自己的双向测试否证

冻结版初稿写「`contract_intercepted:*` 两规则计数相同」。测试跑出真实差异：
`create__observation` 冻结侧有 **72 次** `language_assessment_unavailable`，M4 侧 **0**。
查清后才明白：**那正是让位修复的东西**——首成员独占时永远在不存在的文件上评估语言 ⇒ 被合同拦截；
让位后有人先建文件 ⇒ 评估可过。所以正确的不变式必须**逐键**分两类：
- **未被让位触及的**（`preview_ValueError`）两规则**恒相同**（`36 = 36` 逐格）⇒ 它若变了才说明增益对比被污染；
- **被修复的**（`language_assessment_unavailable`）**必须归零**，且只能在被修的格归零 ⇒ 这是机制证据。
⇒ 教训：**"两规则某计数相同"这种全称断言要按子键写**。我上一轮还把它当作 F11 的亮点抄进了几个文档。

### 三、WP-3 的范围核实（动手前查树，值回票价）

`_member_episode` / `bindable` / `stop_reason` **只在 `scripts/training/eval_taiji_p5_2b_group_causal_corpora_gate.py` 里**，
`taiji/` 运行时**没有对应实现**。⇒「落地 M4」= 把被研究的组合规则正式写进**测量 gate**，
证明的是"这条规则改成失败即让位后，交错与增益在仪器里可复现"，
**不**证明"Taiji 产品机制已支持协作"；01 号协作轴的「机制正式版本」**继续未闭合**。已写进 03 的 WP-3 行与推进计划修订。
⇒ 教训：**"落地"要先落到具体文件上**。同一个词在测量线和产品线上不是一回事，写清否则将来必被误读成能力晋级。

### 四、下一个卡点（已想好方案，别偷偷绕过）

`chosen = bindable[0]` 是反事实的**唯一锚点**（`build_counterfactual` 要求 occurrences==1，否则 SystemExit）。
M4 落地后这行消失 ⇒ `m1a` / `m1b` / `m3` / `m4` 四个变体一起硬失败。
但计划要的出口①是「反事实**退化为恒等**」，不是「反事实报错」。
⇒ 方案：给 `build_counterfactual` 加一条 **`rule_revision` 感知的"已应用 ⇒ 恒等"路径**——
锚点缺失 **且** 替换文本已在位 ⇒ 记 `already_applied` 并让该变体成为恒等；两者缺一仍照旧失败。
这是在**改测量仪器**，必须写在落地说明里，不能当作"顺手修个 bug"。

### 五、待办（顺序不能换）

1. 等 pre-WP-3 全量基线跑完（当前 ~5%，用于出口④）——**期间绝不改 Python 文件**，怕惰性导入拿到混合状态。
2. 改 gate：两处替换用**与反事实逐字节相同**的文本；加 `RULE_REVISION`；冻结成员优先级定义（风险 1）。
3. 反事实加恒等路径；跑五件探针 + 出口①②③⑤；出口④比对基线失败集合无新增。
4. 历史报告只加"规则版本"指针，**不改任何数字**（I8/T-h 落地）。
5. `test_b0_m1_counterfactual_contract.py` 与 `test_b0_handoff_probe_contract.py` 里断言锚点存在的测试须随双版本迁移（J4 同类，不得静默改绿）。

## WP-1.5 扩面结果（上限档，已跑完）

**规模**：每格 **6 context**（66 总）/ **7 种子偏移** `0 101 202 303 404 505 606`。
**实测机时**：两轮连跑 01:05:09 → 01:29:11 = **24 分钟**（单轮约 **12 分钟**），比计划里 20–30 分钟的外推快。

| 项 | 扩面前（2 ctx / 3 seed） | 扩面后（6 ctx / 7 seed） |
|---|---|---|
| 三个 create 格增益 | 各 `+2.000` | 各 `+2.000`（不变） |
| 交错 | `0 → 2` | **`0 → 6`**（6 个 context 全部真实交错，无一退化为回退链） |
| 为正的种子偏移 | 3/3 | **7/7** |
| `unexplained` / `regress` / 预测矛盾 | 空 | 空 |
| `measurable` 格数 | 11/11 | **11/11**（没有靠放宽有效性门换样本） |
| `patch` 行 | 两规则同 `−2.000` | 同（未把不可达变可达） |
| 结果指纹 | 3 / 11 cells | **3 / 11 cells（不变）** |
| 两跑字节相同 | 是 | 是（sha256 `b09b3e62…487b64`） |

**四条出口全过**，第 4 条（某偏移转负则发布界限收缩）**未触发**。

**核到一个比预期更有用的数字**：逃生通道的 `required_combination_only_contexts`
从 **2/2 变成 5/6** ⇒ **k 维度首次有余量**（扩面前一个 context 都不能少）；9 个「格 × 参照」组合全 `feasible: true`。
而 `ceiling_gain` **与 context 数无关**，扩面前后都是 `2.0`，实测增益恰好 `+2.000` ⇒ **增益维度仍零余量**，
但这是**判据的形状**（把可清的组合必需 context 全清 ⇒ 增益就是 2.0），不是采样偶然。
教训：**规模变了就要重核派生量**——我差点把"零余量"整句照抄进新文档，实际上 k 维度已经有余量了，
只有增益维度还贴着上限。凡是随 n 变化的量（`required` / `available` / `feasible`）都必须从新报告里重读，不能沿用旧值。

**闭合了什么 / 没闭合什么**：
- **闭合 L4（样本规模）与 N1b/N1c**；解除 D1 取最强参照的"贴边"担忧。
- **没有闭合 L1（结构独立性）**：指纹数不变 ⇒ 加 context 只是给**同一个结构**加样本，造不出新的结构因素。
  **独立结构因素仍是 1**，要抬到 >1 只能走 **WP-6（改 binder）**。这一条必须写进结果文档，
  否则"扩面后依然全绿"很容易被读成"结构稳健性也证到了"。
- **没有触碰 L2/L3**：可表达性与成员间证据通道是 binder / 签名的性质，与样本量无关。

**落档**：新报告 `reports/taiji_b0_structure_space_probe_wide_20260915.json`（未覆盖 20260913 版）；
结果写入 `M5_B0_STRUCTURE_SPACE_RESULT_20260913.md` **新增 §10**（原文 §1–§9 不改）；
账本加 **F13**、L4 标注已闭合、风险 3 改为已闭合（残留"增益维度零余量"）、风险 2 改为"审查面已交付"；
README 加扩面证据行；03 号加账本行并把 WP-1.5 标为已完成。
8 个文档 **162 条链接、0 断链**。TEMP 里的第二跑副本核对哈希后已删。
**未提交、未启动任何后续包**（用户："先不开始实际推进"）。

## WP-3 落地 HANDOFF-M4（已解除限制后）

**基线复采（改前）**：`1408 passed / 0 failed / 6 skipped / 1063 s`，退出码 0。
⇒ 台账里挂着的 **27 项 `SystemExit: 1` 未复现**。这不是本轮修好的：§4 早已归因为
**本地 WorkBuddy 沙箱批量删除守卫伪影**（单 tool call 删 ≥50 路径即 SystemExit(1)），本轮没有批量删除；
而 09-14 晚分离出的 5 项真实回归已在 CI 轮以 68 处补丁修掉。**所以出口④的基线从"27 项"改为"1408/0/6"**，
且我明确写了"本轮未修改任何测试或阈值让它变绿"，避免把这当成战绩。

**gate 改动（`eval_taiji_p5_2b_group_causal_corpora_gate.py`）**：
1. 两处替换**逐字节照抄**反事实文本（`M4_SELECTION` / `M2_CUE`），不是"重写等价逻辑"；
2. 新增 `RULE_REVISION = 1` / `COMPOSITION_RULE` / 两份规则描述常量；
3. `DEFAULT_REPORT` 改为 revision-1 新文件名，旧 20260913 报告只作 `REVISION_0_REPORT` 引用；
4. 报告 payload 的 `design` 加 `rule_revision` / `composition_rule`，并把 revision-0 描述作为**带标签的历史字段**保留。

**出口①已证**：`m4_selection_in_source=true`、`m2_cue_in_source=true`、两个锚点均消失、
`m4_variant_is_identity=true`、`m4_already_applied=2`、`m4_added_lines=0`、`frozen_attribute_intact=true`、
gate 自报 `RULE_REVISION=1`。**额外**：`m2_per_member_progress` 也成恒等（M4 第二处本就是 m2）；
`m1a/m1b/m2a/m3` 四变体**拒绝构造** ⇒ 加固扫描是 revision-0 仪器，不重跑。

**落地造成的计划自相矛盾（我自己制造、自己发现）**：WP-4 入场写"五件探针全绿（含加固扫描）"，
落地后加固扫描按设计**永远拒绝运行** ⇒ 那句条件字面**永不可满足**。已在 03 / README / 推进计划三处改为
"各在其所属 `rule_revision` 下全绿；revision-0 仪器不重跑"。**教训**：改变规则时要顺带检查**所有引用它的入场条件**，
否则给后人留一个看起来像"证据不足"的死锁。

**本轮我犯的四个错（都当场被抓并修）**：
1. 把历史代码引用 `chosen = bindable[0]` 写进 `_member_episode` **自己的 docstring**，
   使"函数体内只有一条规则在位"的判据被注释触发失败 ⇒ 历史说明应留在模块层。
2. "删除冗余变量"的编辑只删了赋值、留下引用 ⇒ ruff F821。删引用必须同一次做完。
3. 用 `def` 行作插入锚点时漏掉其后一行，把 `sweep = payload["seed_sweep"]` 删掉 ⇒ 制造了未定义名。
4. 在登记册里写了"已在 03 与推进计划同步"，而当时**还没同步** ⇒ 先承诺后兑现；改完才那样写。
   另外一次我把 `tail` 输出的尾部圆点误读成"95%"（其实 71%），据此说了过高的进度判断。

**验证状态**：b0 测试组 **144 passed / 0 failed**，ruff 全绿；结构空间 revision-1 探针在跑（出口②③），
之后是全量套件复采（出口④）与 7 份证据文件哈希复验（出口⑤）。

---

## 同日晚些时候（WP-3 后半程）：一次"看起来通过"的无效测量

**出口②被我自己记错过一次，且错在仪器语义而不是代码 bug。** 落地后满档重跑结构空间探针，得到
三格 `frozen +2.000 / m4 +2.000 / delta +0.000`、`regresses: none`，我据此写了"出口②已过"。
真相：探针的 baseline 臂取 `frozen._member_episode`——M4 落地后**它就是 M4**，而 audited 臂因锚点已落地被判恒等
⇒ **两臂同一份源码**，`delta ≡ 0` 与"无回归"都是同义反复。该次产物改名留档为
`taiji_b0_structure_space_probe_m4landed_degenerate_20260915.json`（物证，不删）。

**修法（同一批改动内）**：`build_reverted()` 沿同一 `(anchor, replacement)` 对**反向**还原 revision-0 基线臂
（部分落地=既非 0 也非 1 ⇒ `SystemExit`）；探针按 gate 自报的 `RULE_REVISION` 选臂；
**两臂是同一函数即 fail-closed**；报告新增 `arm_provenance`，控制台首行打印臂来源。
有效重跑结果：三格 `0.000 → +2.000`、`delta +2.000`、交错 `0→6`、7/7 偏移为正、`patch` 行仍 `−2.000`、
指纹仍 3/11，且与封存扩面报告 `rows/validity/verdict/outcome_distinctness/seed_sweep` **逐字段相同**（已由测试钉住）。

**两台仪器不是一回事，我把它们混为一谈过**：反事实探针的 `regression_check` 比的是**已封存的冻结矩阵数字**
（`_frozen_rates()` 读旧报告），与被测臂无关 ⇒ 落地后**仍可重跑**并直接给出口②：
`regressions=0 / improvements=2`、候选面 `interleaved=4`、`+2.000` 两序一致。
只有**加固扫描**（逐一构造六变体）才真的拒绝运行，是纯 revision-0 仪器。
附带修掉一个自己造的坑：`evaluate()` 原先在第一个被拒变体上就整体 `SystemExit`，改为逐变体记 `refused` 并继续。

**覆写风险只修了一处等于没修**：gate 的报告路径我早些时候版本化了，但**另外六支** B0 仪器的
`DEFAULT_OUTPUT` 仍指向**被 sha256 封存**的 revision-0 文件名，而写入是原地 replace ⇒ 忘记 `--output` 一次就毁掉本轮基线。
已全部改为 `REVISION_0_OUTPUT`（只读）+ 新默认名，并加一条全仓扫描测试（含跨行赋值）。

**清单第四次长大**：出口②的复现守卫要比对 `stop_reasons` 字典 ⇒ 它自己成为第 17 个消费者（J10），
扫描器与套件双双把它抓出来（"Extra items in the left set"）。现值 **17 文件 / 10 处判断点**，文档已同步。

**本轮我犯的第五个错**：用"把新行接在旧行后面"的思路做 Edit，实际是**整块替换**，两次弄丢 J9 消费行与 F14 账本行
（都在下一步立即发现并补回）。**规矩**：插入型编辑的 `old_string` 必须是"将被完整重现的前缀 + 新内容"，
改完必须回读被锚点覆盖的那一行还在不在。

---

## 本轮收尾（WP-3 结项）

出口④复采完成：**1428 passed / 0 failed / 6 skipped（1065 s）**，对基线 1408 / 0 / 6 **失败集合无新增**；
用两份 junit XML 逐名比对，确认"消失的 5 个测试名"全是本轮有意重命名，并在落地结果 §4 逐个列出替代者。
另补做**gate 端到端实测**（此前无任何测试跑过 `main()`）：默认路径直跑 24.8 s、九门无失败、报告
`rule_revision=1`，且封存的 20260913 报告哈希不变 ⇒ 覆写防护被真实运行验证。
本地提交两笔：`d0d466af`（WP-3 全部改动，33 文件）、`101da55a`（降封条口径说明）。**未 push**，`.workbuddy` 未入库。

**新认识（降封条必须知道）**：仓库 `core.autocrlf=true` ⇒ **封条 sha256 是工作区文件字节**，
`git show HEAD:reports/…` 拿到的是 LF 规范化 blob，两者必然不同（同一文件 `b09b3e62…` vs `f527b78a…`）。
我差点把 blob 哈希不一致当成"封存证据被改"——复核只能读工作区文件。

**本轮我犯的第六个错**：用 `printf "..." > .git/COMMIT_MSG_TXT` 写提交说明，正文里的反引号在双引号内被
当作**命令替换**执行（报 `fatal: path 'reports/…' does not exist in 'HEAD'`），整段文字被空输出吃掉，
留下一句缺主语的提交说明。**规矩**：含反引号或 `$()` 的中文 markdown 一律用 Write 落成文件再 `git commit -F`；
已提交且未 push 也不擅自 amend，改为让文档承载正确表述并把缺陷记在这里。

## 本轮（接续）：B2 选择门负结果 + CAP-0 清点

### B2（WP-5a）已执行 = 负结果
- 748 episodes 真实合同执行，wall 31.2s；稳定字段两跑哈希相同（8c78ef23...）
- G1/G2/G3/G6 过；**G4/G5 不过** ⇒ outcome=failed（no_task_benefit）
- 选择器选 member-b+member-c（预测 0.141802），而**唯一真正增益的是 member-a+member-c（+2.000，预测仅 0.083829）**
- 所选 pair 逐格增益 0.0、自身交错 0、lesion 不成立；四单体全 −1.0（对照并列垫底，非对照取胜）
- 逐格判据与冻结表一致：n=6、required k=5、available k=6、ceiling 2.0、max_clearable 1.85
- **预注册缺口**：冻结三态表无 (G4✗,G5✗)+机械门绿 的标签 ⇒ 记 failed+detail 并写 preregistration_gap，不新造第五态
- 教训：报告第一版把 required k 算在**池化**表（n=18 → k=15），而冻结表是**逐格** n=6 → k=5；已修
- 教训：interleaved 初版对**所有 pair** 求和（=18），而 G5 读的是**所选 pair 自己**（=0）；已分开列并加 note

### CAP-0 清点（07 §5.1：主线结项点先做 CAP-0）
- 默认入口服务 seed_corpus.pt，metadata.tick=**2**，trainer=api_seed_runtime ⇒ 刚初始化基底，非训练态
- 记录中最训练过的是 seed_beta.pt = **16,000,000 ticks**，但**加载即失败**：
  ValueError: enabled identity organ checkpoint payload is missing
- 诊断探针 7 轮全部回答、0 报错、1015 字节，但**收敛到唯一 1 个模板签名**：
  「我已收到你的问题："<提问>"。当前原生语言表层正在形成稳定表达。」
  ⇒ 07 §3.B「固定话术不计内容能力」
- A 维度已测；B/C/D/E/G/H/I 一律 untested（不记 0 不记通过）；F=partial

### 搁浅缺陷精确定位（可修，但属安全语义决策）
- 三个搁浅文件是 **taiji-native-v8**；taiji/model.py 的 LEGACY_CHECKPOINT_FORMATS **明确支持** v8/v9
- is_legacy_checkpoint 已在 **9 处**使用（2611/2623/2637/2652/2669/2682/2708/2790/2856）
- 但**身份器官分支 2726-2732 漏了同一个守卫**：对任何缺载荷的检查点都拒绝，含身份器官尚不存在的 v8
- 2611 行是正确范式：`if X is None and not is_legacy_checkpoint: raise` + M2-2h 迁移说明
- 选项：(a) 加 legacy 守卫（推荐，有先例）／(b) 重导出（已证"改信封 config"不可行：
  第二道守卫 `checkpoint configuration does not match architecture`，model.py:2604-2606）／(c) 明示降级

### 环境踩坑（重要，已踩过一次）
- **模型格式在 substrate 键下**，不是 taiji 键！taiji 是 Seed 适配层，带另一条版本轴：
  seed_corpus.pt 的 taiji.format=taiji-native-v1 而 substrate.format=taiji-native-v10。
  读错键会把格式报错 ⇒ 已加 has_taiji_adapter_block 字段与契约断言钉住
- 沙箱删除守卫会调 reg.exe（黑名单）⇒ 清理临时文件要用**截断**而非 os.remove
- bash 里给 python -c 传含反引号的字符串会被 shell 解释 ⇒ 长文本改用 Write 落文件再读

### 提交
fde682ef B2 选择门 / 50b00be7 CAP-0 清点 / 6ffe4496 CAP-0 搁浅缺陷诊断
校验：ruff 全仓 All checks passed；B2 契约 10 passed；CAP-0 契约 10 passed；计划身份测试 5 passed

## CAP-0 反事实收尾 + 评价集冻结（本轮）

**先收尾**：`5c4f68dd` 提交 14:22–14:37 的 CAP-0 legacy-load 反事实交付物
（协议 + 探针 + 契约测试 + 报告），校验 CAP-0 契约 16 passed、ruff 全绿。**未 push**（授权边界）。

**再推进 03 的「当前唯一下一步」第 2 项：冻结 CAP 评价集**（07 §4.1）。

- 冻结时点成立：CAP-0 只有 3 轮**无评分**诊断探针 ⇒ 不存在任何维度成绩（满足「看到成绩前冻结」）。
- 交付：`plans/reference/M5_CAP0_EVAL_SET_FROZEN_20260915.md`（协议）+
  `plans/manifests/cap0_eval_set_v1.json`（机读）+ `tests/taiji_native/test_cap0_eval_set_contract.py`。
- 规模：**B/C/D/E/G 各 20 项 = 100 项任务实例** + A/H 各 6 项确定性检查 + F 4 项合同引用。
  每项带族、turns、评分方式与期望；C 类含 **5 项未知题**（必须拒答/表不确定，不得编造）；
  G 类 20 项含 **11 项 hard_safety**（越权 6 + 诱导伪造 5，必须拒答）。
- F 维度如实标注现状：F02（B2 选择门）**负结果、该能力未达**；F03 结构空间须同时声明
  「独立结构因素仍为 1」；F04 整模型加载链记**缺口**（默认 tick=2、16M-tick 被守卫挡住）。
- 三条纪律写进协议并加断言：项数/维度/量表变更须**同步改契约测试**（不得静默改绿）；
  测试时不训练、现场提问不回填；未测维度记 `untested`（不记 0 也不记通过）。

**两次我自己的失误（当场修正）**：
1. 契约测试初版把 id 假设成单数字（`B1`），实际 JSON 是两位（`B01`）⇒ 报错后**统一 JSON 为两位**
   （含 A/F/H 共 16 处重命名），而不是把断言放宽；
2. 同一版本按 `dim["items"]` 取项，而 `I` 维度按设计**不带 items** ⇒ 改为 `.get("items", ())`。
   两处都是**修正测试对冻结产物的错误假设**，没有放松任何门槛。

**验证**：契约测试 **13 passed**；ruff 全仓 All checks passed；black 该文件 unchanged。

**下一步**：实现 CAP-0 评价 runner（按冻结 JSON 驱动真实模型、保存原始回答、按量表计分/待人工复核），
跑首次计分 B～H 基线。**仍未授权**：binder 改动（WP-6）、产品采用/默认行为切换、push。

### 首次整模型能力基线 + 两项交接债（同日续）

**runner**：`scripts/training/eval_taiji_cap0_baseline.py` —— 子进程驱动、每项独立会话、
`learn=False`、题面 `__RESET__` 触发会话重置；保存原始回答；C/D/E 规则计分；
B/G 一律待人工复核；A/F/H 记 `not_executed`（不记 0 也不记通过）。

**一次被自己抓住的假阳性（重要）**：首版直接用 `expected_contains` 匹配回答，得
C 4/14、D 1/16、E 9/20。逐条回看发现 **14 个「正确」全是回显假阳性** —— 该入口回答是固定模板
`我已收到你的问题："<PROMPT>"。当前原生语言表层正在形成稳定表达。`，把提问**逐字抄回**，
而期望词（`5`/`苹果`/`A`/`冷`/`3`/`小明`/`a,b,c`/`是`…）恰好就在提问里 ⇒ 命中的是回显而非答案。
**修法**：判分前删除逐字回显的提问，并保留 `raw_last_output` / `verdict_text` /
`prompt_echo_stripped` 三个字段供复核。**受污染首版报告留档为物证，不删。**

**修正后基线**：C **0/14**、D **0/16**、E **0/20**（均 0.00）⇒ 远低于 C/E ≥70%、D ≥80%
⇒ 语言缺口**不是边际不足，而是完全缺失**（与 legacy-load 反事实的「同模板」结论一致）。

**两项交接债（已登记技术债册 §4.3，未处理）**：

1. **7 个文件的 black 格式债与 WP-3 锚点契约冲突**：直接格式化这 7 个文件会破坏
   **8 项契约测试**（black 重排了锚点所在代码行，而锚点/规则文本要求**逐字节匹配**）⇒ 已回滚。
   处置选项：(a) 锚点区用 `# fmt: off` / `# fmt: on` 包裹、其余照常格式化（推荐）；
   (b) 锚点判定改为不依赖格式；(c) `extend-exclude` 豁免（放宽门禁，需明确认可）。
   **处置之前，CI 的 `Format check with black` 步骤会红。**
2. **N2 消费面清单漂移**：WP-4 预检脚本只用了一个**字段名**
   （`"terminal_stop_reason_marker"`）⇒ 属**扫描器误报**。登记进 `EXPECTED_CONSUMERS` 并重新
   生成报告会牵出 `test_historical_inventory_is_not_rewritten`（**归档报告不得改写**）与
   `test_the_frozen_preregistration_states_the_measured_counts` 两项新失败（已实测并回滚）
   ⇒ 正确处置是**改扫描器排除规则**，须与 N2 作者对齐后再动。

**提交**：`95a4670f`（评价集冻结 v1）、`01b37be8`（基线 runner + 报告 + §4.3 登记）。**未 push**。

