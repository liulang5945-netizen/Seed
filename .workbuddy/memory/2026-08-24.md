# 2026-08-24 工作日志

## plans/ 目录分类整理（已完成）
- 按内容主题归置 26 份 .md：新建 `archive/architecture_design/`（早期/废止架构设计，4 份）与 `archive/authored/`（Legacy 底稿/事实基线/对比，4 份）。
- 移动：`archive/architecture/TAIJI_TRANSFORMER_SHELL_AUDIT_20260822.md` → `archive/audits/`；`active/` 下 4 份 Legacy 底稿（NEUROPLEX_MECHANISM_RUNTIME_MAP、BOOTSTRAP_CRITERIA、DESIGN_PRINCIPLES、TAIJI_VS_HUMAN_BRAIN_COMPARISON）→ `archive/authored/`。
- 重命名：`2026-07-27-side-channels-implementation.md` → `SIDE_CHANNELS_IMPLEMENTATION.md`；`H1-H8-mechanism-fixes.md` → `H1_H8_MECHANISM_FIXES.md`。原空 `archive/architecture/` 已删除。
- 同步更新 README 及 BIO_INSPIRED、COMPREHENSIVE_NEURON 内的相对链接，grep 校验零断链。
- 命名规范：目录小写+下划线；文件 UPPER_SNAKE，保留语义日期后缀。
- 方案草稿 `REORGANIZATION_PLAN.md` 移至 `.workbuddy/artifacts/`，保持 plans/ 仅含项目文档。

## 前端/桌面端 UI 优化（已完成）
- 侧边栏：去掉 `.sidebar` 的 width 过渡（app.css），拖拽改宽无滑动拖尾。
- 启动动画：desktop/main.py 删除 loading.html 流程（含一处重复 QTimer bug），直接加载前端；后端 start() 本就阻塞至就绪。
- 托盘图标：改用应用 **logo**（用户澄清原意是「改为 logo 样式」，非「非 logo」）。main.py 用 `frontend/public/favicon.ico`（与窗口图标一致）；run_app.py 用打包 `icon.ico`（app_icon）。
- 状态栏：WorkspaceView.vue 各状态项补 lucide 图标 + `.status-item` 对齐样式。
- 无边框窗口：main.py 与 run_app.py 设 FramelessWindowHint + 自绘 36px 标题栏（拖拽/双击最大化/最小化/关闭），标题栏配色随前端 data-theme 同步；新增 _EdgeResizeFilter 保留边缘缩放。
- 验证：两 Python 入口 py_compile 通过；前端 vite build 成功。
- 已知限制：无边框失去 Windows Aero Snap/原生阴影；主题同步为加载后一次性读取。
- 详见 `.workbuddy/artifacts/ui_polish_summary.md`。
