# R2 阶段一合同草案 v0：受控重训语言读出（双臂 + 反事实对照）

状态：**草案，未冻结，不授权执行**。所有者 2026-09-20 弹窗选择"立『重训语言读出』新合同并申请预算"（上限最高项）。
本件需要两次独立批准才能动：**① §5 数值线认可 → 升 FROZEN；② §4 预算批准**。二者齐备前不跑一步训练。

## §1 为什么是这件事（前置事实，均已核实）

1. **P0-a′**：现行产品默认基座 `seed_beta.pt`（16M tick）的语言读出**从未独立训练过**——训练期修订
   `ac37c890` 的 `observe()` 只有 `motor.learn()` 一条写站，全文件无 `predictive_readout.learn()`；
   读出面是 v8 迁移时**从运动面权重重建的副本**（`load_legacy_motor_payload`）。
2. **P1/P2 否证了解释一**：解码参数不能补救——贪心成句率 0.2326 已是四臂最好，
   top-k/temperature 均 0.0 且显著更差（p=0.00195）。⇒ "换个解码设置就会说话"这条路已用数据关掉。
3. **D/E 脱离 0 但仍远未达线**（1/16、3/20），且表层形态**依 checkpoint 而定**（旧默认是模板回声）。
4. 历史负结果约束本件：内容绑定 v1–v6 全部判停，v6 结论是"**同架构放大已无依据**"；
   字节图族五条变体线已按预承诺关闭；字级粒度（D7）是唯一留有正向证据的表示层。
   ⇒ 本件**不是**"再放大一次"，而是"把一个从未被写入的面第一次写入"，这属于新的自变量。

## §2 主张与不主张

主张（若门通过才允许说）：在固定符号预算下，**把语言读出纳入训练**相对同等训练量的非读出路，
能否在**同一批未见题面**上带来可归因的改善。
不主张：不声称语言能力形成；不把成句率提升当回答能力；不以本件解除 R2 的 M5 排除状态。

## §3 设计（双臂 + 一个反事实，缺臂即作废配对）

| 臂 | 处理 | 它隔离什么 |
|---|---|---|
| **A 读出头** | 固定符号预算 N，仅开放 `predictive_readout` 学习，运动面冻结 | 自变量本身 |
| **B 多训一遍** | 同样 N 个符号，走原运动面路径，读出冻结 | "只是更多训练"这一替代解释（P3b 已证它是真混淆） |
| **C 出生即冻结对照** | N 个符号流过但两处都冻结 | 权重变化之外的漂移：episode 计数、动态状态、随机流 |

**重放纪律**：训练符号按**检查点血缘**计算，绝不重放该检查点已见过的数据——否则"改善"可以纯靠
记忆伪造（这条在 P3b 双臂里实测过代价）。未见评价集**先建先封**，训练前落 digest，训练后不得换题。

## §4 预算（先标定再报数，不拍脑袋）

**步骤 0（本件唯一可先行动作，属预算内极小额度）**：跑一次 `--ticks 30` 的试标定，测 wall/tick 与
峰值内存，写进 §4.1 后才提出 N 的上限。理由：本会话已两次因"成本未测就写进决策件"出错
（`--health` 从 17.2 s 一路更正到 36.7 s；标定件旧值作废），不再第三次。

### §4.1 待步骤 0 填入

- 实测 wall/tick = 待填；峰值增量 = 待填
- 由此给出的 N 上限与预计墙钟 = 待填（呈报后单独批准）

## §5 数值线（草案，认可后冻结；执行前不调）

| 项 | 草案值 | 依据 |
|---|---|---|
| 主门 M1 | A 臂在**未见** dev 上的 n 元成句率 ≥ B 臂 + C 臂的最好者 **+15 pp**，且逐题配对符号检验 p < 0.05 | 复用已带反向对照的判据（真中文 1.0 过 / 打乱 0.957 挂），阈值 -7.8 不动 |
| 主门 M2 | A 臂 CAP D+E 机器计分之和 ≥ B、C 的最好者 **+2 题**（同题面同链路） | 现基线 D 1/16、E 3/20，+2 题是可分辨的最小步进 |
| 保持门 K1 | A 维布尔 9 项不退化；H 门（已冻结的 max×2 阈）不越界 | 防止"为了说话把底座训坏" |
| 反事实门 K2 | 若 B 臂与 A 臂在 M1/M2 上无差别 ⇒ **判定为"更多训练"即可解释**，读出侧不得记功 | 这是本件唯一能自我否证的门 |
| 停止线 | 任一臂非确定、崩溃、或血缘/封存题面校验不过 ⇒ 记录并停，不看结果调线 | 本仓既有纪律 |
| 报告纪律 | 所有新报告落**新文件名**；封存件与既有判定不覆写；CAP 读数必须点名 `(checkpoint, chain)` | 本会话四次踩过的坑 |

## §6 与既有冻结面的关系

评价集现行 **v2**（本件不改门文本，只可能新增未见题面集 → 那属 **v3**，需另行批准）；
F 维 `stale_reference` 守卫保证换了基座就报警；H 阈值绑 `(设备,链路,checkpoint)`，
**训练会改 checkpoint ⇒ 训后 H 必须重标**，不得沿用今天的数（否则重演"旧门放行真实劣化"）。

## §7 待所有者定的三件事

1. 认不认 §5 的 +15 pp / +2 题 / K2 反事实门（不认就改判据形态，不改阈值迁就结果）；
2. 批不批**步骤 0** 的极小标定额度（几十 tick，只为把成本测准）；
3. 未见题面集是否允许新增（若不许，M2 只能在既有 40 题上讲，统计功效明显变弱——这点必须写进结论边界）。
