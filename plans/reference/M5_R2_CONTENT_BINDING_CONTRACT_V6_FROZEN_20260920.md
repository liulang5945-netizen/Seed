# M5 / R2 内容绑定 v6 合同（目标调度，冻结版）

日期：2026-09-20。状态：**已冻结**（用户常设授权：弹窗超时按推荐项自动推进）。依据：[v5 判停](M5_R2_CONTENT_BINDING_CALIBRATION_STOP_20260919.md) §9——margin 饱和与 copy 崩塌耦合，指向**目标调度**假设。以 [v4 合同](M5_R2_CONTENT_BINDING_CONTRACT_V4_FROZEN_20260920.md) 为基线，**单一主变量：对比压力的时间调度**。

## §G1 冻结设计

**假设 H-S**：v5 实证 margin 饱和期 copy 崩塌——对比压力在 margin 饱和后从"驱动力"变成"copy 侵蚀源"。调度化（前期全压驱 margin、后期线性撤除保 copy）可在 margin 饱和的同时保住 copy 能力，从而让标定读数首次接近合格线。

**调度（冻结）**：λ_pair(t)：t ≤ 8000 → 1.0（v5 已证 8000 内 margin 显著下降）；8000 < t ≤ 12000 → 线性 1.0→0；t > 12000 → 0。总更新 16000、容量 256/256、其余全部继承 v4（数据/配方基线/判据/停止线）。

**四条线继承＋copy 保持线**：L1–L4 同前；**新增 L5 copy 保持线**：终段（14000–16000 平均）copy 率 ≥ 0.55（v5 无调度对照崩至 0.27；0.55 约为 v5 未崩期水平，可检验）。

## §G2 运行与判定

- 两臂 B/A × cal_lr1 × 16000 更新（预计约 2.5 小时）；产物 `reports/r2_content_binding_v6/`；
- 判读：L1–L5 全过 ⇒ `unified_entry_supported`＋copy 保持 ⇒ 合格候选（进正式流程）；copy 保持失败 ⇒ 调度假设被否（崩塌非仅由对比压力承载）；margin 不饱和 ⇒ 回退 v4 结论。
- 阈值零放宽；判停纪律不变。

## §G3 边界

不碰 sealed；不修改 v1–v5 冻结判据；调度参数（8000/12000）冻结后不调。
