# R2-D2 修订五：R1 微批+形状均衡（类别先验的干净检验，冻结版）

2026-09-18。状态：**已冻结**（用户在评审备忘 §5 二次评审裁决 R1 后提交即冻结）。
依据：P1（均衡+batch1）失稳与 Q1（非均衡+微批稳定但平台不变）把两个混淆变量拆开；本修订是对「首字节失败=训练答案先验（96:55）压制内容梯度」这一假设**第一次无方差混淆**的检验。

## §1 冻结干预（两变量组合，各自已单独验证）

1. **图不变**：v4 A2（per_position+copy，常量 start，82,658 参数）。
2. **采样序列（沿用修订三§2 的冻结算法）**：每 epoch 174 个抽取，sorted(shape) 外循环、配额 25×6+24，组内 `random.Random(20260917+epoch)` 有放回抽取——先生成完整 174 序列。
3. **微批（沿用修订四）**：把该序列顺序切成 21×8+1×6，每批经 `train_step` 平均梯度一次 Adam 更新；22 步/epoch×30=660 步，episode 暴露 5,220。
4. lr 0.01、seed 20260917、30 epochs、wall 20 min、preflight 先行；评估在**未改动**原始 train split。

## §2 门（冻结；与前序同口径）

1. 稳定性：相邻观测（e5/10/15/20/25/30）loss 上升 ≤15%，e25→e30 M1 回撤 ≤0.10。
2. 学习：copy-supported（144）M1 **≥0.90**；negation M1>0；fact/sof 各 ≥0.90（H-A2/Q1 基线为 1.0）；finite/declining、preflight、wall cap。

## §3 出口（预承诺，一次）

- **稳定 + M1≥0.90**：类别先验假设在无方差混淆下获支持 → 允许 matched dev（balanced-microbatch A2 vs C1 同法，3 seeds，G1–G6 原样）。
- **稳定 + M1 仍≈0.667**：答案 CE 下类别先验不是首字节成因（即便稳定均衡也不改变决策均衡），决策层假设在当前图上判否；回评审，主候选转为目标函数/容量(R2)/绑定(R3)，不得再围绕加权/采样做第三次。
- **不稳定**：与 Q1 矛盾，意味着均衡内容本身（而非方差）触发失稳；如实记录并回评审，不调参重试。

## §4 边界

不读 dev/final；不改 D1 仪器与判据；产物 `growth_admitted=false`、`can_promote=false`；train-only 成功不是 L2/内容能力主张；本干预只做一次。
