# M5 R2-D2 A 族三次 learnability 停止后的全面评审备忘

2026-09-18。状态：**决策材料，训练冻结，等待用户全面评审裁决。**
触发：[修订二 §3](M5_R2_D2_QUESTION_START_AMENDMENT_FROZEN_20260918.md) 预承诺（O1 再失败 ⇒ 无第三次自主图修订，A 族路线交回用户全面评审）。

## §1 证据账本（D1 train split，dev/final 未读）

**已确定（实现门+训练报告）：**
- v3/v4/v5 三图的接线、梯度、因果、lesion、checkpoint 恢复全部正确（51 合同测试、ruff/black、核心 mypy=0）。
- H-A1（纯生成+软寻址）：读出在答案 CE 下塌缩为题干条件常量（材料质量≈1e-26）。
- H-A2（+copy）：loss 4.54→0.089；fact/sof/unknown/sou/combo_same train exact=1.0；copy 机制能完成材料取色与拒答。
- 失败形状 = 答案首字节为「不」(0xe4) 的两类（negation、combination_different）；H-A2 TF 除第一步外目标概率 1.0；第一步 0.194 vs 颜色 0.806。
- H-A3（问题条件化 r_0，用户裁决 O1）：实现门全绿但同一 0.667 平台 e10–25 复现，e25–30 优化器失稳（0.195→0.865）。

**不能外推（归因收紧，沿用 03 §0.1）：**
- 不能仅凭 q_0 固定/条件化的两次结果断言「材料条件化不可能」——v3 诊断只证明答案 CE 下软读出塌缩，v5 只证明「仅改起点」在 30 epochs/lr0.01 不充分。
- TF 高概率 ≠ 自由生成全链正确（H3.7B 旧教训在本包再次出现）。
- 0.667 平台与晚期失稳不能外推为：绑定(B)无效、组合计算(C)无效、容量/课程无影响。
- 旧族（H-GEN/H-OBJ/H-FBW）的负结果针对的是 16 题旧 dev 与不同图，不自动适用于 copy 图。

## §2 候选方向（全面评审；每个含干预判据）

| 方向 | 具体假设/干预 | 若先做，先验结果 | 判据（训练集即可判定） | 主要风险 |
|---|---|---|---|---|
| **P1 优化/决策层（推荐先评审）** | 首字节是决策先验问题（119:55）而非表示无能（TF 后验近完美）。干预：形状均衡采样或对首字节类别做均衡的训练合同（O2 的合同化版本，需新训练目标/采样合同，不动 D1 仪器） | H-FBW 的损失加权在旧图/旧 dev 失败；但 copy 图与可辨识仪器是新条件，且本次定位层级不同 | 均衡后 negation 首字节贪心正确率↑且 fact 不塌；若 TF 首字节概率本身也提不上，则不是决策层问题，转 P2/P3 | 被读成「调参救结果」——必须以新合同冻结采样规则与门，且只做一次 |
| P1b 解码/序列目标 | 暴露偏差方向：序列级（beam/最小风险）目标或计划采样，让第一步错误的后果在训练中可见 | H-GEN 计划采样在旧图被否；beam 不改变第一步 0.194<0.806 的事实 | 需配合 P1；单独 beam 在首步 argmax 不变时无效（本次已静态推出） | 与 H-GEN 同型，证据最弱 |
| P2 表达容量/门几何 | rw/sw 加宽、多 copy 头或每步 π 容量不足；随机投影 r_0 的优化难度 | wide-2× 在旧 dev 无增益；A5 失稳提示优化面敏感 | P1 不能把 TF 首字节概率拉离 0.194 时再上；要求参数匹配对照 | 容量加宽历史为负，先验弱 |
| P3 B 对象绑定/关系 | 可学习对象槽支撑「不是X」与双事实比较 | combo_same 已满分说明单比较可学；combo_different 失败与 negation 同首字节，绑定假设未被独立触达 | 待首字节解决后若 combo 两事实仍失败再上（VISION 的 C/B 路由判据原样有效） | 当前证据不支持提前上 B |
| P4 规模/真实课程 | 上更大语料/真实短文 | 03 纪律：微任务未拟合不扩数据 | P1–P3 在 D1 train 全部判负后才进入 | 成本高，证据最弱 |

## §3 推荐与边界

**唯一推荐：先评审并（若批准）冻结 P1 的形状均衡训练合同**——它针对目前唯一被精确定位、且 TF 证据指向「决策/优化而非表示」的故障点；不改正已过门的图、不动 dev 仪器；门仍用 copy-supported M1≥0.90（含 negation 必须>0），并要求 fact/sof 不回落。P1 合同必须预注册：均衡采样规则、只跑一次、失败即按本表转 P2/P3，不追加加权技巧。

**硬边界**：本备忘不授权训练；下一工作包（若批准）另行冻结合同与预算。dev/final 继续封存；任何通过都不主张 L2；五形状满分的正向证据与 0.667 平台同时保留，不互相覆盖。
