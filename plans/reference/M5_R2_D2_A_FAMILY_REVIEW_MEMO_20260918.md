# M5 R2-D2 A 族三次 learnability 停止后的全面评审备忘

2026-09-18。状态：**决策材料，训练冻结，等待用户全面评审裁决。**
同日规划复核：本备忘是可修订决策材料，不是已冻结的后继实验合同。对照[唯一VISION §18](VISION_FUTURE_TECHNOLOGY.md)补全联合设计与归因边界；保留D2停止裁决，不把下面的候选强制排为依次试验的队列。
触发：[修订二 §3](M5_R2_D2_QUESTION_START_AMENDMENT_FROZEN_20260918.md) 预承诺（O1 再失败 ⇒ 无第三次自主图修订，A 族路线交回用户全面评审）。

## §1 证据账本（D1 train split，dev/final 未读）

**已确定（实现门+训练报告）：**
- 对应研发提交报告v3/v4/v5相关接线、梯度、因果、lesion、恢复合同测试通过（51项及所述静态检查）；继承其覆盖范围，本轮未复跑。不能由有限测试推出所有计算/学习路径全部正确或训练必然成功。
- H-A1（纯生成+软寻址）：读出在答案 CE 下塌缩为题干条件常量（材料质量≈1e-26）。
- H-A2（+copy）：loss 4.54→0.089；fact/sof/unknown/sou/combo_same train exact=1.0；copy 机制能完成材料取色与拒答。
- 失败形状 = 答案首字节为「不」(0xe4) 的两类（negation、combination_different）；H-A2 TF 除第一步外目标概率 1.0；第一步 0.194 vs 颜色 0.806。
- H-A3（问题条件化r_0）：实现门记录通过，同一0.667平台在e10–25复现，e25–30训练指标退化（loss 0.195→0.865，copy-supported最终20/144）；尚未独立归因于某个优化器缺陷。最终negation=4/48，不将中途状态当作全部时点结果。

**不能外推（归因收紧，沿用 03 §0.1）：**
- 不能仅凭 q_0 固定/条件化的两次结果断言「材料条件化不可能」——v3 诊断只证明答案 CE 下软读出塌缩，v5 只证明「仅改起点」在 30 epochs/lr0.01 不充分。
- TF 高概率 ≠ 自由生成全链正确（H3.7B 旧教训在本包再次出现）。
- 0.667 平台与晚期失稳不能外推为：绑定(B)无效、组合计算(C)无效、容量/课程无影响。
- 旧族（H-GEN/H-OBJ/H-FBW）的负结果针对的是 16 题旧 dev 与不同图，不自动适用于 copy 图。

## §2 候选方向（全面评审；每个含干预判据）

| 方向 | 具体假设/干预 | 若先做，先验结果 | 判据（训练集即可判定） | 主要风险 |
|---|---|---|---|---|
| P1 更新/训练分布 | 先验失衡或更新规则是候选解释；均衡采样/训练目标是候选干预，保持D1评价不变但需新训练合同 | 旧图加权失败不自动否定copy图；TF后续字节正确也不能排除首步表示问题 | 配对比较首字节概率、自由回答、形状保持与稳定性；失败只否定该配置，不自动排除整个优化解释 | 易落入无限调权；须限定搜索、预算、停止与保持门 |
| P1b 解码与序列学习 | 解码搜索与序列级训练是两个不同变量，分别定义成本/目标，不把beam当训练损失 | 旧H-GEN不直接评价当前图；首步argmax不变不足以静态否定所有序列搜索 | 未有实际对照，不预判有效/无效；解码改善不能冒充学习改善，训练策略另定合同 | 增加计算或改变指标可能掩盖原始内容问题 |
| P2 表达/内容状态与门几何 | 不只加宽：问题与证据如何形成可供生成的任务内容状态、copy与推导怎样分工 | v5晚期退化与旧wide负结果提示风险，但未唯一定位容量或更新原因 | 对照可达信息、参数/计算、目标信用与材料依赖；完整联合设计只归因到整包 | 状态更大可能仍学捷径，迁移与学习成本增加 |
| P3 B对象绑定/关系 | 可学习对象/事件槽和来源引用，关系更新实际服务原生回答 | v4的combo_same训练成功不证明一般关系能力；绑定假设尚未独立检验 | 角色/关系反转、未见组合及公平证据通路；不强制先试尽A的局部补丁 | 绑定学习和开放输入覆盖成本高，不能以规则parser替模型完成任务 |
| P4 数据/课程/规模联合设计 | 明确实际经历覆盖与容量/更新匹配，不是无界增加语料 | 当前D2微集门失败必须停止；它不是对所有未来训练范式的永久禁令 | 若要改变研究范围，先审查数据许可、独立评价、质量—成本、预算与新合同；无需将P1–P3依次判负作为理论前提 | 资源成本与归因混淆；未批准不采集、不训练 |

## §3 推荐与边界

**唯一推荐：先完成联合设计评审，再由用户裁决一个有边界的后继包或停止决定。** 早先优先P1的建议保留为候选理由，不是已证唯一根因或默认队首。依据VISION §18.9，把A内容形成层、B绑定核心、A/B＋C计算以及学习/课程/规模规格放到相同的能力、判别价值、复用和代价标尺下；不是全部实施，也不必等局部补丁逐一失败才允许更高上限设计。

被选中的后继包须冻结目标、数据/采样、对照、预算、保存/恢复和出口。沿用D2评价时保持其历史口径；若研究主张改变则新建明确身份，不事后放松旧门。失败后回到证据与范围决策，不自动执行P2/P3/P4。

**硬边界**：本备忘不授权训练；下一工作包（若批准）另行冻结合同与预算。dev/final 继续封存；任何通过都不主张 L2；五形状满分的正向证据与 0.667 平台同时保留，不互相覆盖。
