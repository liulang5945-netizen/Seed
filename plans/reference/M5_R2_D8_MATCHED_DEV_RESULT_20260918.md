# M5 R2-D8 matched dev 结项：**K1 主门首次通过**，但 M4/flip 绑定未达 —— 路由至「连续函数」检验点

日期：2026-09-18。状态：**matched dev 完成，按合同 §5 路由表落第二行**。
合同：[M5_R2_D8_SCALE_CONTRACT_FROZEN_20260918](M5_R2_D8_SCALE_CONTRACT_FROZEN_20260918.md)；
裁决依据：[(b) 裁决记录](M5_R2_D8_ADJUDICATION_B_20260918.md)、
[probe 补证](M5_R2_D8_PROBE_MULTISEED_20260918.md)。
报告：`reports/r2_d8_matched_dev_20260918.json`（train fixture v3，digest `a73703de…`）。

## §1 门与读数（**看 dev 前冻结**）

| 门 | 阈值 | 实测 | 判 |
|---|---|---|---|
| **K1 多字值 `full_rate` 均值（主门）** | **≥ 0.30** | **0.3621** | **✅ 通过** |
| K2 `M4_flip` 均值 | ≥ 0.50 | **0.0000** | ❌ |
| G1 `M3` 均值 | ≥ 0.26 | **0.3101** | ✅ |
| G2 `M4_flip` + `fact_flip` | ≥0.50 / ≥2/6 | **0.0 / 0.0** | ❌ |
| G3 copy misbind drop | ≥ 0.50 | 0.0000 | ❌ |
| G4 context margin | ≥0.30 且 no-ctx M1 ≤0.50 | **✅**（`no_context_m1_max = 0.0`）| ✅ |
| **G5（D8 合同 §5 定义）** | ≥2 seed `full_rate`>0.10、最差 ≥0 | **✅**（2/3 站稳）| ✅ |
| G6 boundary | min ≥ 0.95 | ✅ | ✅ |
| `first_byte` | ≥ 0.529（byte 图水平）| **0.7529** | ✅ |

**逐 seed**（`multibyte_full_rate_per_seed = [0.4310, 0.0000, 0.6552]`）：

| seed | M1 | M3 | **M4** | **多字值 full_rate** | `first_byte` | no-ctx M1 |
|---|---|---|---|---|---|---|
| 20260917 | 0.2551 | 0.2907 | 0.0 | **0.4310** | 0.6379 | 0.0000 |
| 20260918 | 0.1735 | 0.1977 | 0.0 | **0.0000** | 0.9310 | 0.0000 |
| **20260919** | **0.3878** | **0.4419** | 0.0 | **0.6552** | 0.6897 | 0.0000 |

## §2 按合同 §5 路由表裁决

合同预置四行路由，本包读数（**K1 过、K2/G2 败**）精确落在**第二行**：

> **「复制站稳但绑定精度未达 → 「绑定 = per-item 率连续函数」预测检验点，
> 评审决定再一度放大或收束」**

**⇒ 不是"粒度+尺度双判否"**（那一行要求三 seed 全零 `full_rate` 均值 <0.05，
实测 0.3621）；**也不是"全门通过"**（M4 全线为 0）。

## §3 本包的三项实质进展（R2 系列第一次）

1. **主门 K1 首次通过**：多字值 `full_rate` 均值 **0.3621 ≥ 0.30**，
   最佳 seed 达 **0.6552** ⇒ **"从材料复制多字值"这一能力在尺度包下首次站稳**；
2. **M3 均值 0.3101**（预注册表面天花板 **0.1735**）⇒ **1.79×**，
   最佳 seed **0.4419**；**M1 均值 0.2721** 同样超过 0.1735；
3. **`no_context_m1_max = 0.0`（三个 seed 全部 0）** ⇒ **无材料时不猜** ——
   这是"确实在用材料"的**最强单点证据**；`first_byte 0.7529` 也**远超 byte 图水平 0.529**。

## §4 明确的缺口：**M4/flip 绑定全线为 0**

`M4_flip_pair` 与 `fact_flip` / `combo_flip` **三个 seed 全为 0**，`G3`（misbind drop）也因此为 0。
⇒ 与 probe 阶段的附带发现一致：**combo 形状学到的是恒定回答**（917/918 恒"相同"、919 恒"不同"），
**"翻转对"所要求的、对同一问题按材料取反的绑定能力没有建立**。

**这不是"复制"失败，而是"复制→按条件绑定"这一段的失败** ——
故合同的第二行把它表述为「**绑定 = per-item 率的连续函数**」的检验点。

## §5 combo / unknown 单列（合同要求）

| 形状组 | 读数 |
|---|---|
| `fact_flip` | **0.0** |
| `combo_flip` | **0.0** |
| combo（`combination_same/different`） | 恒定回答（见 §4）|
| unknown / same_opening_unknown | 未参与主门；`M1` 口径已含（`no_context_m1_max = 0.0`）|

## §6 结论与建议（**需用户裁决**）

**结论**：D8 尺度包**支持 H-S 的一半** ——
「扩尺度使三 seed 的 dev 多字值 `full_rate` 收敛到预注册阈值之上」**已成真**（K1 过、G5 过、2/3 站稳），
但「M4/flip 随 per-item 率越级出现」**未成真**（全线 0）。

**建议（按合同第二行，二选一，均需裁决）**：

- **(α) 再一度放大**：若认为"绑定"是 per-item 率的连续函数，则当前 per-item 率（0.36）尚不足以
  越过 flip 所需的更高门槛，需再放大一档（对象/颜色池再翻倍）；
- **(β) 收束**：若认为 0.36 的 per-item 率已足够而 flip 仍为 0，则"绑定"不是同一函数，
  R2 应转入收束/入口转换评审（D1–D8 机制地图已完整）。

**助手倾向 (β)**：因为 §3 的三项进展都指向**"复制链已建成"**，
而 §4 的 flip 全零是**结构性**（三 seed 一致、且 combo 呈恒定回答）——
用"再多一点数据"解释一个**跨 seed 完全一致**的结构性缺失，说服力不足。
但**该判断需要用户裁决**，本文件不自行推进。

## §7 边界

- 已按 (b) 裁决显式标注：probe 的 `loss_increases_bounded_15pct` 门**经特批按单 seed 抖动处理**，
  依据见 [补证](M5_R2_D8_PROBE_MULTISEED_20260918.md)；
- **dev 口径**：v3 的 dev 与 v1 **逐字节相同**，故与 D7 的 dev 读数可直接比较；
- `final` 未读；`growth_admitted=false`、`can_promote=false`；**无能力收益主张**；
- 本包是机制地图最后一根预注册轴，其后无论读数如何都进入阶段收束/入口转换评审。
