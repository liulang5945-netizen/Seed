# M5 / R2 内容形成与绑定：实施合同（冻结版）

2026-09-19。状态：**已冻结**。冻结出处：零训练实现门与资源预检于提交 `f9a62576` 交付并通过（[实现门交付报告](M5_R2_CONTENT_BINDING_IMPLEMENTATION_GATE_20260919.md)），用户于同日**批准训练预算**（问答记录：批准按拟议上限运行）。本文件把[实施合同草案 v1](M5_R2_CONTENT_BINDING_CONTRACT_DRAFT_20260919.md) 升级为 FROZEN：**§1–§7 全部条款逐字冻结**（草案文件 sha256 `e31a379f26e5191b7959105fd6bc892d88f0556768a349586bf0f21ed3e07c68`，其后任何草案正文改动都不改变本合同，须另版），本文件只登记冻结事实、已核准预算与执行身份，不新增或放宽任何门。

## §A 冻结时已核准的预算与身份（合同草案 §4/§6 的数值自此生效）

| 项 | 冻结值 |
|---|---|
| 统计训练更新总量 | 最多 20000（标定 4 次＋正式 6 次，每次 2000） |
| 全包墙钟 | 12 小时（串行累计，不以并行掩盖） |
| 单次运行墙钟 | 90 分钟 |
| 独立产物 | 10GiB |
| 设备 | 一个已批准本地 CPU 执行设备（12 线程，float32；预检 [resource_preflight_20260919.json](../../reports/r2_content_binding_v1/resource_preflight_20260919.json)） |
| 标定 seed | 20260920；两臂（A/B）×两配方（cal_lr1 峰值 0.001 / cal_lr3 峰值 0.003），各 2000 更新；500/1000/1500/2000 保存并评价 calibration |
| 正式 seed | 20260921 / 20260922 / 20260923；每臂用各自选定配方独立重训 3 次，只取第 2000 更新 checkpoint |
| 标定选择规则 | 满足复制≥0.90、未知≥0.90 且无非有限值的候选中，事实/对象/关系三类成对宏平均最大；并列优先低 lr，再较早更新；不合格即停止该臂，整包不进入确认，不补第三配方 |
| 确认批 | 全部正式候选身份锁定后**一次授权批次**评价 sealed（§5 各门一次判读，不边读确认边调参） |
| 产物目录 | `reports/r2_content_binding_v1/<arm>/<seed>/<config>/`（atomic 保存，manifest 含 SHA-256） |

预检实测基线（外推）：最坏臂 0.109 秒/更新，单次 2000 更新≈3.7 分钟，10 次运行合计≈37 分钟，RSS≈335MB，零步 checkpoint≈1.4MB——全部远在上限内；实际运行以 run report 记录为准。

## §B 冻结的执行顺序与停止条款（重申草案原文，不改动）

1. 标定 4 次运行 → 每臂按上表规则选择配方；某臂无合格候选即该臂停止、整包不进入确认。
2. 正式 6 次运行（或某臂停止后按剩余臂执行）→ 正式候选身份锁定。
3. 确认批：sealed 一次性评价，按草案 §5 全部门判读（BIND/DELTA/COPY 保持/其他切片/旧 dev 回归/内容消费/真实性恢复）；全部所需门过才支持"有限内容结构包"结论。
4. 非有限 loss/参数/梯度、checkpoint 写入/续训失败立即停止；超资源上限记 incomplete，不以早期权重冒充终点。
5. 冻结后不调门：阈值、分母、seed、更新预算均以本文件与草案为准；如执行中发现不相容，先修订版本并记录理由，不先训练再调设计。

## §C 冻结后本轮之外的约束

不重开 D8 同配方追加；不把确认题路径接入训练进程；不自动晋级 L2/M5、不切换默认模型、不重启 P3b；确认集 fixture 内容不打印到训练日志。任何对 §A 预算或 §5 门的修订都是新的用户决定。
