# M5 P3b 结项报告的**结构与允许表述**预案（写于任何阶段成绩之前）

日期：2026-09-15（本地 01:38）。状态：**预案**——只定结构与口径，不含任何结果。
依据：[P3b 预注册（判据冻结）](M5_P3B_ALIGNED_LANGUAGE_TRAINING_PREREGISTRATION_20260915.md) §3/§4、
[双臂新颖度配对修订](M5_P3B_NOVELTY_MATCHED_ARMS_AMENDMENT_20260915.md) §4–§5、
[路线 B 冻结版 §7](M5_B0_ROUTE_B_PREREGISTRATION_FROZEN_20260915.md) 的"允许/禁止表述"体例。

**为什么要单独写这一份**：结项时最容易犯的错不是算错数，而是**看着数决定怎么写**。
本文件在 `stages_recorded = 0` 的时刻把表结构、数值口径与允许说的话钉死；
正式结果文档只能**填格**，若发现结构不够用，须另立文档说明"预案缺了什么"，
**不得**回改本文件（与冻结件同一纪律）。

## §1 产物与来源约束

| 项 | 约定 |
|---|---|
| 结果文档名 | `M5_P3B_ALIGNED_LANGUAGE_TRAINING_RESULT_<结项日>.md`（新文件，不改写预注册与修订件） |
| 分数唯一来源 | `reports/p3b_stages/<arm>/cap0_tick_<tick>.json` 的 `dimensions.<X>.tally.machine_normalised` |
| 差值来源 | `reports/taiji_p3b_campaign_<arm>_20260915.json` 的 `stages[].deltas_vs_p3a`；**主效应**由 `summarize_p3b.py` 的 `main_effect_verdict()` 输出，不手算 |
| J1–J5 来源 | `check_p3b_criteria.py --baseline <P3a> --candidate <每份阶段报告>` 的机检结果（两臂各一次） |
| `null` 的处理 | 一律记 **`untested`**，不得记 0、不得记通过、不得从表里删行 |
| 已封存文件 | 只加前进指针，数字与判据不改写 |

## §2 必备表（先画空表，结项时只填格）

### 表 A — 逐 tick 双臂分数与主效应（每一行是一个**共同**检查点）

| tick | C实 | C对 | C−P3a实 | C−P3a对 | **C 主效应** | D实 | D对 | **D 主效应** | E实 | E对 | **E 主效应** | chain 全等 | 面漂移 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| _待填_ | | | | | | | | | | | | | |

> 主效应列必须同时给出 `main_effect_verdict()` 的**标签**（`effect_positive` /
> `above_resolution_single_point` / `not_detected_at_this_resolution`），不能只写差值。

### 表 B — 判据逐条 × 逐臂（J4 的 A/H 分支永久写 `untested`）

| 判据 | treatment | control | 依据产物 |
|---|---|---|---|
| J1 同一链路 | | | `stages[].chain_matches_p3a` |
| J2 C/D/E 三项均严格高于 P3a | | | `deltas_vs_p3a` |
| J3 最低线 C/E ≥70%、D ≥80%，**且 B 人工复核达标** | | | `check_p3b_criteria` + 复核表 |
| J4 G 硬安全失败 = 0 | | | G 维 `tally` |
| J4 A/H 不退化 | **`untested`** | **`untested`** | DEBT-I4（整维 `not_executed`） |
| J5 机检与人工复核分列、原始回答留档 | | | 报告 `scoring_discipline` + 复核表 |
| **主效应**（H-P3b 的直接对象） | 共用一列 | | `summarize_p3b.py` |

### 表 C — 停止条件轨迹（把"为什么停/为什么没停"变成可核对的序列）

| tick | improved | stall streak | regression kind | pending_delta(B/G) | 是否触发 §4 |
|---|---|---|---|---|---|
| _待填_ | | | | | |

### 表 D — 披露清单（**缺任一项即视为报告不完整**）

1. 两臂**共同 tick 区间**与各自停止时刻（若不同，必须写）。
2. 语料事实：两臂各 52,280,541 / 52,280,150 符号、同一未见过窗口、起点同为源行 2,893、
   重放占比 **0%**、共享 leading 符号 61,249（预算 0.13%）、对话密度 **1.000 对 0.2356**。
3. 残余不对称：**源覆盖宽度**（实验臂读到源行 106,324，对照臂只到 32,768）⇒ 结论口径见 §3。
4. 机时：达成符号数与真实墙钟；**不写**"速率 × 预算"式承诺。符号率随同机进程数变化
   （273.7 / ~253 / ~180），并列出跑测期间本机还跑了什么。
5. 评测面：每阶段 `eval_set` / `eval_set_format` / `eval_set_frozen_on` / `declared_mode`
   与 C/D/E 题序**全等**（驱动守卫 `eval_surface_drift` 未触发），且两臂快照的
   `metadata.corpus_fingerprint` **不同**（证明两臂确实吃了不同数据）。
6. 受保护检查点 `seed_corpus.pt` / `seed_beta.pt` 起止大小与 mtime 未变。
7. 作废的第一次跑（35 分钟）与其原因，指向修订件。
8. 远端 CI **未查询** ⇒ 全文禁止"CI 已绿"表述。

## §3 允许表述 / 禁止表述

**允许说**（示例句式，数字结项时填）：

- "在**同一未见过源窗口、同预算、同目标函数、同推理链路**下，对话密度更高的训练流使
  C 维相对对照提高 X（Y 题），并在 **≥2 个连续共同检查点**保持同向。"
- "两臂各自的 J2 为 ___；因此 ___（'多训一遍'/'数据分布'）两条解释中至少一条成立/不成立。"
- "本次为负结果：主效应在 0.15 分辨率下未检测到 ⇒ 按预注册 §1 的**反假设方向**，
  缺口更可能在序列建模/架构层，**但本实验不证明该点**（见禁止表述第 1 条）。"
- "B/G 的人工复核结论为 ___（填表人、日期、复核表路径）。"

**禁止说**：

1. **不得**把"未检测到效应"写成"数据分布无关/无效"——分辨率以下无结论。
2. **不得**把任一带对 P3a 的单边提升说成"数据分布起了作用"（那是主效应的定义域）。
3. **不得**用 `online_accuracy` / `mean_surprise` 的两臂差当效应（两臂吃不同数据）。
4. **不得**用 `holdout_surprise`（固定 ~100 字节探针）下任何结论；它可能是格式熟悉度。
5. **不得**写"A/H 未退化"（DEBT-I4：整维 `not_executed`）。
6. **不得**写"模型学会对话/具备回答能力"这类能力断言，除非 B 维**人工复核**已填且达标。
7. **不得**外推到未测的源窗口、其它预算档、其它起点检查点或 E/T 模式（§5 冻结口径）。
8. **不得**声称远端 CI 通过；**不得**用本包结果给协作轴（M5 B 线）晋级。
9. **不得**把"某一臂先触发 §4.2 止损"读成"它的分布更差"，也**不得**把"跑满 48 h 档预算"读成
   "有效应"——止损门槛（任一维度 > 0）与结论门槛（≥ 0.15 且两点同向）**故意不对称**，
   见修订件 §4.1；两臂停止时刻不同只是预算事实，写进表 C 即可。
10. **不得使用 campaign 记录里的 `best_stage` / `best_stage_tick`**。实测该字段是
    `max(stages, key=scores["D"])`——**只按 D 一个维度取最大**，与本预案"按 tick 配对、
    不用各自最好的一次"直接冲突，是一个坐在仪器里的挑数钩子。
    结项只用表 A 的逐 tick 序列与表 C 的停止轨迹；`best_stage` 只能作为"我们看到了这个风险"的证据。
    删掉/改造它是**campaign 结束后**的代码改动（在跑的驱动已把该逻辑加载进内存，
    本次记录必然含此字段；为此重启要再花 54 小时，不值得，故按"文档先行设界 + 事后修仪器"处置）。

## §4 结项最小动作清单（顺序即检查清单）

1. `summarize_p3b.py --json` → 取 `main_effect`（表 A 的判定列）。
2. **逐阶段**跑判据检查器，且**每阶段一个独立输出名**（默认路径只有一个，连跑会互相覆盖）：
   `check_p3b_criteria.py --baseline reports/taiji_cap0_baseline_constrained_20260915.json
   --candidate reports/p3b_stages/<arm>/cap0_tick_<tick>.json
   --output reports/p3b_criteria/<arm>_tick_<tick>.json`（机检只读两份 JSON，秒级）。
   表 B 的 J2 单元格必须写成**"通过的阶段数 / 共同阶段数"**，不得只给末阶段的单次结论——
   否则一次非单调波动就能把"通过/不通过"整个翻掉，而读者看不见。
3. `make_p3b_review_worksheet.py --report <末阶段> --changed-vs <上一阶段>` → 交用户打分；
   **未回表前 J3 的 B 分支写 `待人工复核`**，不得自行填分。
4. 读两臂最后两个快照的 `metadata.corpus_fingerprint`，确认不同并写进表 D 第 5 项。
5. 填表 A/B/C/D → 按 §3 写结论段 → 更新 [03](../active/roadmap/03_CURRENT_EXECUTION.md)
   的"唯一下一步"与 [README 索引](../README.md) → 本地提交（不推送）。
6. 负结果**照写**，并按预注册 §1 的反假设登记后续议题；同时把 WP-6 的
   `binder_version=v2` 预注册解禁（本文件写作时它仍被在跑 campaign 挡住）。
