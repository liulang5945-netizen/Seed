# R2 受控重训语言读出：M1 功效补足轮预注册（v3 未见题面集）

状态：**预注册已冻结，题面集已封存，尚未打分**。所有者 2026-09-23 裁决「甲选1 + 乙选1」后立即落盘本件。

## §0 本件是什么，不是什么

**是**：对上一轮 M1 的**功效补足重测**。上一轮 `reports/taiji_r2_readout_retrain_verdict_20260920.json`
判定 M1 **不成立**，但只差一个不一致对（`b=14 c=5 n=19`，**p=0.0636** vs 门 0.05；精确检验要 ≥15/4）。
本件在**更大的一份未见题面**上，用**同一把尺子**再问一次同一个问题。

**不是**：
- 不是"换一套题重判到过为止"。**效应量是固定的**，N 变大只会让**同一个门**更容易或更难达到，
  不存在挑样本的空间；而且本件**只看一次**（§4）。
- 不改门、不改裕度、不改显著性水平、不改判据、不改臂、不重新训练。
- 上一轮的结论**照旧成立并保留**（CAP-43 上 M1 不成立），本件是**第二轮**，两轮都要一并引用。

## §1 所有者裁决记录（2026-09-23）

| 事项 | 裁决 | 落地 |
|---|---|---|
| 甲：K2 预注册缺陷（有符号比较） | **选1** | 上一轮**按字面报**（读出侧不记功），`§5.1` 原文**一字不改**；**修正版只在本件预注册、只对本轮生效**（§3） |
| 乙：M1 只差一个不一致对，加不加功效 | **选1** | 批 v3 未见题面集，**不涉训练预算**（三臂 checkpoint 已存在，纯评价） |

## §2 未见题面集 v3（已封存，打分前落 digest）

- 件：`plans/manifests/r2_readout_m1_v3_prompts.json`
- **sha256 `f1ac06a1b8652c4dd8fd7a659117ae960d5604c9f65779b1955ff32d93305489`**，160 条
- 源：`data/p3b_all_fresh.jsonl`（sha256 记在集合内），血缘清单 `plans/manifests/p3b_all_fresh_manifest.json`
- **抽取规则（谁都能重跑，我没有任何挑选余地）**：行内累计偏移 = `Σ(len(text.encode())+1)`（与清单
  `emitted_symbols` 口径逐位相符）**≥ 16,000,000** 的行 ⇒ 第 **4026** 行起共 25,851 行；
  `stride = max(1, 25851 // 160) = 161`，从 4026 起等距取 **160** 条；题面 = 该行 `text` 的**前 32 个字符**。
- **为什么这算"未见"**：三臂的训练窗口是 p3b 的 `[11,199,800, 27,199,800)`；本集取自
  **16,000,000 符号之后** ⇒ 位于训练窗口**之外**，三臂都没见过这些字节。
  构建器 `scripts/training/build_r2_readout_m1_v3_prompts.py`（已存在即拒跑）。
- **如实登记的代价（必须一起引用）**：题面从"CAP B/G 的 43 个问句"换成了"未见中文散文的前 32 字"。
  这是**题面总体变了**，不只样本量变大。之所以接受：它是**机械可重放**的，而人工撰题在看过上一轮
  结果之后有"无意挑到对 A 有利的题"的空间。代价换来的是**没有挑选余地**。
- **新增回声控制**（因为题面是真实散文，存在"照抄上下文"的通道）：逐臂报
  `prompt_echo_8gram_rate` = 输出中存在任一 8 字子串出现在题面里的比例。它**不参与判定**，
  只用来让"照抄"这一解释可见；若三臂回声率差异很大，读结论时必须一起看。

## §3 判据（除注明者外，与上一轮逐字相同）

- **成句判据**：**直接 import** 已 FROZEN 的 P1/P2 仪器（`diag_taiji_r2_surface_decode.py`）——
  语料 n 元每字符平均对数似然 ≥ **−7.8**，且长度 ≥ 8 字符，且单字占比 ≤ 0.35。
  **一个字都不重写**；反向对照门（真中文通过率 ≥0.5、打乱拒绝率 ≥0.9）不过则整件拒跑。
- **解码**：`greedy`（P1 已实测四臂最好，本件问训练侧）。
- **M1（本件主门，与上一轮同一个门）**：A 的成句率 ≥ **max(B, C) + 15 pp**，
  **且** A vs 该最好者的逐题配对精确双侧符号检验 **p < 0.05**。
- **K2（本件**修正版**，只对本轮生效）**：
  > **无差别 := |B − A| 的成句率差 < +15 pp 且 配对符号检验 p ≥ 0.05。**
  修正说明：上一轮 `§5.1` 写成 `（B − A）< +15 pp`（**有符号**），导致"B 差 A 20.94 pp"也被判成无差别。
  本件取**双侧**。**上一轮的 `§5.1` 原文不动**、上一轮判定不变（仍按字面：读出侧不记功）。
  报告里**两种读法都列**，便于对照。
- **M2**：本件**不重跑**（CAP D+E 依赖 CAP 评价集，与题面集无关）；上一轮三臂齐平 0/36 的
  地板效应结论保持，并**不得**被本件任何数字覆盖。

## §4 停止线与记账纪律

1. **只看一次**。本件公布读数后，**不再**以任何理由在 M1 上开第三轮——要改判据形态，另立合同。
2. 两轮（CAP-43 与 v3-160）**必须一并引用**；只引好看的哪一轮＝选择性报告。
3. 若 v3 上 M1 仍不成立 ⇒ 记为**"方向一致、两轮都未达线"**并据此收线；
   若 v3 上成立 ⇒ 记为**"CAP-43 未达、v3 达"**，并**必须同时给出两轮的数字**，
   不得写成"M1 成立"而不带限定。
4. 任一步失败/拒跑 ⇒ 如实记录，不重跑、不换题。

## §5 允许与禁止的主张

- 允许：在本条链路、这三份 checkpoint、这两份题面集上，说"M1 达/未达"以及两轮的一致性。
- 禁止：① 任何语言能力/回答能力主张（成句率不是会回答，上一轮 M2 的 0/36 已实证）；
  ② 用本件解除 R2 的 M5 排除状态；③ 用本件改 CAP 冻结线；④ 把 K2 的修正版**回溯**套到上一轮；
  ⑤ 只报 v3 不报 CAP-43（或反之）。
