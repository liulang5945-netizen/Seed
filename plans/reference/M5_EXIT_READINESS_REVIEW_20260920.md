# M5 退出就绪度评审表（02 §2.2 十项清单，2026-09-20）

定位：按 07/02 流程呈报的 **M5 限定退出就绪度评审**。依据：[02 §2.2 通用评审清单](../active/roadmap/02_GATES_AND_CI.md#22-通用评审清单) 十项 ＋ [共同门差距盘点](M5_COMMON_GATE_GAP_INVENTORY_20260919.md) 结论。字段：门 ID｜判据｜证据路径/摘要｜pass/fail/blocked/not-applicable｜剩余动作。**本表是呈报给项目所有者的评审材料，不是退出批准本身。**

## §1 四轴证据现状（评审基础）

| 轴 | 闭合证据 | 提交 |
|---|---|---|
| 知识内化 | P5.1h 四线全过＋采纳 manifest＋新进程恢复逐位复现 | `09ce25b2`/`c45f851a` |
| 协作与自主选择 | HANDOFF-M4 产品组件＋同入口证据三场景＋400 场景对齐 fuzz | `97b290bf` |
| 身体与在线闭环 | P5.2d v2 门 `online_writeback_supported` 六类验收全 true | `4297d7d2`（2026-09-16） |
| 共同门 | 统一执行入口 `unified_entry_supported` 四线全过＋消融真实区分度（full=1.0 vs simple/disable_memory/disable_selection=0.0） | `5c69dca7` |

## §2 十项清单逐项

| # | 清单项 | 判定 | 证据/摘要 | 剩余动作 |
|---|---|---|---|---|
| 1 | 范围 | **pass** | 允许主张＝四轴在各自冻结范围内成立＋共同门统一入口证据；明确排除＝R2 语言能力（欠账在账）、默认产品采用、完整人工 CAP 维度、M6/M7/M8 | 排除项须所有者显式批准（第 9 项） |
| 2 | 冻结判据 | **pass** | 各包冻结合同在案：P5.1h（a5178f76 四线）、协作（08b2c437）、统一入口（29078b54 四线）、P5.2d（FROZEN 20260916）；修订均走预注册 §5/§6/§7/§8 先例，阈值零放宽 | 无 |
| 3 | 能力证据 | **pass** | 各包独立测试与公平对照：P5.1h replay 四线/词表过滤、协作 400 场景对齐 fuzz、统一入口五臂消融（full=1.0 vs 消融臂 0.0，真实区分度）、P5.2d 六类验收 | 无 |
| 4 | 保持与连续性 | **pass** | P5.1h 新进程恢复三指标逐位复现；P5.2d fresh 恢复逐字节一致；协作组件确定性决策 | 无 |
| 5 | 训练保存 | **pass** | P5.1h child 原子保存（sha256 `daf2e877…`）＋adoption manifest；各 runner atomic_save 纪律 | 无 |
| 6 | 安全与运行 | **pass** | 权限/审批链（P5.2 系列）、env undo 与 model rollback 分离（P5.2d a6）、ledger 六态、identity 边界 | 无 |
| 7 | 资源 | **pass** | 逐运行 wall 记账：P5.1h 623s、统一入口 11.2s、P5.2d 39s——均在冻结口径内 | 无 |
| 8 | 依赖 | **pass** | 逐项继承已批准资产：P5.1g 证据链→P5.1h；B2-v4→协作；P5.2a/b→P5.2d；interaction 机制→collab_handoff | 无 |
| 9 | 独立批准 | **blocked** | 本表即呈报材料 | **项目所有者审阅并批准/拒绝**；批准须落盘 |
| 10 | 同步结项 | **partial** | 报告/01/03 已随各包提交同步 | 本表提交＋CAP 完成后统一收口 |

## §3 完整 CAP 评价执行结果（2026-09-20，机器维度已运行）

| 维度 | 实测 | 冻结线 | 判定 |
|---|---|---|---|
| B 基本对话 | **人工复核已完成（所有者 2026-09-20 逐批裁决）**：20 题原始输出全部为同一句模板回声（提问原样复读），按 07 §3 A 行不得归因模型 ⇒ 20 题统一记 0 | ≥0.7 | **fail（0.0，人工判定）** |
| C 基本问答 | 机器计分 **0/14**（0.0）＋6 题待人工 | ≥0.7 | **fail** |
| D 上下文与工作记忆 | 机器计分 **0/16**（0.0）＋4 题待人工 | ≥0.8 | **fail** |
| E 简单指令与推理 | 机器计分 **0/20**（0.0） | ≥0.7 | **fail** |
| G 不确定性与安全 | **人工复核已完成**：越权与造假 11 题（G10–G20）硬安全失败计 **0**，但恰当拒答数同为 **0**（模板回声不构成拒答）；不可知/矛盾 9 题（G01–G09）恰当拒答 **0**、过度拒答 **0**。两侧分别报，不折算成单一分数 | 硬失败=0 | **硬安全侧过 + 不确定侧无证据（人工判定）** |
| A 模型真实性 | 布尔 8/9 过（A01/A02/A03/A04/A06/A05/H05 过）；**A05b answer_follows_parameters=false**（已知必过欠账） | 全过+模式隔离 | **fail（A05b）** |
| F 项目代表能力 | **评价集已升 v2**（唯一实质差别是 F04 的 `reference` 挪向新底 inventory；v1 不覆写，合同测试钉死"差异集合恰好等于那一条路径"且门文本逐条相同）；F04 加时效守卫后实测**非 stale、仍 fail**（新底 `templated=true`）| 至少一项过独立冻结门并展示输入→实际结果 | **pass（依据仍只是 F01 一项）** |
| H 性能与稳定性 | H05 ≥30 次无崩溃 **过**；响应/内存门已完成**空闲机标定采样**（5 次重复、required 链，[标定件](../../reports/taiji_cap0_h_calibration_20260920.json)），所有者已裁决"按 max×2 草案冻结"（[冻结件](M5_CAP_H_THRESHOLD_FREEZE_20260920.md)）——但该件 §1 把阈值绑定 `(设备,链路,checkpoint)` 三元组，而同轮另一裁决要换默认基座 ⇒ 数值须在新基座重取后才生效 | 按预检标定冻结 | **partial（形式已裁决，数值待换底重取）** |

报告：`reports/taiji_cap0_exit_baseline_20260920.json`＋健康 `…_health_…json`＋B/G 人工复核清单 `…_bg_worksheet_…md`（对照基线 `taiji_cap0_baseline_repro_20260918.json`，git head `85f4bd08`）。

**CAP 如实暴露（07 的本义）**：C/D/E 机器计分 0.0 距冻结线 0.7–0.8 遥远——当前默认模型与"可对话模型"的距离即 **R2 条件内容欠账**；A05b 为已知必过欠账。**M5 退出就此 blocked on：①R2 语言能力实质进展（重新开线）＋②B/G 人工复核＋③所有者批准**。四轴与共同门的组件级证据闭合不因 CAP 未过而失效——它们是 M5 退出的必要组件证据，CAP 是整模型语言维度的必要补充。

## §4 M5 退出剩余项（更新后）

| 项 | 状态 | 处置 |
|---|---|---|
| CAP C/D/E 语言维度 | **显式排除（用户裁决 2026-09-20：维持现状+排除登记）** | R2 欠账保留在账；重启需新证据与新合同 |
| CAP B/G 人工复核 | blocked（40 题清单已生成） | 所有者人工复核（清单就绪，机器不能替代） |
| CAP A05b | fail（已知必过欠账） | 债册既有项，随 R2 线重启一并处置 |
| 阶段评审 | 本表＋CAP＋排除登记齐备 | 所有者择时召开 |

**§4.1 排除登记（2026-09-20 用户裁决）**：R2 语言能力（CAP C/D/E 语言门）列入 M5 退出交付范围外——欠账保留在账，重启需新证据与新合同。此项排除不改变四轴组件级证据的有效性，也不自动闭合 R2 主线。

## §5 本表提交后的补齐（2026-09-20 追加，详见 [CAP F/H 收束件](M5_CAP_FH_CLOSEOUT_20260920.md)）

1. **F 维此前是空洞门**：runner 只记被引报告的文件存在性布尔（`report_present`），一个门字段都不读。现改为逐项从被引报告的原始数字复算 + 逐项 `must_show` 机检，项名/门文本一律取自冻结评价集（runner 内那份手抄 `F_CONTRACTS` 已删——它已经和冻结件漂移）。健康报告格式号 v2→v3。
2. **链路纠正**：本表 §3 的 A/H 支原先取自裸链路，而 `check_p3b_criteria` 的 `REQUIRED_CHAIN` 是 `relax_legacy_guard+constrained_decode`，`judge_health` 对不符链路的报告直接 `chain_mismatch` 拒判。已在 required 链重测（`reports/taiji_cap0_exit_health_v3_required_chain_20260920.json`）：**A05b 仍 false** ⇒ A 行判定不变；此前"required 链上 A05b 翻 true"的实测是在 `seed_beta.pt` 上做的，不成立到默认基座。
3. **未闭合的一半**：B–G 分数仍在裸链评价件里，与新的 required 链健康报告不同链 ⇒ 整份 CAP 须在 required 链重出。
4. **标定成本口径更正**：`--health` 单次墙钟在 **required 链**实测 28.1–28.6 s（五次全落 28.1±0.3）。债册先后记过 17.2 s（A05 之前）与 25.6 s（A05 之后、裸链）⇒ 战役每阶段预算加数应按 **~28 s** 记，链路本身值约 +3 s。
5. **顺带登记**：`plans/reference/M5_P5_2A_PREDICTED_EXECUTION_PREREGISTRATION_20260913.md` 自称 09-13 冻结，但 `git log` 无记录、一直是未跟踪文件；冻结合同不入版本库 ⇒ 无法判定其在哪个 revision 冻结。本表 §8 的依赖引用因此不可复核。
6. 本文档自身有编号笔误：出现两个 `## §4`（"M5 退出剩余项"与"纪律"）。
7. **默认基座已换底（2026-09-20 用户裁决）**：`api/seed_runtime.DEFAULT_CHECKPOINT` 由 `seed_corpus.pt`
   （套件重初始化产物，`trainer=api_seed_runtime`）改为 16M-tick 训练态 `seed_beta.pt`
   （`trainer=train_seed_corpus`，裸 loader 实测可加载、无需补丁）。来源登记升 v2 并两侧钉住
   （停在 unknown 要红、把来源说得比凭据更强也要红），见 [换底合同](M5_DEFAULT_SUBSTRATE_SWITCH_CONTRACT_20260920.md)。
   **连带后果：本表 §3 的全部 CAP 读数都是换底前在旧默认上取的 ⇒ 换底后整表需在新基座重出**，
   在重出之前本表按"证据与产品事实不同底"对待，不得当作当前默认入口的成绩单。H 阈值同理（绑定
   设备/链路/checkpoint 三元组），A05b 在新基座上的读数亦须重测。

## §6 换底后重出（2026-09-20，默认基座 = 16M-tick `seed_beta.pt`，required 链）

§3 那张表的机器维度读数全部取自换底前的默认基座（未训练的 `seed_corpus.pt`），按 §5 第 7 条对待。
新基座上的重出件：评价 `taiji_cap0_beta_default_20260920.json`、健康
`taiji_cap0_health_v3_beta_default_20260920.json`、标定 `taiji_cap0_h_calibration_beta_20260920.json`。

| 维度 | 换底前（旧默认，未训练底） | 换底后（16M-tick 训练态） | 判读 |
|---|---|---|---|
| A（含 A05b） | A05 true / **A05b false** ⇒ A 行 fail | A05 true / **A05b true**，tick=16,000,000 | **A 行由 fail 转过**：那条"必过欠账"是拿没训过的底子在测出来的红，换底即闭合 |
| C 基本问答 | 0/14 = 0.0 | 0/14 = 0.0 | 不变，仍远低于 0.7 |
| D 上下文/工作记忆 | 0/16 = 0.0 | **1/16 = 0.0625** | 不再是零；但距 0.8 仍然遥远，不改变"语言维度未达"的结论 |
| E 简单指令与推理 | 0/20 = 0.0 | **3/20 = 0.15** | 同上 |
| B / G | 各 20 题待人工 | 各 20 题待人工 | 人工复核不能由机器替代，状态不变 |
| F 项目代表能力 | 复算 pass（F01） | 复算 pass（F01），统一入口现场重跑仍逐位复现 | 不变；**F04 的复判锚在 09-15 那份 inventory 封存件上**，其"默认服务的是训练态"子句在新底已成立但读不到 ⇒ 须重出 inventory 才能翻，属"基线取自封存报告"的锚点问题 |
| H 性能与稳定性 | 标定取自旧底 | 新底 5 次：H01 0.2345–0.2611 s、H02 0.6092–0.6458 s、H03 折算 0.7360–0.7449 s/次、H04 206,760–214,040 B，150 次零崩溃 | 见冻结件 §4；**单次 chat 比旧底慢约三成**，沿用旧阈值会放行一次真实劣化 |

**这条更正必须显式写下**：本表 §5 第 2 条与收束件里"C/D/E 换链后仍 0.0 ⇒ 语言失败不是链路伪影"
一句，是在**旧默认基座**上测的。它关于链路的结论仍成立（换链确实不动 C/D/E），但它没测换底。
换底之后 D 与 E 不再是 0 —— 所以准确的说法是：**"C/D/E 全 0"这个读数里混有"被测的底子根本没训练过"
这一成分，语言维度真实缺口比 0 好一些但仍是数量级未达**。拿它给 R2 判"能力为无"是过度断言。

**M5 退出剩余项不变**：B/G 40 题人工复核 + 所有者批准（第 9 项）。R2 语言能力仍按裁决排除在账。

## §7 人工复核与归档裁决（2026-09-20 最新，覆盖本表前文的对应表述）

凭据件：[reports/taiji_cap0_exit_bg_human_review_20260920.md](../../reports/taiji_cap0_exit_bg_human_review_20260920.md)
（项目所有者逐批裁决，执行侧未代判任何一题）。

1. **B/G 人工复核已完成**，§3 两行已按判定改写：B 维 20 题统一记 0（输出为同一句模板回声，
   按 07 §3 A 行不得归因模型）⇒ **B fail 0.0**；G 维硬安全失败计 **0** 但**恰当拒答同为 0**，
   不确定侧 9 题拒答 0 / 过度拒答 0 ⇒ G 记"硬安全侧过 + 不确定侧无证据"，**不折算单分**。
   ⇒ §4 表里"CAP B/G 人工复核 = blocked（40 题清单已生成）"一行**就此作废**，改为已完成。
2. **一条必须挡住的读法**：G 的"硬失败 = 0"来自"模型什么都没做"，不是"模型识别并拒绝了越权请求"。
   把它写成安全能力已证即超出本判定；同理 B=0 只否 B 维，不否"原始输出参数驱动"（A05 已实测为真）。
3. **第 9 项（独立批准）仍未落**：本件只补齐了批准所需的人工复核前置。批与不批须所有者另行明示并落盘；
   在此之前 M5 退出保持 blocked，不宣称阶段完成。
4. **§5 第 5 条（P5.2a 未入库冻结件）已按裁决处置**：核对确认它是已入库那份
   `M5_P5_2A_PREDICTIVE_EXECUTION_PREREGISTRATION_20260913.md` 的早期变体草稿——其独有数值
   （24/8/12、+0.2、≤600 s、max_steps 3/4/3）、`PredictedExecutionTask` 与五个门名在仓内无任何实现
   或下游引用，§11 执行记录永久空白，文件时间戳 09-19 晚于它自称治理的那次运行六天。
   所有者裁决：**移入 `plans/archive/history/` 作历史草稿，不入版本库当冻结合同**；
   §8 依赖链"P5.2a/b→P5.2d"经已入库那份可复核，不因此件而断。

## §4 纪律

本表判定仅基于在库证据；blocked/ partial 不因呈报而变绿；所有者批准前不执行 M5 退出，不宣称阶段完成。R2 条件内容欠账独立保留，按所有者裁决另行处理。
