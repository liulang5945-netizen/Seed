# B0 结果整理与推进计划修订（WP-1 … WP-6）

> 日期：2026-09-14（本地时间已过零点；上游证据文档一律标 2026-09-13）。基线提交 `82afff51`。
> **2026-09-15 状态更新**：WP-1 的 D1–D5 / N2 / N1a / N1a-2 **已由用户确认全部取上限档**（见 §2「WP-1 取值已确认」表）。
> 用户随后**解除执行限制**（原话：刚刚我的意思就是先把计划制定好，现在就刚好可以持续推进了）⇒
> 序 1 **已交付**[路线 B 冻结版预注册（binder v1）](M5_B0_ROUTE_B_PREREGISTRATION_FROZEN_20260915.md)；
> 序 0（WP-1.5 扩面）四条出口全过；当前推进**序 2（WP-2 语义冻结）与序 3（WP-3 落地）**。
> 用途：把 B0 十轮测量**整理成一份结果账本**，并把"下一步"从一句话**修订为带出口判据与机时的工程计划**。
> 性质：**执行计划文档，不是预注册**。本文件不冻结任何判据、不放宽任何门、不授权任何训练。
> 顺序仍由[当前推进方案](../active/roadmap/03_CURRENT_EXECUTION.md)决定；本文件是其 §末工作包表的展开。

## 1. 结果账本（截至 `82afff51`，逐条带可核对的产物）

### 1.1 已结项的事实（不需要再测）

| # | 事实 | 证据产物 | 关键数字 |
|---|---|---|---|
| F1 | 当前矩阵下**目标不可达**：任何评分参照的天花板都低于要求 | [设计包](M5_B0_MEASUREMENT_AND_REACHABILITY_AUDIT_20260913.md) / `taiji_b0_measurement_reachability_audit_20260913.json` | 天花板 −0.5 / 0.5 / 0.0 vs 要求 1.65 / 0.65 / 1.15；`oracle_all_cell = oracle_singleton = 1.5` |
| F2 | 组合机制是**优先级回退链**，故**仲裁类机制的协作上界恒 ≤ 0**（可证） | [机制续篇](M5_B0_MECHANISM_AND_TASK_PRECHECK_20260913.md) | 24 个 pair×context **零交错**；现行任务参照上限 0.35 |
| F3 | 表征改进**永远无法单独**支撑协作主张 | 同上 + [F 报告](../../reports/M5_P5_2C_TRIPLE_PRIME_REPRESENTATION_REPAIR_RESULT_20260913.md) | rank 1→2、区分度 1→2 而两未见对收益 −0.5 / 0 |
| F4 | 存在**任务层达标**的复合形态 | [交接探针](M5_B0_HANDOFF_PROBE_RESULT_20260913.md) | `create_and_override`：四单体全败、`k=4/4`、三参照全 feasible、上限 1.85 |
| F5 | 阻塞点**唯一**在机制层：交接触发条件是"绑定失败"而非"无进展" | 同上 | 同一候选六个 pair 全败、`interleaved=0` |
| F6 | `m4_failure_handoff` 使协作主张**可达**（未实施） | [反事实测量](M5_B0_M1_COUNTERFACTUAL_RESULT_20260913.md) | +21 行 / 2 处替换；冻结面 0 回归 / 2 改善；候选面 `interleaved=4/4`、`+2.000 > 1.65`；五败一成 |
| F7 | 该正增益**不是测量伪影** | [伪影审计](M5_B0_M4_ARTIFACT_AUDIT_20260913.md) | 特异性 / 干预真实性 / 机制 lesion / 种子 四项全过；冻结面 2 改善逐步归因为真实交接 |
| F8 | 增益**对表面变化稳健** | [加固轮](M5_B0_M4_HARDENING_RESULT_20260913.md) | 12 context / 6 变体各自 `+2.000`；冻结规则 0/6 为正；5 种子恒定；重跑字节相同 |
| F9 | 增益**不局限于 `create + override` 一种结构**，但承重结构因素只有一个 | [N1 结构空间](M5_B0_STRUCTURE_SPACE_RESULT_20260913.md) | 11 格全测：`create` 行三条语言路由各 `+2.000`（22/22 为正、两成员均执行）；其余 8 格不动；`patch` 行两规则同 `−2.000` |
| F10 | 三种**候选参照**在达标格上全部 feasible | 同上 | `required = 2 ≤ available = 2`、`ceiling_gain = 2.0`、`max_clearable_reference = 1.85` |
| F11 | 新停止原因 `all_members_blocked` 的消费面已清单化且不受 M4 污染 | 加固轮 + N1 + [N2 处置审查](M5_B0_N2_STOP_REASON_DISPOSITION_20260914.md) + [重扫](../../reports/taiji_b0_n2_stop_reason_disposition_20260915.json) | 加固轮清单 **11 个文件**；**2026-09-15 重扫现值为 17 个消费文件 / 10 处判断点（4 live + 6 测试断言；WP-2 落盘时 15/8，WP-3 落地期增 J9/J10）**，`drift.clean = true`；未修复的拦截 `preview_ValueError` 两规则逐格相同（`36 = 36`），N1 实测三格各 24 次（扩面后 72 次）。<br>**已由 WP-2 双向测试收窄的表述**：`language_assessment_unavailable` **并非**两规则相同——它只在 `create__observation` 的冻结侧出现 72 次、M4 侧归零，而那**正是让位修复的对象**，属机制证据而非污染 |
| F12 | 测量工具本身可复现 | 全部 | 每轮两次重跑 JSON **字节相同**；b0 测试组 142 passed / 1 skipped / 0 failed；仓库级 `ruff check .` 通过 |
| F13 | 结构结论**对样本规模稳健**（WP-1.5 扩面，2026-09-15） | [扩面报告](../../reports/taiji_b0_structure_space_probe_wide_20260915.json) / [结果 §10](M5_B0_STRUCTURE_SPACE_RESULT_20260913.md) | 每格 **6 context**（66 总）/ **7 种子偏移**：三格各 `+2.000`、交错 **`0 → 6`**、7/7 偏移为正、`unexplained`/`regress` 皆空、11 格全 `measurable`、两跑字节相同（`b09b3e62…`）、单轮 **12 分钟**。**闭合 L4**；逃生通道 `required` 从 **2/2 → 5/6**（k 维度首次有余量），9 个「格 × 参照」全 `feasible`。**未闭合 L1**：指纹仍 `3 / 11 cells`，独立结构因素仍为 1 ⇒ 只能靠 WP-6 |
| F14 | **被测规则不移动比较参照** ⇒ 取最强参照 `all_singleton_oracle` 作主判据是可信的（2026-09-15） | [结果 §10.4](M5_B0_STRUCTURE_SPACE_RESULT_20260913.md) / 扩面报告逐格 `singleton_success_rates_{frozen,audited}` | 三格声称增益处单体成功率 **全 `0.0` → 全 `0.0`**；存在单体成功的格逐项相同。**这条排除了"早停削弱单体 ⇒ oracle 被压低 ⇒ 增益是伪影"的路径**（M4 在单成员格里会把"失败重试到 `STEP_CAP`"变成"失败即终止"，确有此风险，**实测未发生**）。<br>**已固化**：逐格断言 `frozen == audited` 已写入 `test_the_audited_rule_does_not_move_the_comparison_reference`，任何未来抬高/压低参照的规则改动都会失败 |
| F15 | **落地后两台仪器各自复现了 revision-0 的预测**（WP-3 出口②③，2026-09-15） | [落地结果 §3.3/§3.4](M5_B0_M4_LANDING_RESULT_20260915.md) / [落地后网格报告](../../reports/taiji_b0_structure_space_probe_m4landed_20260915.json) / [落地后反事实报告](../../reports/taiji_b0_m1_counterfactual_m4landed_20260915.json) | 结构空间 11 格与封存扩面报告 **`rows`/`validity`/`verdict`/`outcome_distinctness`/`seed_sweep` 逐字段相同**（三格 `0.000 → +2.000`、交错 `0→6`、7/7 偏移为正、`patch` 行仍 `−2.000`、指纹仍 3/11）；反事实冻结面 **`regressions=0` / `improvements=2`**、候选面 `interleaved=4`、`+2.000` 两序一致 ⇒ 出口②的原文判据在已发布源码上直接成立。**两臂对照的"无回归"是相减结果，不是恒等** |
| F16 | **负结果/仪器缺陷**：落地会把 A/B 仪器的基线臂悄悄变成被测规则本身 | [落地结果 §3.1/§3.2](M5_B0_M4_LANDING_RESULT_20260915.md) / 物证 [`..._m4landed_degenerate_20260915.json`](../../reports/taiji_b0_structure_space_probe_m4landed_degenerate_20260915.json) | 第一次落地后重跑两臂同源 ⇒ `delta ≡ 0`、"`regresses: none`"为同义反复，却被本文误记为"出口②已过"（该表述已作废并留档）。修法：`build_reverted()` 反向还原基线 + 按 `RULE_REVISION` 选臂 + **两臂同一函数即 `SystemExit`** + 报告 `arm_provenance`。同批还抓出**六支仪器默认输出仍指向被封存的 revision-0 文件名**（忘记 `--output` 一次即可毁掉本轮基线），已全部改指新名并由全仓扫描测试把关 |

### 1.2 本轮新立的界限（写清楚，不能靠正结果隐含）

> 编号用 **L**（limit），避免与工作包 B1/B2 混淆。

| # | 界限 | 依据 |
|---|---|---|
| L1 | **11 个可表达格里只有 1 个结构因素产生增益**（存在性前提）；三条语言路由共享同一结果指纹 ⇒ 是"一个因素经由三条路线"，不是三次独立确认 | N1 §7.1 |
| L2 | **跨内容结构在冻结 binder 下不可表达**：`create` 绑定 `goal_files[main_path]`（创建即达标）、`_bind` 恒指 `main_path`（第二路径不可达）⇒ T1/T3 建不出来 | N1 §2 |
| L3 | **纠错交接（T2）不是任务设计能给的**：`predict_episode` 只有一个参数、元组内是同一张量重复 ⇒ 成员之间没有失败证据通道；M4 的失败信息活在组合层（读 step 列表），不在任何成员里 | N1 §2 |
| L4 | 每格 2 context（正增益共 6 个联合必需 context）、3 个种子 ⇒ **规模与种子仍低于加固轮**。<br>**2026-09-15 已闭合**：WP-1.5 扩面到每格 6 context / 7 种子，四条出口全过（见 **F13**）；原句保留为扩面前的状态记录 | N1 §7.2（N1b/N1c）→ [结果 §10](M5_B0_STRUCTURE_SPACE_RESULT_20260913.md) |
| L5 | 反事实门自身的一次失效已修：第一版 `contract_admissible` 绕过 `policy_for`，把四个合同拒绝的格认证为合法 ⇒ 修正**改变了结果**（`create__mismatch` 0.000→+2.000，3 个假矛盾归零）。教训：**合法性判定必须走被测系统真正经过的路径** | N1 §4.2 |
| L6 | 增益是"超过全体单体 oracle 的均值增益"，**不是一次真实协作事件**；`collaboration_achieved` 仍需独立验收 | N1 §1 |

### 1.3 一句话总结

> **H2 协作主张在当前机制下不可达，在 `m4_failure_handoff` 下可达且证据已跨 12 context、6 表面变体、
> 11 个可表达结构与 5/3 个种子复核；未实施、未准入、未晋级。**

## 2. 推进计划（工作包）

约定：每个包给 **前置 / 动作 / 出口判据（可机检）/ 机时 / 需否决策**。
"实测"= 已有计时记录；"外推"= 按实测单位成本线性推算，须先标定。

### WP-1 决策窗口（**2026-09-15 取值已确认 ⇒ 不再阻塞其余工作包；剩"冻结版落盘"待许可**）

> 下表第 2 列写的是当时的**修订建议**，其取值已由下方「WP-1 取值已确认」表**逐项确认**；两者一致，冲突时以确认表为准。

| 项 | 需决策内容 | 决策所需事实（已全部备齐） |
|---|---|---|
| D1 | 估计目标与参照（**2026-09-15 修订建议：主判据用 `all_singleton_oracle`（最强参照），另两种降为诊断列**） | 三种参照在达标格全 feasible；`max_clearable_reference = 1.85`。**口径更正**：实测 `+2.000` 由 `policy_mean_gain_vs_all_singleton_oracle` 算出，**本来就是对着最强参照（`reference_gain 1.5`、`required 1.65`）的增益**，不是对弱参照；F2 的「≤0 上界」只约束冻结规则，M4 因 `interleaved 4/4` 已逃出。**代价**：`+2.000 = ceiling_gain 2.0`，即零余量，任何一格退化就跌破 `required`，故 WP-1.5 扩面必须先跑满档 |
| D2 | 复合任务定义（**2026-09-15 修订建议：取上限档 = `create` 行全部 3 条语言路由入候选面**） | F4 / F9 / L1 / L2。3 条是冻结 binder 下**可表达的全部**（11 格已穷举，正增益只在 create 行三格）⇒ 再往上只能走 N1a，不能在 D2 内部加码。冻结版必须逐字携带 L1：三格共享同一结果指纹 = **1 个独立结构因素 × 3 种表现**，禁止把 `+2.000×3` 读成三重独立证据 |
| D3 | 旧 1.65 判据处置（建议：保留为历史，新判据按闭式推导并声明所需 `k`） | `k > n·(参照+margin)/(成功−B)`，margin=0.15 |
| D4 | 旧 144/88 载体定位（建议：降级为开发回归，新测试面独立冻结） | [路线 B 草案](M5_B0_ROUTE_B_PREREGISTRATION_DRAFT_20260913.md) §3.4 数据隔离条款 |
| **D5** | **是否预注册并落地 `m4_failure_handoff`**（**2026-09-15 修订建议：落地**） | F6–F11；不落地 ⇒ H2 不可达（F2）。上限档 = 落地，WP-1.5 满档扩面**作为强化前置而不是推迟理由**（因 D1 取最强参照时增益正好贴天花板、零余量，扩面是把「2 context / 3 种子」这条 L4 界限在冻结前压掉） |
| N2 | `all_members_blocked` 门禁语义（**2026-09-15 修订建议：落地前逐条预注册，并把它定为一类终局结果而非失败别名**） | F11；消费面 **14 个文件（其中 5 个真判断点**，余 9 个仅记录/聚合；加固轮的 11 为历史值）；每个正增益格出现 **24 次**；`contract_intercepted` 两规则计数完全相同（说明新停止原因没有挤占合同层拦截）。上限档要求它自带归因桶（不得静默折算为 `execution_failed`），否则 **5 个判断点**里任何一个把它当失败都会改变达标计数。审查面与初步处置**已完成**（[N2 处置审查](M5_B0_N2_STOP_REASON_DISPOSITION_20260914.md)），**冻结版预注册仍待 D5** |
| N1a | 是否允许改 binder 以表达跨内容结构（**新议题，不属 M4**）。**2026-09-15 修订建议：开**，但排在 WP-3 之后并打 binder 版本标注 | L2 / L3。**这是唯一能把独立结构因素从 1 抬到 >1 的杠杆**（L1：三个 create 格共享同一结果指纹，属复现非独立）。代价：改 binder = 改冻结面定义，F1–F12 全部结论须在版本标注下重述 ⇒ 先冻结现有证据为「binder v1 基线」再开。T1（串联交接）/ T3（共享状态约束）只需 binder 改动；**T2（纠错交接）另需成员间证据通道**（现签名 `(self, cues) -> tuple[str, ...]`、`arguments_carry_history_length_only` 只携带历史长度），属比 binder 更大的一层改动，若也要上限则单列为 N1a-2 |

- **出口判据**：D1–D5 + N2（+ 可选 N1a）书面确认；据此把[路线 B 草案](M5_B0_ROUTE_B_PREREGISTRATION_DRAFT_20260913.md)转为**冻结版新文件**（草案原文不改）。
- **机时**：0（纯决策）。

#### WP-1 取值已确认（2026-09-15，用户："全部按照上限档来"）

| 项 | 确认取值 | 冻结版必须据此写死的条款 |
|---|---|---|
| D1 | **主判据 = `all_singleton_oracle`（最强参照，`reference_gain 1.5`、`required 1.65`）**；`best_fixed_singleton`(0.5) 与 `best_observed_fixed_pair`(1.0) 降为诊断列 | 参照名与数值、`margin=0.15`、"增益 `+2.000` = `ceiling_gain 2.0` ⇒ 零余量"必须写成显式风险条款 |
| D2 | **`create` 行全部 3 条语言路由**（observation / override / mismatch）入候选面 | 任务规格逐格写明；并逐字携带 L1："1 个独立结构因素 × 3 种表现"，禁止把 `+2.000×3` 读成三重独立证据 |
| D3 | 旧 1.65 **保留为历史**；新判据按 `k > n·(参照+margin)/(成功−B)` **逐格推导并逐格公布 `required`**，不用单一全局阈值 | 每格 `n` / `success` / `B` / `required` / `available` 全部入表 |
| D4 | 旧 144/88 载体**降级为开发回归**；新测试面**独立冻结**，规模随 WP-1.5 上限档 | 数据隔离条款（草案 §3.4）+ 新面不得复用旧载体列 |
| **D5** | **落地 `m4_failure_handoff`**（+21 行 / 2 处替换）；WP-1.5 满档扩面为**冻结前强化前置** | 走 WP-2 → WP-3；WP-3 五条出口全过才算落地；同时冻结优先级定义（风险 1） |
| N2 | **落地前**逐条预注册 **14 个消费文件**（5 个真判断点）的处置 + 双向测试；`all_members_blocked` 定为**一类终局结果**，自带归因桶，不得折算为 `execution_failed` / `goal_reached` / `safety_stops` / "成员无能力" | 以[N2 处置审查](M5_B0_N2_STOP_REASON_DISPOSITION_20260914.md)的 6 条不变式为底稿转冻结版 |
| N1a | **开**，升为正式包 **WP-6**（挂 WP-3 之后、打 `binder_version` 标注） | 现有证据先冻结为 **binder v1 基线**；WP-6 出口含"新格结果指纹必须与 create 行三格不同" |
| N1a-2 | T2 纠错交接所需的**成员间证据通道**单列、单独决策、单独预注册 | 不与 N1a 合并；未决策前不得声称 T2 可表达 |

- **剩余交付（WP-1 出口）**：把上表写成**路线 B 冻结版预注册新文件**（参照 / 任务规格 / 逐格阈值推导 / 数据隔离 / 门禁分层 / 停止条件齐备）。草案原文不改。
- **本轮授权范围**：用户同时明确"**先不开始实际推进**" ⇒ 本文件与相关计划文档只做修订；**不写冻结版、不改 runner、不注册任务、不训练、不 push**。冻结版与 WP-2/WP-3 需另行许可。

### WP-1.5 扩面重跑（**只读；D1 已确认取最强参照 ⇒ 冻结前必过门**）

- **动作**：`probe_taiji_b0_structure_space.py` 加上 `--contexts-per-cell` / `--seed-offsets` 两个旗标后重跑 —— **上限档**：每格 **6 context**、**7 个种子偏移**（`0 101 202 303 404 505 606`）。
- **前置**：**不改默认值**（`CONTEXTS_PER_CELL=2` / `SEED_OFFSETS=(0,101,202)` 原样保留）⇒ **无旗标运行仍逐字节复现 20260913 归档报告**，F12 不被自己的扩面破坏；规模以关键字参数穿到 `probe()` / `validity()` / `measure_cell()` / `seed_sweep()`，payload 发布实参而非常量。不改 runner、不注册任务。已由 4 项新合同测试钉住（默认值、宽规模下 id 不撞、有效性门不放松、退化规模被拒）⇒ 34 passed、ruff 全绿。
- **与 D1 的关系**：D1 已确认取 `all_singleton_oracle`（`required 1.65`），而实测 `+2.000` 正好贴 `ceiling_gain 2.0` ⇒ **零余量**，故本包**不是可选并行项，而是冻结版落盘前的必过门**。
- **状态**：**已完成（2026-09-15）**。报告 [扩面报告](../../reports/taiji_b0_structure_space_probe_wide_20260915.json)（新文件，未覆盖 20260913 版）；结果见[结构空间结果 §10](M5_B0_STRUCTURE_SPACE_RESULT_20260913.md)。
- **出口判据（四条全过）**：
  1. 三格 `create × 语言` 在**每个**偏移仍 `> 0` ⇒ **过**（7/7 偏移 × 3 格，`positive every seed` = 三格）；
  2. `unexplained_changes` 仍为空 ⇒ **过**（`none`；`m4_regresses_cells` 亦为 `none`）；
  3. 两次重跑 JSON 字节相同 ⇒ **过**（两次 sha256 均 `b09b3e62a0679c27bee1f482c2ef7bf69a9908386c3acb349bb9dace2d487b64`）；
  4. 若某偏移转负 ⇒ 如实发布为界限收缩：**未触发**（无偏移转负）。
- **实测数字**：11 格全 `measurable`（无一格靠放宽门换样本）；三格各 `+2.000`、交错 **`0 → 6`**（6 个 context 全部真实交错）；`patch` 行两规则同 `−2.000`；`3 distinct fingerprints / 11 cells`（与扩面前相同 ⇒ **L1 未闭合，只能靠 WP-6**）。
- **附带收获**：逃生通道的 `required_combination_only_contexts` 从 **2/2 变成 5/6** ⇒ **k 维度首次有余量**；9 个「格 × 参照」组合全 `feasible: true`；`ceiling_gain` 与 context 数无关，仍为 `2.0`，故**增益维度仍零余量**（须在冻结版写成显式风险条款）。
- **机时**：**实测 24 分钟 / 两轮**（单轮约 **12 分钟**），低于原 20–30 分钟外推。
- **交付**：新 JSON（新文件名，不覆盖 20260913 版）+ 结果文档追加段。

### WP-2 `all_members_blocked` 门禁语义预注册（**D5=落地的前置**）

- **已完成（2026-09-14，只读审查面）**：[N2 处置审查](M5_B0_N2_STOP_REASON_DISPOSITION_20260914.md) 已交付消费表 —— [扫描脚本](../../scripts/training/audit_taiji_b0_n2_stop_reason_disposition.py) / [处置报告](../../reports/taiji_b0_n2_stop_reason_disposition_20260914.json) / [契约测试](../../tests/taiji_native/test_b0_n2_stop_reason_disposition_contract.py)。**实时消费面 14 个文件**（取代加固轮的 11：新增 N1 探针与两个契约测试；脚本显式排除自身避免自污染），其中 **5 个是真判断点**、9 个仅记录/聚合；判断点只有两类语义（副本一致性、`contract_intercepted:*` 前缀），新原因不带该前缀 ⇒ **不会**被误计为安全拦截。已给出规范定义草案与 6 条强制不变式。
- **剩余动作（需 D5=落地）**：
  1. 把审查面的草案语义转为**冻结版新预注册**（新文件；历史预注册不动），逐条写死"让位成功"与"整体失败"的区分；
  2. 双向测试钉住：新原因**不得**被当作 `goal_reached`，也**不得**被当作"成员无能力"或 `safety_stops`；
  3. 5 个判断点各加断言，并确认副本一致性把 `stop_reason` 纳入 surface 比较。
- **出口判据**：14 行全部有处置（5 个判断点各有断言）+ 语义预注册冻结 + 双向测试通过 + `contract_intercepted` 两规则计数仍相同。
- **机时**：分钟级（纯函数测试）。

### WP-3 落地 `m4_failure_handoff`（**唯一会改行为的包，放最后**）—— **已完成（2026-09-15，五条出口全过）**

> **落地范围核实（2026-09-15，动手前查树）**：`_member_episode` / `bindable` / `stop_reason` **只存在于
> [`scripts/training/eval_taiji_p5_2b_group_causal_corpora_gate.py`](../../scripts/training/eval_taiji_p5_2b_group_causal_corpora_gate.py)**，
> `taiji/` 运行时**没有对应实现**（全仓 grep：`bindable` 在非 `scripts/` 处只出现在计划、测试与报告里）。
> ⇒ 本包"落地 M4"= **把被研究的组合规则正式写进测量 gate**，使反事实探针退化为恒等；
> **它证明的是**"该组合规则改成失败即让位后，交错与增益在仪器里可复现地出现"；
> **它不证明的是**"Taiji 产品机制已支持协作"——01 号能力账本里协作轴仍要求的**「机制正式版本」保持未闭合**，
> 须另立 runtime 设计（不得用 gate 改动冒充）。本包结项时的所有报告与表述必须带这条限定。

- **落地前证据封条（2026-09-15，sha256；出口 5 靠"落地后逐字节重算不变"来验证，不靠口头承诺）**：

  | rule_revision=0 证据文件 | sha256 |
  |---|---|
  | `taiji_p5_2b_group_causal_corpora_20260913.json` | `531cf6efedb029bf67556e90c913ab3148629250772df8ce5d0b44a065f5939e` |
  | `taiji_b0_handoff_feasibility_probe_20260913.json` | `576a0543a9141491842038a03dbf9cf747d2f51075b8693681378755d2e74ec9` |
  | `taiji_b0_m1_counterfactual_20260913.json` | `cb22087e6a2fc9aba08e5e02efa02e54cd4e2eff60aca530d4daf460557b773f` |
  | `taiji_b0_m4_artifact_audit_20260913.json` | `c3d328d50a91deb5623697b3f3d8f25250070c905be55c0fa441231dbdc003b5` |
  | `taiji_b0_m4_hardening_20260913.json` | `79302dc0503dbbe46afb3db0ebade177270477861d9cdf51b8c4b3792f402f56` |
  | `taiji_b0_structure_space_probe_20260913.json` | `03456070213c7657f49435fb67dc086cc6b6821d6dfb0ed5a441f179c486423e` |
  | `taiji_b0_n2_stop_reason_disposition_20260914.json` | `49e965dad23c0932357995f38e2b921ce371b8aa7ea556d6298255f314d1fc58` |

  **必须在落地同一批改动里处理的覆写风险**：gate 的 `DEFAULT_REPORT` 就指向上表第一份（`…_20260913.json`），
  而 `_write_json` 用 `temporary.replace(path)` **原地覆盖**、`main()` 没有 `--output`
  ⇒ **落地后只要跑一次 gate，revision-0 证据就消失**，且新规则的数字会写进名字叫 20260913 的文件里。
  所以本包必须同时引入 `RULE_REVISION` 并让报告路径按版本分流（新默认名 + `--output`），使旧文件**物理上不可被覆写**。

- **动作**：对 `_member_episode` 施加与反事实**完全相同**的 2 处替换（+21 行）：失败即让位（自身步失败 ⇒ 解除阻塞、下一成员上）+ 每成员自身进度 cue 长度。
- **出口判据**（五条，全过才算落地）：
  1. **反事实 ≡ 实现**：探针的 M4 副本与落地后的真 runner 在同一面上**逐 episode 结果相同**；
  2. 冻结面回归：仍 **0 回归 / 2 改善**；
  3. 达标面：`interleaved > 0` 且同参照增益 `> 1.65`；
  4. 全量套件失败集合**不新增**（**基线 2026-09-15 复采：1408 用例 / 0 失败 / 0 错误 / 6 跳过，1063 s**，
     `--junitxml` 后台跑。旧表述"对 27 项基线无新增"作废——那 27 项是本地沙箱批量删除守卫伪影，
     本轮未复现且**非本轮修复成果**，详见[技术债登记册](../active/roadmap/05_TECH_DEBT_REGISTER.md) 2026-09-15 节）；
  5. 历史报告**只加"规则版本"指针，不改任何数字**；旧报告明示"只对旧规则有效"。
- **同时冻结**：优先级定义（风险 1：反序成功 pair 不同 ⇒ `CELL_MEMBER_SETS` 的序必须写入预注册）。

#### 落地后的仪器语义（#16 决策，2026-09-15）

落地把"两条规则的对照"变成了"一条已发布规则"，因此各仪器的判读必须显式改口径，否则同一次改动会被误读为退步或矛盾：

| 仪器 | 落地后的行为 | 规定 |
|---|---|---|
| 反事实探针 | 每个锚点三态：`applied` / `already_applied`（锚点消失**且**替换文本已在位 ⇒ 恒等）/ 否则**照旧硬失败** | `m4_failure_handoff` 现为 `variant_is_identity=true`、`added_lines=0` ⇒ **出口①**；不得靠放宽锚点检查让它"过" |
| 加固扫描（六变体） | `m2`/`m4` 变恒等，`m1a`/`m1b`/`m2a`/`m3` **拒绝构造** | 它是 **`rule_revision=0` 仪器** ⇒ **不重跑**；其归档结论（六变体各自 `+2.000`、冻结规则 0/6）作为 revision-0 证据原样成立 |
| 结构空间探针 | **缺陷已修, 而非改口径**：落地后 baseline 若仍取 `frozen._member_episode`，两臂就成了同一份函数 ⇒ `gain_delta` 恒 `0.000`、`positive_under_frozen` 翻真、"0 回归"变成同义反复 | 改为**按 gate 自报 `RULE_REVISION` 选臂**：`>=1` 时 audited 臂取源码、baseline 臂由 `build_reverted()` 反向还原成 revision 0；**两臂同一函数即 `SystemExit`**；报告新增 `arm_provenance`。⇒ 出口②保持其**两臂**含义，"11 格与反事实报告逐位相同"仍可比（对照封存 `b09b3e62…`） |
| 反事实探针（整体） | `regression_check` 比的是**已封存的冻结矩阵数字**（`_frozen_rates()` 读旧报告），与被测臂无关 ⇒ 落地后**仍是有效的两臂陈述**；只有 `build_counterfactual` 对已落地变体退化 | **可重跑**并直接给出口②"冻结面 0 回归 / 2 改善"；四个未落地变体改为**如实记 `refused` 并继续**（不再让整台仪器在第一个变体上退出）；默认输出改指新文件名 |
| 覆写防护 | 六支 B0 仪器的 `DEFAULT_OUTPUT` 原本仍指向**被封存的** revision-0 文件名，忘记 `--output` 一次即可原地毁掉本轮基线 | 全部改为 `REVISION_0_OUTPUT`（只读）+ 新默认名；由 `test_no_instrument_defaults_its_output_onto_sealed_evidence` 全仓扫描把关（含跨行赋值） |
- **机时**：全量 **1063 s**（2026-09-15 复采，1408 用例）+ 结构空间探针**每轮约 12 分钟**（上限档实测）。

### WP-4 B1 入场（条件化关系表示）

- **前置**：WP-1 出口 + WP-2 + WP-3 + **五件探针在各自所属 `rule_revision` 下全绿**（预检 / 交接 / 加固 = revision-0 归档结论，**不重跑**；反事实与结构空间 = **revision-1 实测复现**，前者比封存的冻结矩阵数字、后者两臂对照逐字段相同，见 WP-3 出口①②③）+ 训练前六门（§9.2）。
  > 2026-09-15 更正：原写法"五件探针全绿"在 M4 落地后**按字面无法满足**——加固扫描为六变体逐一构造反事实，其中四个的锚点已随规则落地而消失，它现在**拒绝运行**。若不改这一条，未来任何一轮都会在这里卡住并误判为"证据不足"。
- **纪律（B0 已证，不得违反）**：仲裁类机制上界恒 ≤0 ⇒ **B1 的表示改进最多回收冻结规则 headroom（实测 ≤ +0.5）**，
  **永远不能单独支撑 H2**；B1 不得把"预测区分度提升/秩提升"报告为协作证据。
- **出口判据**：特征可区分候选、无标签泄漏、恢复通过；对照含无学习 / A 计数 / train-only 简单模型 / lesion / 固定与随机组合 / 可部署单体路由 / 重命名与编号置换。
- **机时**：**未知 ⇒ 先做标定跑测单位成本再定预算**（教训：上一轮按 pytest 印象估时，差点砍掉样本规模）。

### WP-5 下游链（不变）

B2（离线选择 → 真实执行收益）→ P5.2d（在线 outcome 回写）→ P5.1h（真实语料准入）→ runtime 采用
（shadow → opt-in canary → 持久化/恢复 → 默认行为评审）。研究通过不自动授权产品切换。

### WP-6 binder 议题（N1a / N1a-2，**2026-09-15 新增：上限优先 ⇒ 从"可选分支"升为正式包**）

- **动机**：L1/L2 是 H2 主张的**上限瓶颈**——11 格只有 3 个结果指纹、正增益三格同属"存在性前提"这一个结构因素；跨内容结构（T1 串联交接、T3 共享状态约束）在冻结 binder 下不可表达。要把"独立结构因素 > 1"这句话变成可声称的结论，只能改 binder。
- **前置**：WP-3 出口 ⇒ 现有全部证据先冻结为 **binder v1 基线**（带版本标注），再动 binder；不得在 v1 证据未落账前改冻结面。
- **动作**：
  1. **N1a**：只读可行性论证——列出 `_bind` 的 main-path-only 解析需要放宽成什么形态，才能让 T1（成员 A 的产物成为成员 B 的输入路径）与 T3（两成员受同一共享状态约束）可表达；给出最小改动面与受影响的合同条款清单；
  2. 若 1 通过 ⇒ 新预注册（新文件）+ 重跑五件探针，全部报告打 `binder_version` 标注；
  3. **N1a-2（T2 纠错交接）**：需要成员间证据通道（现签名 `(self, cues) -> tuple[str, ...]`，`arguments_carry_history_length_only = true`，成员之间只传递历史长度）。这是比 binder 更大的一层改动，单列、单独决策、单独预注册。
- **出口判据**（可机检）：① v1 基线在改动后仍逐字节复现（旧报告数字不变）；② 新增结构格在 validity 门下 `measurable = true` 且零 `contract_blocked_steps`；③ 新格的结果指纹与 create 行三格**不同**（否则仍是一个因素，如实记为界限未突破）；④ 全量套件失败集合不新增。
- **机时**：**未知 ⇒ 先做标定**（教训同 WP-4：不得按印象估时后砍样本规模）。

### 并行轨（每轮落账，不阻塞主线）

| 轨 | 内容 | 现状 |
|---|---|---|
| T-债 | 27 项 `SystemExit: 1` 全量套件失败：先做可观测性（拿栈）再定位 | 登记在案，**不在主线轮内批量修** |
| T-CI | 远端未查询（`gh` 未认证）⇒ 禁止任何"CI 已绿"表述；本地以命令级基线为准 | 本地 `ruff check .` 通过 |
| T-Git | 不 `gc`/`prune`；`*.corrupt-backup` 与 `E:/Seed-backup-gitstate-*` 不删 | 维持 |
| T-推送 | 远端由用户处理；本轮所有提交仅本地 | 用户决定 |

## 3. 依赖图（最短表述）

```
WP-1 决策【2026-09-15 已确认，全取上限档】
   └─→ 路线 B 冻结版预注册（新文件）══ 许可门 ══╗
WP-1.5 扩面（只读，上限档 6 context / 7 种子，冻结前必过）╣
                                                ╠═→ WP-2 停止原因语义（冻结版 + 双向测试）
                                                ║      └─→ WP-3 落地(+冻结优先级定义, = binder v1 基线)
                                                ║             ├─→ WP-4 B1 ─→ WP-5 B2/在线/采用
                                                ║             └─→ WP-6 binder 议题(N1a → N1a-2)
                                                ╚══ 以上每个"→"都需用户另行许可（本轮未给）
```

## 4. 风险登记（更新后）

| # | 风险 | 状态 |
|---|---|---|
| 1 | 反序成功 pair 不同 ⇒ 须冻结优先级定义 | **已闭合（2026-09-15）**：成员族与序（`CELL_MEMBER_SETS` 的索引规则、`STEP_CAP`、模板）已写进[路线 B 冻结版预注册](M5_B0_ROUTE_B_PREREGISTRATION_FROZEN_20260915.md) §「任务规格」，WP-3 未改动该序（落地后反事实两序均 `interleaved=4`、`+2.000`） |
| 2 | `all_members_blocked` 门禁语义 | **已闭合（2026-09-15）**：[N2 冻结版](M5_B0_N2_STOP_REASON_PREREGISTRATION_FROZEN_20260915.md) I1–I8 + 双向测试；消费面现值 **17 文件 / 10 处判断点**，扫描器 fail-closed、`drift.clean=true`（细则见该文档 §0，计数只能来自重扫） |
| 3 | 规模与种子 | **已闭合（2026-09-15）**：WP-1.5 上限档 6 context / 7 种子跑完，四条出口全过、两跑字节相同（F13）。**残留**：`ceiling_gain` 与 context 数无关（恒 2.0）而实测增益恰为 `+2.000` ⇒ **增益维度仍零余量**，须在冻结版写成显式风险条款；k 维度则已从 2/2 变为 5/6（有余量） |
| 4 | 成员族变化须重跑 | **仍开放**（当前 4 成员族未变） |
| 5 | 结构独立性 | 已回答并带界限（L1/L2/L3）⇒ 跨内容结构走 **WP-6**（N1a → N1a-2），2026-09-15 起为正式包而非可选分支。**落地未改变此界限**（指纹仍 3/11，F15） |
| 6 | 报告自述与代码不同步（P5.2b 曾犯） | 已由 `frozen_attribute_intact` + 双向合同测试抑制。**2026-09-15 扩展**：自述不止"是否实施"一行——`does_not_change`、机制描述串、以及**两臂标签**都要随 `RULE_REVISION` 变；已按 revision 取值并加测试 |
| 7 | **A/B 仪器的基线臂会在改动落地后静默变成被测臂** | 本轮实际踩到（F16）。**常驻守卫**：`build_reverted()` + 两臂同一函数即 `SystemExit` + `arm_provenance` 入报告 + 逐字段对照封存报告。**任何"改完再重跑一次对照"的计划都必须先回答"基线从哪来"** |
| 8 | **续训型 A/B 的一臂会静默重放"状态已经见过的数据"** ⇒ 对照差里混进数据新颖度 | P3b 实际踩到（起点在 `simple_zh_texts.jsonl` 上已消耗前 11,199,800 符号 ⇒ 对照臂 23.69%、实验臂 22.27% 预算在重放；见 [新颖度配对修订](M5_P3B_NOVELTY_MATCHED_ARMS_AMENDMENT_20260915.md)）。**常驻守卫**：已见前缀只能由**检查点血缘**推出（上一环 `tick` 差 + `corpus_fingerprint` 名；上一环若就是同一语料则**拒绝推断**），两臂切到**同一未见过窗口**、由同一支脚本产出，共享前缀超预算 1% 即拒绝产出。**任何续训对照在报告效应前必须先给出各臂重放占比**——"两臂语料彼此只共享 0.6%"不是证据（那是另一个量） |

## 5. 行动清单（取值已定，执行待许可）

> **2026-09-15 排序原则修订**：用户明确"更倾向于上限更高的方案，而不是成本更低的"。故下表一律按**可声称结论的强度**排序，成本只影响执行顺序，不作为否决理由。
>
> **2026-09-15 取值确认**：用户答复"全部按照上限档来，把计划修订好，先不开始实际推进" ⇒ 下表第 2 列**已由建议转为确认取值**（详见 §2 WP-1 取值确认表），但**本轮只修订计划，不启动执行**。

**已确认取值（上限档）：**

| 项 | 确认取值（上限档） | 比低成本档多拿到什么 |
|---|---|---|
| D1 | 主判据 = `all_singleton_oracle`（最强参照，`required 1.65`），另两种降为诊断列 | 低成本档会选可部署对照 0.5 ⇒ 只能说"超过对照"，不能说"超过最强单体" |
| D2 | `create` 行**全部 3 条**语言路由入候选面 | 低成本档只取 1 条 ⇒ 撑不起"不限一种语言路由"的表述 |
| D3 | 旧 1.65 留作历史；新判据按闭式逐格推导并**逐格公布 `required`**（不用单一全局值） | 低成本档沿用全局 1.65 ⇒ 参照未声明时数字会"看起来过了" |
| D4 | 旧 144/88 降级为开发回归；新测试面独立冻结，且规模随 WP-1.5 上限档 | 低成本档在旧载体加列重跑 ⇒ 重开"加列即达标"的伪影通道 |
| **D5** | **落地** `m4_failure_handoff`；WP-1.5 满档扩面作为强化前置 | 低成本档"先扩面再定"会把决策推后；不落地 ⇒ 由 F2，H2 不可达 |
| N2 | 落地前逐条预注册 **14 个消费文件**（其中 **5 个真判断点**）的处置 + 双向测试；`all_members_blocked` 定为**一类终局结果**（自带归因桶，不折算为 `execution_failed`）。审查面与初步处置**已完成**，剩冻结版预注册 | 低成本档落地后补语义 ⇒ 5 个判断点里任一误判都会改达标计数 |
| N1a | **开**，列为 WP-6，排在 WP-3 之后并打 `binder_version` 标注 | 低成本档不开 ⇒ 独立结构因素永远停在 1 个（L1），H2 主张封顶 |
| N1a-2 | T2 纠错交接需成员间证据通道，单列单独决策 | 不开 ⇒ T2 永久不可表达（L3） |

**执行队列（取值已定，逐段需许可）：**

| 序 | 动作 | 性质 | 许可状态 |
|---|---|---|---|
| 0 | WP-1.5 扩面重跑（6 context / 7 种子，连跑两次校验字节） | **只读**，写新报告文件 | **已完成（2026-09-15）**：四条出口全过、两跑字节相同（F13）⇒ **冻结前必过门已通过**；未据此启动任何后续包 |
| 1 | 把 WP-1 取值表写成**路线 B 冻结版预注册新文件** | 文档；**已落盘即冻结** | **已完成（2026-09-15）**：[冻结版（binder v1）](M5_B0_ROUTE_B_PREREGISTRATION_FROZEN_20260915.md) |
| 2 | WP-2：N2 语义冻结版预注册 + 双向测试（**现值 17 文件 / 10 处判断点**） | 文档 + 纯函数测试 + 扫描器处置同步 | **已完成（2026-09-15）**：[N2 冻结版](M5_B0_N2_STOP_REASON_PREREGISTRATION_FROZEN_20260915.md) + [双向测试](../../tests/taiji_native/test_b0_n2_stop_reason_semantics_contract.py) + J8 处置入扫描器，`drift.clean=true`；WP-2 落盘时为 15/8，WP-3 的封条与出口②守卫再加 J9/J10 ⇒ 17/10 |
| 3 | WP-3：落地 `m4_failure_handoff`（+21 行 / 2 处替换）+ 冻结优先级定义 | **改 runner，唯一改行为的包** | **已完成（2026-09-15）**：五条出口全过（①恒等 ②冻结面 0 回归/2 改善 ③交错 6 且 +2.000>1.65 ④全量 1428/0/6 无新增失败 ⑤八份哈希不变）；另修两台仪器的落地语义与六支仪器的默认输出覆写风险，见[落地结果](M5_B0_M4_LANDING_RESULT_20260915.md) |
| 4 | WP-4：B1 入场（五件探针 + 训练前六门） | 训练相关 | 序 3 已全过 ⇒ **等待训练授权** |
| 5 | WP-6：binder 议题（N1a → N1a-2） | 改冻结面定义 | 待序 3（v1 基线先冻结） |
| 并行 | WP-6 步骤 1 的 binder 只读可行性论证、DEBT-I3（revision-1 下缺多修法对照扫描） | 只读/登记 | 排队，不阻塞主线；**27 项 `SystemExit` 草案已随归因更正撤销**（系沙箱批量删除守卫伪影） |
| 序 5 | **本轮主线审计**：WP-1 出口 + WP-2 + WP-3 全部交付并本地提交（不 push） | 记账 | **已完成（2026-09-15）** |

**当前不做**：不改任何**已**冻结的预注册（冻结版只增不改）、不注册任务、不启动训练（须序 3 出口 + 五件探针 + 六门）、不 push、不 `gc`/`prune`。
