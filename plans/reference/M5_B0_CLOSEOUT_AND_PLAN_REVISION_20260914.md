# B0 结果整理与推进计划修订（WP-1 … WP-5）

> 日期：2026-09-14（本地时间已过零点；上游证据文档一律标 2026-09-13）。基线提交 `82afff51`。
> 用途：把 B0 十轮测量**整理成一份结果账本**，并把"下一步"从一句话**修订为带出口判据与机时的工程计划**。
> 性质：**执行计划文档，不是预注册**。本文件不冻结任何判据、不放宽任何门、不授权任何训练。
> 顺序仍由[当前推进方案](../active/roadmap/03_CURRENT_EXECUTION.md)决定；本文件是其 §末工作包表的展开。

## 1. 结果账本（截至 `82afff51`，逐条带可核对的产物）

### 1.1 已结项的事实（不需要再测）

| # | 事实 | 证据产物 | 关键数字 |
|---|---|---|---|
| F1 | 当前矩阵下**目标不可达**：任何评分参照的天花板都低于要求 | [设计包](M5_B0_MEASUREMENT_AND_REACHABILITY_AUDIT_20260913.md) / `taiji_b0_measurement_reachability_audit_20260913.json` | 天花板 −0.5 / 0.5 / 0.0 vs 要求 1.65 / 0.65 / 1.15；`oracle_all_cell = oracle_singleton = 1.5` |
| F2 | 组合机制是**优先级回退链**，故**仲裁类机制的协作上界恒 ≤ 0**（可证） | [机制续篇](M5_B0_MECHANISM_AND_TASK_PRECHECK_20260913.md) | 24 个 pair×context **零交错**；现行任务参照上限 0.35 |
| F3 | 表征改进**永远无法单独**支撑协作主张 | 同上 + [F 报告](../../reports/M5_P5_2C_TRIPLE_PRIME_REPRESENTATION_REPAIR_RESULT_20260913.md) | rank 1→2、区分度 1→2 而两未见对收益 −0.5 / 0 |
| F4 | 存在**任务层达标**的复合形态 | [交接探针](M5_B0_HANDOFF_PROBE_RESULT_20260913.md) | `create_and_override`：四单体全败、`k=4/4`、三参照全 feasible、上限 1.85 |
| F5 | 阻塞点**唯一**在机制层：交接触发条件是"绑定失败"而非"无进展" | 同上 | 同一候选六个 pair 全败、`interleaved=0` |
| F6 | `m4_failure_handoff` 使协作主张**可达**（未实施） | [反事实测量](M5_B0_M1_COUNTERFACTUAL_RESULT_20260913.md) | +21 行 / 2 处替换；冻结面 0 回归 / 2 改善；候选面 `interleaved=4/4`、`+2.000 > 1.65`；五败一成 |
| F7 | 该正增益**不是测量伪影** | [伪影审计](M5_B0_M4_ARTIFACT_AUDIT_20260913.md) | 特异性 / 干预真实性 / 机制 lesion / 种子 四项全过；冻结面 2 改善逐步归因为真实交接 |
| F8 | 增益**对表面变化稳健** | [加固轮](M5_B0_M4_HARDENING_RESULT_20260913.md) | 12 context / 6 变体各自 `+2.000`；冻结规则 0/6 为正；5 种子恒定；重跑字节相同 |
| F9 | 增益**不局限于 `create + override` 一种结构**，但承重结构因素只有一个 | [N1 结构空间](M5_B0_STRUCTURE_SPACE_RESULT_20260913.md) | 11 格全测：`create` 行三条语言路由各 `+2.000`（22/22 为正、两成员均执行）；其余 8 格不动；`patch` 行两规则同 `−2.000` |
| F10 | 三种**候选参照**在达标格上全部 feasible | 同上 | `required = 2 ≤ available = 2`、`ceiling_gain = 2.0`、`max_clearable_reference = 1.85` |
| F11 | 新停止原因 `all_members_blocked` 的消费面已清单化且不受 M4 污染 | 加固轮 + N1 | 11 个文件；`contract_intercepted` 两规则计数相同；N1 实测三格各 24 次 |
| F12 | 测量工具本身可复现 | 全部 | 每轮两次重跑 JSON **字节相同**；b0 测试组 142 passed / 1 skipped / 0 failed；仓库级 `ruff check .` 通过 |

### 1.2 本轮新立的界限（写清楚，不能靠正结果隐含）

> 编号用 **L**（limit），避免与工作包 B1/B2 混淆。

| # | 界限 | 依据 |
|---|---|---|
| L1 | **11 个可表达格里只有 1 个结构因素产生增益**（存在性前提）；三条语言路由共享同一结果指纹 ⇒ 是"一个因素经由三条路线"，不是三次独立确认 | N1 §7.1 |
| L2 | **跨内容结构在冻结 binder 下不可表达**：`create` 绑定 `goal_files[main_path]`（创建即达标）、`_bind` 恒指 `main_path`（第二路径不可达）⇒ T1/T3 建不出来 | N1 §2 |
| L3 | **纠错交接（T2）不是任务设计能给的**：`predict_episode` 只有一个参数、元组内是同一张量重复 ⇒ 成员之间没有失败证据通道；M4 的失败信息活在组合层（读 step 列表），不在任何成员里 | N1 §2 |
| L4 | 每格 2 context（正增益共 6 个联合必需 context）、3 个种子 ⇒ **规模与种子仍低于加固轮** | N1 §7.2（N1b/N1c） |
| L5 | 反事实门自身的一次失效已修：第一版 `contract_admissible` 绕过 `policy_for`，把四个合同拒绝的格认证为合法 ⇒ 修正**改变了结果**（`create__mismatch` 0.000→+2.000，3 个假矛盾归零）。教训：**合法性判定必须走被测系统真正经过的路径** | N1 §4.2 |
| L6 | 增益是"超过全体单体 oracle 的均值增益"，**不是一次真实协作事件**；`collaboration_achieved` 仍需独立验收 | N1 §1 |

### 1.3 一句话总结

> **H2 协作主张在当前机制下不可达，在 `m4_failure_handoff` 下可达且证据已跨 12 context、6 表面变体、
> 11 个可表达结构与 5/3 个种子复核；未实施、未准入、未晋级。**

## 2. 推进计划（工作包）

约定：每个包给 **前置 / 动作 / 出口判据（可机检）/ 机时 / 需否决策**。
"实测"= 已有计时记录；"外推"= 按实测单位成本线性推算，须先标定。

### WP-1 决策窗口（**阻塞其余全部工作包**）

| 项 | 需决策内容 | 决策所需事实（已全部备齐） |
|---|---|---|
| D1 | 估计目标与参照（建议：主判据用可部署对照，oracle 仅作诊断上界） | 三种参照在达标格全 feasible；`max_clearable_reference = 1.85` |
| D2 | 复合任务定义（建议：`create` 行 × 1–3 条语言路由入候选面） | F4 / F9 / L1 / L2 |
| D3 | 旧 1.65 判据处置（建议：保留为历史，新判据按闭式推导并声明所需 `k`） | `k > n·(参照+margin)/(成功−B)`，margin=0.15 |
| D4 | 旧 144/88 载体定位（建议：降级为开发回归，新测试面独立冻结） | [路线 B 草案](M5_B0_ROUTE_B_PREREGISTRATION_DRAFT_20260913.md) §3.4 数据隔离条款 |
| **D5** | **是否预注册并落地 `m4_failure_handoff`** | F6–F11；不落地 ⇒ H2 不可达（F2） |
| N2 | `all_members_blocked` 门禁语义 | F11；11 文件清单 |
| N1a | 是否允许改 binder 以表达跨内容结构（**新议题，不属 M4**） | L2 / L3 |

- **出口判据**：D1–D5 + N2（+ 可选 N1a）书面确认；据此把[路线 B 草案](M5_B0_ROUTE_B_PREREGISTRATION_DRAFT_20260913.md)转为**冻结版新文件**（草案原文不改）。
- **机时**：0（纯决策）。

### WP-1.5 扩面重跑（**无需决策、只读，可与 WP-1 并行**）

- **动作**：`probe_taiji_b0_structure_space.py` 改参重跑 —— `CONTEXTS_PER_CELL` 2→4~6、`SEED_OFFSETS` 3→5。
- **前置**：无代码改动（两个常量），不改 runner、不注册任务。
- **出口判据**：
  1. 三格 `create × 语言` 在**每个**偏移仍 `> 0`；
  2. `unexplained_changes` 仍为空；
  3. 两次重跑 JSON 字节相同；
  4. 若某偏移转负 ⇒ **如实发布为界限收缩**，不删偏移、不改阈值。
- **机时**：**外推** ≈12–15 分钟（当前规模实测 3.5 分钟 / 3 种子 / 22 context ⇒ 约 2.5–4×）。
- **交付**：新 JSON（新文件名，不覆盖 20260913 版）+ 结果文档追加段。

### WP-2 `all_members_blocked` 门禁语义预注册（**D5=落地的前置**）

- **动作**：
  1. 起草"**11 文件停止原因消费表**"骨架（只读，**现在就能做**）：每文件列出它如何分类停止原因（计入失败 / 计入成本 / 触发回滚 / 仅记录），并标出哪些分类会因新原因而改变行为；
  2. 就"让位成功"与"整体失败"的区分**写新预注册**（新文件；历史预注册不动）；
  3. 双向测试钉住：新原因**不得**被当作 `goal_reached`，也**不得**被当作"成员无能力"。
- **出口判据**：11 行全部有处置 + 语义预注册冻结 + 双向测试通过。
- **机时**：分钟级（纯函数测试）。

### WP-3 落地 `m4_failure_handoff`（**唯一会改行为的包，放最后**）

- **动作**：对 `_member_episode` 施加与反事实**完全相同**的 2 处替换（+21 行）：失败即让位（自身步失败 ⇒ 解除阻塞、下一成员上）+ 每成员自身进度 cue 长度。
- **出口判据**（五条，全过才算落地）：
  1. **反事实 ≡ 实现**：探针的 M4 副本与落地后的真 runner 在同一面上**逐 episode 结果相同**；
  2. 冻结面回归：仍 **0 回归 / 2 改善**；
  3. 达标面：`interleaved > 0` 且同参照增益 `> 1.65`；
  4. 全量套件失败集合**不新增**（基线 851 / 27 失败 / 0 错误 / 1 跳过，938 s，`--junitxml` 后台跑）；
  5. 历史报告**只加"规则版本"指针，不改任何数字**；旧报告明示"只对旧规则有效"。
- **同时冻结**：优先级定义（风险 1：反序成功 pair 不同 ⇒ `CELL_MEMBER_SETS` 的序必须写入预注册）。
- **机时**：938 秒全量（实测）+ 各探针若干分钟。

### WP-4 B1 入场（条件化关系表示）

- **前置**：WP-1 出口 + WP-2 + WP-3 + **五件探针全绿**（预检 / 交接 / 反事实 / 加固 / 结构空间）+ 训练前六门（§9.2）。
- **纪律（B0 已证，不得违反）**：仲裁类机制上界恒 ≤0 ⇒ **B1 的表示改进最多回收冻结规则 headroom（实测 ≤ +0.5）**，
  **永远不能单独支撑 H2**；B1 不得把"预测区分度提升/秩提升"报告为协作证据。
- **出口判据**：特征可区分候选、无标签泄漏、恢复通过；对照含无学习 / A 计数 / train-only 简单模型 / lesion / 固定与随机组合 / 可部署单体路由 / 重命名与编号置换。
- **机时**：**未知 ⇒ 先做标定跑测单位成本再定预算**（教训：上一轮按 pytest 印象估时，差点砍掉样本规模）。

### WP-5 下游链（不变）

B2（离线选择 → 真实执行收益）→ P5.2d（在线 outcome 回写）→ P5.1h（真实语料准入）→ runtime 采用
（shadow → opt-in canary → 持久化/恢复 → 默认行为评审）。研究通过不自动授权产品切换。

### 并行轨（每轮落账，不阻塞主线）

| 轨 | 内容 | 现状 |
|---|---|---|
| T-债 | 27 项 `SystemExit: 1` 全量套件失败：先做可观测性（拿栈）再定位 | 登记在案，**不在主线轮内批量修** |
| T-CI | 远端未查询（`gh` 未认证）⇒ 禁止任何"CI 已绿"表述；本地以命令级基线为准 | 本地 `ruff check .` 通过 |
| T-Git | 不 `gc`/`prune`；`*.corrupt-backup` 与 `E:/Seed-backup-gitstate-*` 不删 | 维持 |
| T-推送 | 远端由用户处理；本轮所有提交仅本地 | 用户决定 |

## 3. 依赖图（最短表述）

```
WP-1 决策 ─┬─→ WP-2 停止原因语义 ─→ WP-3 落地(+冻结优先级定义) ─→ WP-4 B1 ─→ WP-5 B2/在线/采用
           ├─→ (N1a 议题：改 binder？) ─→ 跨内容结构实验
           └─→ D3/D4 ─→ 路线 B 冻结版预注册
WP-1.5 扩面（只读）──────────────┘（随时可插入，不改任何判据）
```

## 4. 风险登记（更新后）

| # | 风险 | 状态 |
|---|---|---|
| 1 | 反序成功 pair 不同 ⇒ 须冻结优先级定义 | **仍开放**，随 WP-3 一并冻结 |
| 2 | `all_members_blocked` 门禁语义 | **仍开放** ⇒ WP-2（骨架可立即起草） |
| 3 | 规模与种子 | 部分闭合：加固轮 12/5；N1 为每格 2 context / 3 种子 ⇒ WP-1.5 |
| 4 | 成员族变化须重跑 | **仍开放**（当前 4 成员族未变） |
| 5 | 结构独立性 | 已回答并带界限（L1/L2/L3）⇒ 若要跨内容结构须走 N1a |
| 6 | 报告自述与代码不同步（P5.2b 曾犯） | 已由 `frozen_attribute_intact` + 双向合同测试抑制 |

## 5. 立即行动清单（按"要不要决策"分栏）

**要用户给：** D1、D2、D3、D4、**D5**、N2 语义处置、N1a 是否开议题。
**我给（不需决策，随时可做）：** WP-1.5 扩面重跑；WP-2 的 11 文件消费表骨架；27 项失败的可观测性改造草案。
**明确不做：** 不落地 M4、不改冻结预注册、不注册任务、不启动训练、不 push。
