# 项目阶段收束：截至 2026-09-17

依据：用户要求“对项目进行收束”。本次是阶段封账和停止扩展，不是M5完成、架构采用或正式发布。收束起点为Git `6d12d7a4`；以当前磁盘/报告为准，不以对话旧快照覆盖后续工作。

## 一、统一结论

项目仍在M5，R2语言能力没有通过既定用户验收线。保留已实现、已验证的代码和训练资产；停止新增机制、扩大语料、增加epoch或开启新候选。VISION不作为前置，也不启动其开发。Mini继续后置。M4成长、M6采用、M7发布、M8设备优化均不因本轮收束而晋级。

| 工作包 | 收束状态 | 能说什么 / 不能说什么 |
|---|---|---|
| H3.7 | 负结果结项 | 原三seed未过门，final未评分；不恢复同质训练 |
| H3.7B一致性修复 | 工程和有界dev验证已结项 | prior、混合信用、目标格式/恢复修复保留；byte对齐几何未表现dev优势，不等于语言能力成立 |
| H3.8 v1/v2 | 两轮matched dev均 `workspace_unused`，按预承诺停止该族投入 | 具体预算与任务下未过门；不能把负结果或源码推断写成所有相关架构必然失败 |
| P3b-v2数据 | 80行（40/20/20）及生成/检查工具保留 | 是合成研究语料，不是泛化能力证据；各类digest必须区分 |
| P3b-v2 pilot | 本次收束中断，尚无完整结果报告 | 旧checkpoint存在，但不能据此宣布当前run完成、J1–J6通过或失败 |
| CI/验收 | 局部证据与未完成项分列 | 不发布当前HEAD全量绿灯，不交付Mini或默认采用 |

## 二、P3b-v2磁盘与进程封账

2026-09-17约15:29（北京时间）只读核实后，仅停止PID `25624`：
`scripts/training/train_taiji_r2_aligned.py`，指定P3b-v2数据、2 epochs、每epoch最多12episodes、300k参数、`--defer-final`。未停止其他Python进程。

该进程启动于15:25:16；日志为空，progress与最终报告不存在。磁盘checkpoint修改时间为15:21:19，早于本进程，故属于先前已保存产物，不是本次被停止进程的完成凭证。进程内未落盘状态未保留；磁盘文件未删除、未覆盖。当前没有创建自动接续任务。

保存文件：`reports/p3b_v2_pilot_20260917/checkpoint.pt`，16,520,045 bytes。

- 文件SHA256：`b14ae9b0048172b56b6ed20893457607f036ee3a2fca1e393797907ee44242c9`（停止前后相同）。
- payload代码：`d74fd9fb9b62e58c9b10af561cb312d9a230bc8d`。
- payload内部digest：`81aca12f84663918177edb27130e68abfebd8f00c2dc955b7dffcfd02a6e68c6`（读取字段，非本轮重算恢复证明）。
- payload：24 episodes / 1080 byte updates；目标实际为 `h3_7_factorized_response_chunks`，不是byte-aligned格式。
- 当前语料加载digest与checkpoint字段一致：`f7a5841860f3059147557564cc99cca05b3f71f71155c95e1db26a129c4446a1`。
- 当前语料文件SHA256：`5ceaad40532de5a10178f23abcee68970744d0efefd19bd341f1d0660a54f9e2`。

这些事实不能代替恢复后的能力评估。预注册/生成报告中的历史digest标记与当前加载digest不能混用；旧格式目标也偏离“沿用byte-aligned目标”的文字约定，必须先解释血缘和合同偏差，再决定报告是否可比较。未擅自改写冻结阈值或历史报告。

## 三、验证与未闭合项

本任务执行时的修复测试18项通过，覆盖缓存prior、混合信用数值差分、目标分段、旧/新encoder保存恢复。此计数绑定当时修复代码，不是后续`6d12d7a4`全量CI。较大范围的language-alignment测试运行以临时目录访问拒绝及pytest清理异常结束，不记为通过；未为收束重跑长测试。格式器曾在输出格式化结果后未退出，已结束本任务的调用；ruff检查通过，不冒称完整格式/CI全绿。

尚未闭合：P3b-v2当前run与旧checkpoint关系、目标格式与冻结合同偏差、正式报告缺失、原始输出与C/D/E同链路评价、B/G人工检查和其他未测维度、当前HEAD完整CI。所有未测保持未测，不用代理loss抵扣。

## 四、保留与清理边界

保留：源码、冻结合同、成功与失败报告、现有checkpoint、数据和原始日志。清理的是导航里的过时“下一步”和自动扩展指令，不删失败证据、不批量清理临时目录、不改用户其他提交、不自动push。旧计划台账作为历史阅读，不再驱动执行。

## 五、唯一下一步（待用户重新启动，不自动执行）

对现有P3b-v2磁盘checkpoint做一次**无训练的身份、目标配置、恢复和评测入口核验**，先判定其是否满足已冻结合同的可比较条件；不满足则按“协议偏差、结果不可判”结项，不为了凑结论重新训练。只有合同一致才进入既定只读评价。此次收束到此，不顺带启动该步骤。

证据入口：[H3.7B归档](../../reports/r2_h3_7b_validation/)、[H3.8 v1](../../reports/r2_h3_8_matched_dev_20260917.json)、[H3.8 v2](../../reports/r2_h3_8_matched_dev_v2_20260917.json)、[P3b-v2冻结预注册](M5_P3B_V2_GOAL_ALIGNED_PREREGISTRATION_FROZEN_20260917.md)。
