# Seed / Taiji 当前详细推进方案

更新：2026-09-17。本文是唯一执行顺序来源；[01](01_SCOPE_AND_PHASES.md)管大阶段，[02](02_GATES_AND_CI.md)管证据与晋级，[07](07_MINI_MODEL_DELIVERY.md)管用户验收。[根需求](../TAIJI_CORE_REQUIREMENTS.md)与[架构合同](../TAIJI_NATIVE_ARCHITECTURE_V1.md)定义目的、职责和规则层级。

## 0. 当前裁决与唯一下一步

**执行仍暂停。** 本轮讨论转向“能力如何形成”：任务经历、可学习表示、训练目标、跨时间信用与运行期适应。没有恢复训练、重评封存final、切换默认模型或批准新预算。H3.7B已结项，H3.8 v1/v2停止投入；后继提交36a7b861已加入P3b-v2报告及停止结案，不能继续写“完整报告不存在”。该报告的目标配置与结案文字存在偏差，限定读法见§4。M5/R2未晋级，Mini后置。

**唯一下一步：完成R2原生语言能力的下一候选方案决策。** 围绕条件内容、上下文与自由生成，提出一个有明确学习路径、判别实验和预算的候选变更规格，供采用/否决；详见§5。旧方案不自动续训，Workbench最小任务族不替代主线、不成为新前置。

**路线纠正**：用户指出“继续主线”与“走最小任务族”发生冲突。此前把能力形成讨论直接升级为执行队首，是助手混淆了设计建议与路线批准；本次撤回该升格。当前工作主线仍为M5中的R2原生语言能力，能力形成是需要解决的问题，小型任务只是可能的验证方法。保留VISION设计不等于采用或排期；本次澄清不授予训练权限。

本轮已只读核对后继报告，不再把重复P3b结案核验作为能力设计的队首。历史中断记录保留，报告后继版本与中断进程不混同；配置偏差限制相应归因，不通过重训、重评或改阈值修饰结论，也不补发J1–J6全通过。

**2026-09-17 追加（§5 的唯一候选已否决）**：按 §5 产出的候选「**生成态训练**（train/inference 分布对齐）」
已实现、跑完两臂 pilot，并按 §4 停止线第 2 条**否决** —— 两臂 dev `exact` 均 `0/16`
（同语料、同预算、同种子，只差 φ_max；见 [H-GEN 结项](../../reference/M5_R2_HGEN_CLOSURE_REJECTED_20260917.md)）。

**净收益与剩余空间**：把「**生成状态**」从"候选充分根因"中**划掉**。剩余待区分项优先级重排为：
**① 训练目标语义 > ② 信用粒度 > ③ 表示 > ④ 数据规模/课程 > ⑤ 评估入口**。
关键观察：两臂生成**完全相同**的字节串且与参考不符（`蓝` vs `不是白`/`不是黄`）
⇒ 模型没学到条件性，指向**训练信号没有把"回答问题"区分出来**，而不是前向/生成路径。

⇒ **本页的唯一下一步回到决策点**：下一候选应围绕 **① 训练目标语义** 立项，
**等用户确定方向**（§5 出口：不能形成可辨识假设时说明缺哪项证据并停下，不自行改换主线）。

**2026-09-17 追加之二（H-OBJ 亦否决，且出现关键模式）**：第二个候选「**上下文对比目标**」
（保留逐字节 CE、只加一项"正确 prefix 须胜过打乱 prefix"的对比损失）已实现并跑完两臂，
同样按停止线第 2 条**否决** —— 两臂 dev `exact` 均 `0/16`。**对比项确实生效**
（treatment mean_loss 系统性更高、`margin_gap` 有非零读数），但 `margin_gap` **始终 ≤ 0**
（正确配对的序列似然从未超过打乱配对）。见 [H-OBJ 结项](../../reference/M5_R2_HOBJ_CLOSURE_REJECTED_20260917.md)。

**关键模式（直接影响下一步）**：H-GEN 与 H-OBJ **连续两次**在**同一预算**
（2 epochs × 12 episodes）下，**任何目标层改动都完全不移动任何指标** ——
甚至两臂的 train accuracy 完全相同（`0.6265`）。
⇒ 「**数据规模/课程**」在未分离因素表中的优先级**显著上升**：
"改动无效"更可能是"**预算不足以让任何改动表达出来**"，而不是"该因素不是根因"。

⇒ **本文的唯一下一步更新为**：下一候选应为**预算判别实验**（**不是**再加机制或目标）——
同一对照臂在**放大预算**下的表现；若对照臂**自身**指标开始移动，
则此前两次否决都应**重新解读为"预算不足"**。**方向仍需用户确定**。

**2026-09-17 追加之三（预算判别已完成：**预算为首要限制**，用既有数据，未重跑）**：
对照 H3.8 v2 的既有读数（**30 纪元 × 25 episode**，即 **12.5 ×** 本轮 pilot 预算）：

| 预算 | 来源 / 臂 | train_fit | dev forced | **dev `exact`** |
|---|---|---|---|---|
| **2 × 12** | 本轮 pilot（λ=0） | 0.6265 | 0.4717 | **0.0000** |
| **30 × 25** | H3.8 v2 / baseline | 0.8494 / 0.8554 / 0.8434 | 0.5755 / 0.5660 / 0.6604 | **0 / 0.125 / 0.250** |
| **30 × 25** | H3.8 v2 / workspace | 0.8554 / 0.8554 / 0.8012 | 0.6321 / 0.5849 / 0.6509 | **0 / 0 / 0.0625** |

⇒ **12.5 × 预算下，dev `exact` 从恒 0 变为非零** ⇒ **预算是首要限制因素**（见
[预算判别](../../reference/M5_R2_BUDGET_DISCRIMINATION_20260917.md)）。
⇒ **H-GEN 与 H-OBJ 的否决据此重新解读**：不是"假设被否证"，而是"**在该预算下改动无法表达**"。
⇒ **H3.8 的 `workspace_unused` 判定不受影响**（baseline 臂 `exact` 不劣于 workspace 臂）。

⇒ **本文的唯一下一步更新为**：**在 30 × 25 档上重跑 H-OBJ（λ=1 vs λ=0）** ——
因为该目标现在**有机会表达出来**；这才是对它的**有效**否决或支持。
成本量级：H3.8 六次 arm-seed 运行合计 **76.7 秒** ⇒ **分钟级，不是小时级**。
**方向仍需用户确定**。

**2026-09-17 追加之四（H-OBJ 在**有效预算**下重跑：否决，但**失效形态升级**）**：
把两臂实验从 2 × 12 搬到 **30 × 25 × 3 seeds** 后 —— control 臂**能**产出非零 `exact`
（`0.25` / `0.0625`），treatment 臂**全 0**：

- **H-OBJ 否决**（λ=1 不优于 λ=0，`dev_exact_mean` 0.0000 vs 0.1042）；
- **但 seed 20260917 的 `margin_gap = +4.6867`** ⇒ **"同一回答在正确 prefix 下的似然显著更高"
  这一依赖被真正建立**，且该臂 `train_acc` 更高（0.9277 > 0.8675）——
  **而它的 dev `exact` 仍为 0** ⇒ **"依赖已建立、不转化为产出"**，
  正是候选规格 §5 预设的**最强**失效形态。

⇒ 据此**排除**「**目标未显式要求使用 context**」（奖励确实被吃进去了）。
结合 **H-GEN 已在 30×25 有效预算下重验并排除 exposure bias**（生成态训练使 dev `exact`
从非零变为全 0，且对照臂与 H-OBJ 的 λ=0 臂逐位一致 ⇒ 仪器可复现；见其结项 §6），
剩余候选按优先级：
**① 前向/生成路径的读出力**（依赖存在但读不出） > **② 容量/表示** > **③ 课程/数据**。
详见 [H-OBJ 有预算结项](../../reference/M5_R2_HOBJ_BUDGETED_CLOSURE_20260917.md)。
**方向仍需用户确定**（§5 出口：不自行改换主线）。

**2026-09-17 追加之五（只读诊断：缺口精确定位到「材料 → 第一个回答字节」）**：
按"① 前向/生成路径的读出力"做了**只读**诊断（未训练、未改 checkpoint），
对 λ=0 checkpoint（seed 20260919，30×25）逐条 dev 对比 **teacher-forced 路径**与**生成路径**：

| 量 | 值 |
|---|---|
| TF 命中率 | **0.5943** |
| 生成 exact 率 | **0.0000** |
| **TF 首错位置** | **`pos_0`：16/16** |
| **生成首分歧位置** | **`pos_0`：16/16** |

⇒ **16 条 dev 全部在 response 的第一个字节就错**，且生成路径在同一位置分歧 ——
**不是"生成过程漂移"**（若漂移，首错应分散在后面），也**不是"读出段失灵"**
（一旦喂真字节，后续命中 59%）。

⇒ **缺口精确定位在「prefix 状态 → 第一个回答字节」的条件化起点**：模型在"该开始回答了"
的时刻不知道该从材料里取什么，于是输出训练集边际众数（`蓝`）。
与 H-OBJ 的 `margin_gap=+4.69`（seed 917）合并解读：**"正确材料整体更好"是序列级判断
（已建立），"第一个字节该是什么"是位置级条件化（未建立）—— 缺口在两者之间**。

⇒ 剩余候选的具体形式（**需用户决策**，按侵入性从小到大）：
**(a) 诊断深化**（只读，判 exact=0.25 的 seed 917 是否"碰巧对"）；
**(b) 训练目标**（boundary 后首字节的位置级加权 CE）；
**(c) 架构**（显式"回答起点"状态/标记）。
详见 [读出诊断](../../reference/M5_R2_READOUT_DIAGNOSIS_20260917.md)。

## 1. 核心问题与本轮解法

当前最关键缺口不是“模块数量不足”或“必须先找到另一种算法”，而是：

1. **语义没有统一消费证据**：感知、记忆、世界信念、目标/计划可能同时存在，却未证实在同一模型、任务和时间状态下共同参与决策。
2. **行为与学习没有统一证据**：输出变化、loss下降、局部gate通过，不能证明实际任务改善，也不能证明任务错误已有效分配到负责表示/预测/选择的状态或参数。
3. **规则没有正确分层**：长期能力目标、当前工程合同、实验方法与冻结阈值混写，产生“原型先满足成熟目标”和“历史实现永远不可改”的循环前置；实际接口与失败处理反而缺定义。

本轮已完成的治理/设计修复：

| 问题 | 本次落地 | 尚未完成、不可冒称 |
|---|---|---|
| 核心目标分散 | 以同任务的记忆→世界预测→规划→实际反馈作为统一能力主张的验收链；语言仍是必达能力 | 统一模型能力本身尚未获证 |
| 接口/学习责任含糊 | 架构§4.6–4.7与§6.3明确共同状态、消费/失效、执行生命周期和学习权限 | 默认 chat 尚未据此完成集成；新字段/迁移未自动实现 |
| 规则僵硬与缺失 | 根需求区分成熟目标与原型；架构§0.2/§8.5建立P/C/E/X层级、例外、状态、复审与责任 | 不是放宽已冻结门，也不是所有规则都已有机器守卫 |
| 执行指令互相覆盖 | 旧逐轮“下一步”移出活动页，保留本页唯一队首与条件出口 | 不删除历史证据，不恢复暂停 |

## 2. 继承成果与不能外推的结论

详细原始证据见[结果与结论总览](../../reference/PROJECT_RESULTS_AND_CONCLUSIONS_20260917.md)，逐轮材料见[旧执行计划快照](../../archive/history/EXECUTION_BEFORE_RULE_REPAIR_20260917.md)。

| 对象 | 当前可继承 | 不可外推 |
|---|---|---|
| M0–M3、K轴 | 基础模型、所有权/恢复/执行合同；K于9月12日在五类合成载体、opt-in范围限定晋级 | 不是M4成长完成、真实语料准入或默认产品采用 |
| B2-v4 | 冻结create族内协作与相关损伤证据 | 不是独立内容结构、同产品入口或协作轴整体晋级 |
| R2/H3.7B | 原生生成路径、目标/信用一致性修复、工程恢复证据 | 字符生成和工程修复不是基本问答通过 |
| H3.8 v1/v2 | 两轮按协议停止，保留全部负结果 | workspace_unused是协议类别；不能推出机制处处无作用或整个架构必然失败 |
| CAP-0、原P3b | 有绑定身份的实际输出；C/D/E有明确缺口；P3b分布效应只有一个共同tick | 不将某快照的0分说成所有模型能力完全不存在；人工项和A/F/H缺测不补分 |
| P5.2d | 仪器与恢复有局部进展；A1真实后测收益仍缺 | 报告 supported/布尔true不能替代实际收益；不自动重开在线支线 |
| P3b-v2 | 后继报告记录训练/评价完成、dev逐字节预测改善、自由生成exact为0/20；已提交停止结案 | 原中断事实不抹去；实际目标为旧chunks而非结案文字的byte-aligned，指定变体比较不可据此下结论；不升格为唯一架构根因 |

事实、解释、决策分别记录：冻结prefix仍可向下游提供条件信息；H3.9旧“回答监督不存在所以失败必然”不作为已证根因。H3.8 v1逐seed优势更正为1/3；数值修订不改变原协议停止裁决。

## 3. 主线如何收敛，而不是再开几条支线

**项目目标**：Taiji自己的状态和参数在同一实际任务中形成可用内容、作出选择、承受结果并改进后续行为。**当前工作主线**：R2原生语言能力——根据问题、材料与上下文自由生成正确短回答。全项目联合能力目标与当前工作包不混同；R2通过不自动完成M5，也不需要先立项全部世界模型/规划工作才允许推进R2。

原R0–R6是责任分类，不是七条同时开工的主线：

| 分类 | 在单一工作包中的职责 | 何时才成为实施前置 |
|---|---|---|
| R0 结案/证据 | 关闭存量身份、协议和结果解释 | §4已记录后继报告和归因限制；不重复全仓审计、不再占设计队首 |
| R1 可信底座 | 加载、保存、数据隔离、实际相关CI | 确实阻塞该包且有复现时；不先清完全部技术债 |
| R2 内容与语言 | 当前唯一工作主线：条件内容、上下文、跨时间信用、原生表达 | 当前处于下一候选方案决策点；L2仍是语言完成出口 |
| R3 持续适应 | 真实反馈后的合格更新、新旧任务与恢复 | 仍为后续范围；与R2有关的必要学习机制须在该包明确，不自动开启在线轴 |
| R4 同入口集成 | 同bundle消费链、真实执行、代表能力 | R2所需输入/状态/输出接线在R2内完成；联合行动任务未立项，不作为R2前置 |
| R5 扩面/缺口 | 知识准入、独立结构、身体全生命周期 | 按M5退出缺口另选，不同时打开 |
| R6 阶段/产品 | 轴评审、L3、M5退出、M6/M7采用发布 | 相应必达证据齐备后独立批准；包完成不等于阶段完成 |
| V 未来架构 | 长期高上限候选及替代方案 | 独立批准后才实施；不作为绕开当前负结果的入口 |

**设计建议与实施决定分离**：共同任务信念、联合发展训练和适应后表现等高上限方案保留在VISION，作为R2候选设计可参考的机制，不预先锁定新核心或Workbench任务。若小型实验直接判别R2语言假设、保持同一能力出口和批准预算，它是主线内方法；若需要新环境、额外能力轴或改变成功标准，则是范围变更，必须先批准，不能靠“只是最小实验”自动插队。

## 4. P3b-v2后继报告核对：退出队首，保留归因限制

本轮只读取[原始报告](../../../reports/p3b_v2_pilot_20260917.json)、[结案文字](../../reference/M5_P3B_V2_PILOT_CLOSURE_20260917.md)与源码，未加载checkpoint、训练、重评dev或读取final。

- 报告status为completed，code_revision为22954e66；dev有20条，teacher-forced accuracy为0.1968898（baseline 0），surprise为4.5277424（baseline 5.5984298），exact为0/20。报告已存在，旧“待完整报告”不再是当前状态。
- training.records为24次更新episode，只有12个不同episode，均属fact/negation并各出现两次；不能把语料40条train/总80行视作实际训练覆盖量。此事实说明证据范围，不证明补足覆盖就一定成功。
- config.response_plan_target_geometry和response_plan_target.geometry均为h3_7_factorized_response_chunks，与结案的byte-aligned描述不符。实际配置的未达标可记录，指定byte-aligned组合的归因不能据此成立。
- “按停止规则关闭该pilot”保留；“结构原因已唯一证明”不接受为因果结论。该预算下正确生成未形成，目标/表示/覆盖/容量/优化等仍未分离；不为弥补归因而自动加训。
- 原[收束记录](../../reference/PROJECT_CONSOLIDATION_20260917.md)描述当时被中断进程和旧checkpoint，作为历史事实保留。本轮不是新进程恢复验证，不将报告内read-only/preflight字段冒称本轮重验。

本项只修正当前证据读法和导航，后续不重复结案或把全量技术债清理作为能力形成讨论的前置；正式归因若要扩张，必须另有可辨识对照。

## 5. 当前唯一工作包：R2原生语言能力的候选方案决策

状态：**设计决策点；研究执行仍暂停，尚无新训练或默认迁移授权。**

当前要解决的是同一Taiji模型能否根据问题、给定材料和上下文，自主生成正确的原生短回答。L2仍是语言能力出口，Mini后置；本轮不把成功标准换成工件完成率、动作ID或宿主模板。

本工作包只产出**一个可评审、可证伪的R2候选变更规格**，不另开宏观路线或并行候选：

1. 依据H3系列及P3b-v2已有证据，列清已知事实和未分离因素；不重跑结案包、不把报告措辞当已证明根因。
2. 指定拟改变的训练目标/表示/信用路径与可复用资产，说明为什么该改动可能改善条件内容和自由生成；允许必要的核心学习设计变更，不把“只能修输出头”定成永久限制。
3. 写清问题/材料/上下文如何进入可训练状态，哪些参数接收回答误差，训练实际覆盖与评价如何匹配；必须区别梯度可达、训练可拟合、未见回答和自由生成。
4. 指定一个公平基线、主效应、保持/恢复及预算和停止线。若需小型诊断，优先使用R2文本/上下文范围内的可辨识任务；其结果必须决定该候选采用或否决，而不是独立刷一个玩具任务分数。
5. 明确与已停止H3.8机制族的实质区别；没有新假设就不换名称追加预算。实现、正式训练和核心迁移在相关规格/权限确认后才继续。

**边界**：不建立新的Workbench任务环境，不同时展开世界模型训练、行动规划、在线适应或结构成长，不要求先完成联合能力验证才允许继续R2。相应高上限设计保留在[唯一VISION](../../reference/VISION_FUTURE_TECHNOLOGY.md)，仍受全项目联合能力验收原则约束，但不成为本包前置。

**出口与停止**：提交一个候选的计算/学习规格、判别实验和资源申请，供用户作采用或否决决策；不是仅交一份新的总计划。不能形成可辨识假设时，明确说明缺少哪项证据并停下讨论，不自行改换主线。“继续主线”不等于续训已负结果结项的配置。

## 6. 防止规则再次变成僵硬闸门

- P层边界不能豁免；C层只约束相应能力声明；E层允许有迁移/回退的版本替换；X层在新实验中可更换方法。完整规则见架构§0.2/§8.5。
- 路由/睡眠/成长先证明操作效力，再证明学习策略与自主触发；固定控制可作公开原型/基线，不把原型叫成熟能力，也不禁止先研究机制。
- 基础联合有效性与更强协作涌现分级；恢复一致性、行为保持与参数位级不变分账。旧精确门不追改，新主张依风险冻结容差。
- 失败先定位到合同实现、仪器、证据不足或机制负结果，不把每个失败变成更大的新门。新增硬门必须写具体风险和可关闭条件。
- 结案报告区分事实/假设/因果结论/资源决策；“停止某候选”不需要证明整类理论不可能。
- 外部动作结果未知时先对账，不盲重试；模型恢复不等于环境撤销。低风险缺信息可澄清/降级，高风险权限不明拒绝相关动作。
- 新数据通过显式warm-start新血缘进入，不绕过现有corpus校验；当前尚无相关迁移实现时标待实现。
- 本次已核对代码允许backward；保留所有权与现有禁止外部认知检查，不再以过时AST禁令阻塞方法讨论。

## 7. 大阶段、用户验收与剩余缺口不消失

| 出口 | 必达条件及当前状态 |
|---|---|
| 单包结案 | 正/负/不可判均可结束；不得自动关轴或生新分支 |
| 协作/知识/在线/身体轴 | 各自冻结范围、独立证据、保持/恢复/安全/预算与批准；K限定批准继续继承 |
| L2/L3 | L2原生基本对话＋L3同bundle代表能力与安全/性能；Mini仍后置，不能只交动作ID |
| M5退出 | 知识准入、协作/选择、在线/身体与共同门、完整CAP；排除必达范围须显式批准 |
| M4成长 | 结构贡献、同最终容量对照、保持/资源/恢复；不因U或K通过而完成 |
| M6采用 | 指定artifact准入、shadow/canary、客户端能力与失败语义一致；未经批准不改默认 |
| M7发布 | 当前版本实际CI、安装/manifest、端到端、安全和回退，另行发布批准 |
| M8规模化 | 真设备同质量/恢复和端到端成本收益；不阻塞CPU能力验证 |

知识P5.1h、B2跨内容结构、身体能力注册/撤销与补偿、多系统记忆/生命调节/自主成长仍在[01总图](01_SCOPE_AND_PHASES.md)，不从清单抹去，也不全变成本包前置。需要扩面时一次选一个与阶段出口直接相关的缺口，并说明为何现有切片不足。

## 8. CI、历史与维护

文档改动做链接、相互引用、状态/权限与差异检查；实现改动运行相关合同/恢复/隔离回归。风险相称，不把文档检查称为模型测试或当前HEAD远端CI通过。

上一轮规则修订核验：8份文档、126个本地链接、5个锚点无缺失；唯一执行队首、8条规则登记及diff空白检查通过；归档正文除相对链接重定位外与ad812961版本一致。未运行训练、能力评测或全量测试，未修改模型/默认入口。

本轮能力形成讨论核验：8份修订文档、99个本地链接、4个锚点无缺失，diff空白检查通过；只读检查P3b-v2 JSON指标/配置/训练记录和现有训练源码，并参考原始研究。没有修改实验报告、checkpoint、代码或训练权限，不声称能力测试或当前远端CI通过。

已记录的远端run34869725409对应d09dcc21且总体失败，不是当前HEAD状态；本次不凭旧记录声称当前CI红或绿。相关技术债见[05](05_TECH_DEBT_REGISTER.md)，读取时核对条目证据与后继修订，不按日期自动覆盖不同范围规则。任何可能写产品checkpoint的测试先验证隔离。

本轮清理的是活动执行页中的旧命令，完整旧正文保留在归档；报告、checkpoint、源码和失败证据均不删除。每包结束更新本页状态与实际受影响的01/02/07/首页，提交仅本任务改动、不自动push。暂停与授权状态变化必须显式记录，不由历史“继续推进”字样恢复。
